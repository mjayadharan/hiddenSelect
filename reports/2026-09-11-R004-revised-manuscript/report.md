# Sparse Optimization using Multiple Shooting and Guess Propagation — revised manuscript (R004)

**Report ID:** R004 (`reports/2026-09-11-R004-revised-manuscript/`)  
**Date:** 11 September 2026  
**Authoritative source:** `report.tex` / `report.pdf` — this `.md` is a derived, full-content view generated by `analysis/make_report_md.py`; on disagreement the `.tex` wins.  
**Gates:** 9 of 10 gates pass — `analysis/results/gates_summary.json`

Theorem-like statements are numbered Lemma 1 (state), Lemma 2 (parameters), Lemma 3 (largest residual), Proposition 1 (cost at the truth), Proposition 2 (minimiser vs truth), Proposition 3 (node removal), Theorem 1 (minimiser under node removal), Corollary 1 (per-window exponents), matching the PDF.

# Multiple shooting and guess propagation: a greedy approach

#### Problem to solve.

Discover the system of governing equations
``` math
\begin{equation}
\frac{\,\mathrm{d}\mathbf x}{\,\mathrm{d}t}=\mathbf F(\mathbf x;p),\qquad \mathbf x\in C^1(\mathbb R)^{n},
\label{eq:11}
\end{equation}
```
such that the trajectory matches the observations $`\{\mathbf y_0,\mathbf y_1,\dots,\mathbf y_N\}`$ at the time points $`\{t_0,t_1,\dots,t_N\}\subset[0,t_N]`$. Suppose there is a library of candidate functions $`\Theta=\{\Theta_1(\mathbf x),\dots,\Theta_J(\mathbf x)\}`$ such that <a href="#eq:11" data-reference-type="eqref" data-reference="eq:11">(eq. 11)</a> can be written as
``` math
\begin{equation}
\frac{\,\mathrm{d}x_i}{\,\mathrm{d}t}=\sum_{j=1}^{J}p_{ij}\,\Theta_j(\mathbf x),\qquad i=1,\dots,n,
\label{eq:12}
\end{equation}
```
or, more generally, with library functions that carry their own parameters, $`\dot x_i=\sum_j p_{ij}\Theta_j(\mathbf x;\alpha_{ij})`$. Model discovery then means finding the coefficients $`\{p_{ij}\}`$ (and $`\{\alpha_{ij}\}`$) that make these equations agree with the observations, with most coefficients equal to zero. Figure <a href="#fig:problem" data-reference-type="ref" data-reference="fig:problem">1</a> shows the instance used throughout the numerical section: the FitzHugh–Nagumo (FHN) oscillator written over the ten cubic monomials in two states, so that $`7`$ of the $`20`$ coefficients are non-zero.

**Figure 1** (`figures/R002_fig01_problem.pdf`)

!1(figures/R002_fig01_problem.pdf)

***The inverse problem.** (a) The FHN states $`v(t)`$ (blue) and $`w(t)`$ (orange) with the $`N=101`$ noisy samples (dots) at spacing $`\Delta t=1`$; the thin dashed line is the exact trajectory. (b) Phase plane with the vector field, the true orbit and the samples. (c) The unknown $`2\times10`$ coefficient matrix over the library $`\{1,w,w^2,w^3,v,vw,vw^2,v^2,v^2w,v^3\}`$: only 7 of 20 entries are non-zero. The task is to recover the non-zero pattern and the values from the dots in (a). Inherited from R002 Figure 1 (‘figures/R002_fig01_problem.pdf‘).*


#### Loss functions.

Assuming all states are measured, the model-only loss is
``` math
\begin{equation}
J(p)=\frac1N\sum_{k=1}^{N}\lVert \mathbf y_k-\mathbf x_{m,k}\rVert^2,\qquad \mathbf x_{m,k}=x_m(t_k;p),
\label{eq:loss_func_1}
\end{equation}
```
where $`x_m(t;p)`$ is the model trajectory. A variant that also accounts for errors in the observations adds a data term,
``` math
\begin{equation}
\mathbf L(p,\mathbf x)=R_1\frac1{N+1}\sum_{k=0}^{N}\lVert \mathbf x_k-\mathbf y_k\rVert^2+R_2\frac1N\sum_{k=0}^{N-1}\lVert \mathbf x_k-\mathbf x_{m,k}\rVert^2,
\label{eq:loss_fun_2}
\end{equation}
```
which is useful for sparse and noisy data. The analysis below uses <a href="#eq:loss_func_1" data-reference-type="eqref" data-reference="eq:loss_func_1">(eq. loss_func_1)</a>.

#### Integration and the difficulty of single shooting.

Let $`\delta t`$ be the smallest time step of the integration scheme and $`F_{\delta t}`$ its one-step operator, $`x_m(t+\delta t;p)=F_{\delta t}(x_m(t;p))`$. Tracing the trajectory from $`\mathbf x_0\approx\mathbf y_0`$ to $`t_k`$ composes the operator $`S=(t_k-t_0)/\delta t`$ times, $`x_{m,k}=F^{(S)}_{\delta t}(x_0;p)`$. Many compositions introduce local minima into the loss landscape, make the gradient unstable, and let the trajectory blow up for unstable parameter values. Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a> shows the effect on FHN: a parameter vector only $`0.038`$ away from the truth (in the Euclidean norm on the 20 coefficients) slips phase after two periods and then pays the full size of the attractor in every residual, so the single-shooting cost is $`2.60`$ against $`0.0099`$ at the truth.

**Figure 2** (`figures/R002_fig02_single_shooting.pdf`)

!2(figures/R002_fig02_single_shooting.pdf)

***Single shooting.** The model is integrated once from the first datum $`y_0`$ over the whole record with a wrong parameter $`p_{\rm wrong}`$ (cubic coefficient $`-0.30`$ instead of $`-1/3`$, $`\dot w`$ time-scale multiplied by $`1.15`$; $`\lVert p_{\rm wrong}-p^\star\rVert=0.038`$); thin vertical lines are the residuals. The residual (right, log scale) sits at the noise level until the model slips phase near $`t\approx18`$, then grows and saturates at the size of the attractor; the cost is $`J_{100}(p_{\rm wrong})=2.60`$ against $`0.0099`$ at $`p^\star`$. Inherited from R002 Figure 2 (‘figures/R002_fig02_single_shooting.pdf‘).*


#### Multiple shooting.

One way to tackle this is to restart the trajectory from several points of the record. Partition $`[t_0,t_N]`$ into $`K`$ windows $`[\tau_{k-1},\tau_k]`$ with nodes $`t_0=\tau_0<\tau_1<\dots<\tau_K=t_N`$; for simplicity we take $`\{\tau_k\}\subset\{t_0,\dots,t_N\}`$ (Figure <a href="#fig:partition" data-reference-type="ref" data-reference="fig:partition">3</a>). Each node is the initial condition of the model trajectory on its window, and the cost is
``` math
\begin{equation}
J_K(p)=\sum_{k=1}^{K}\ \sum_{t_i\in(\tau_{k-1},\tau_k]}\lVert x_m(t_i;p,y_{\tau_{k-1}})-y_i\rVert^2,\qquad x_m(t_i;p,y_{\tau_{k-1}})=F^{(S_i)}_{\delta t}(y_{\tau_{k-1}};p).
\label{eq:JK_intro}
\end{equation}
```
The node states are the noisy data themselves; they are *not* optimisation variables and no continuity is enforced at the window ends (this differs from textbook multiple shooting). If $`t_i`$ is not an integrator step point the solver interpolates. As the error analysis of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a> shows, restarting resets the propagation of the initial-condition error at every node, so the amplification is paid over one window instead of over the whole record (Figure <a href="#fig:multi" data-reference-type="ref" data-reference="fig:multi">4</a>). Our goal is not to replace a solver that starts at the first observation but to provide it with a good initial guess: the parameters found with windows are handed to the full single-shooting problem, which avoids its stability problems and lands near its global minimum.

**Figure 3** (`figures/R002_fig03_partition.pdf`)

!3(figures/R002_fig03_partition.pdf)

***The shooting partition.** Top: the data times $`t_0,\dots,t_N`$ (ticks, spacing $`\Delta t`$) and the shooting nodes $`\tau_0<\tau_1<\dots<\tau_K`$ placed at every $`\kappa`$-th datum; the windows $`[\tau_{k-1},\tau_k]`$ of length $`\Delta T=\kappa\Delta t`$ are shaded alternately. Bottom: the coarser partition obtained by removing every other node ($`K/2`$ windows of length $`2\Delta T`$), the elementary step of guess propagation. Schematic; replaces the hand-drawn `shooting_partition.png` of the draft. Inherited from R002 Figure 3.*


**Figure 4** (`figures/R002_fig04_multiple_shooting.pdf`)

!4(figures/R002_fig04_multiple_shooting.pdf)

***Multiple shooting.** The same wrong parameter as Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a>, integrated (a) as single shooting ($`\kappa=100`$, $`J=2.60`$) and (b) restarting from the datum at every 10th sample ($`\kappa=10`$, $`J=0.0140`$); windows shaded, node markers highlighted; (c) the per-datum residual magnitude for $`\kappa=100,10,5`$. Restarting resets the accumulated error at every node. The flip side, visible in (c): short windows keep even a wrong model near the noise floor, i.e. they give a smooth landscape at the price of less power to discriminate $`p`$ from $`p^\star`$, which is why the sweep must end at single shooting. Replaces `multiple_shooting.png`. Inherited from R002 Figure 4.*


#### Guess propagation.

Start with many shooting nodes, solve, remove nodes, and restart the optimisation from the previous optimum (Algorithm <a href="#alg:gp" data-reference-type="ref" data-reference="alg:gp">Algorithm 1</a>, Figure <a href="#fig:gp" data-reference-type="ref" data-reference="fig:gp">5</a>). With one window per data interval and $`\delta t=\Delta t`$ the problem is close to a finite-difference (SINDy-type) regression and nearly convex; as nodes are removed the model is enforced over longer and longer horizons and the landscape acquires local minima, but the optimiser is already inside the right basin. In the later sections we show that, under suitable assumptions, the optimum moves by a bounded amount at each node removal.

```
<div class="algorithmic">
$`p_{\rm guess}←`$ random $`p^{(K)}←\arg\min_p J_K(p)`$ started from $`p_{\rm guess}`$ $`p_{\rm guess}← p^{(K)}`$ $`p^{(1)}`$ (the single-shooting fit)
</div>
```


**Figure 5** (`figures/R002_fig06_guess_propagation.pdf`)

!5(figures/R002_fig06_guess_propagation.pdf)

***The guess-propagation algorithm.** One-dimensional slices $`J_\kappa(p^\star+s\,u)`$ along one random unit direction $`u`$ (the one with the most local minima at $`\kappa=100`$: $`1,4,8,25`$ local minima at $`\kappa=1,5,25,100`$, circled) on a log scale; hatched band = blow-up plateau; star = global minimum at $`s=0`$. Small windows give a smooth, nearly convex landscape; large windows add local minima and a wide plateau. The algorithm solves the $`\kappa=1`$ problem from a random start and carries each minimiser (arrow) into the next, more rugged landscape, ending with a single-shooting polish. Inherited from R002 Figure 6.*


# Notation and definitions

Everything used in the statements, captions and tables is defined here, including what is inherited from the earlier draft and from reports R002 and R003; the provenance column says where each item was fixed.

## Model, flow, data and costs

Consider $`\dot x=f(x;p)`$ with $`x\in\mathbb R^d`$ and $`p\in\mathbb R^m`$ (the earlier draft wrote $`p\in\mathbb R`$; nothing below uses a scalar parameter). $`p^\star`$ is the true parameter and $`x^\star(t)`$ the true trajectory from $`x_0`$. The *flow* $`\varphi_f(t;\,p,\,x_0)=X(t)`$ is the solution of the initial-value problem at time $`t`$; it satisfies $`\varphi_f(t_1+t_2;\,p,\,x_0)=\varphi_f(t_2;\,p,\,\varphi_f(t_1;\,p,\,x_0))`$. In the analysis the flow replaces the numerical integrator, i.e. discretisation error is ignored. Data are $`y_i=x_i+\eta_i`$ with $`x_i=x^\star(t_i)`$ and $`\eta_i\sim\mathcal N(0,\sigma^2 I_d)`$ independent, $`i=0,\dots,N`$, spacing $`\Delta t`$; $`\lVert \eta\rVert_{\max}=\max_i\lVert \eta_i\rVert`$. Shooting nodes $`t_0=\tau_0<\dots<\tau_K=t_N`$ are a subset of the data times; $`\tau(i)`$ denotes the last node at or before $`t_i`$, $`\Delta T=\max_k(\tau_k-\tau_{k-1})`$ the longest window, $`n_k`$ the number of data in window $`k`$ and $`n_{\max}=\max_k n_k`$. The costs are
``` math
\begin{align}
J_K(p)&=\sum_{k=1}^{K}\sum_{t_i\in(\tau_{k-1},\tau_k]}\lVert \varphi_f(t_i-\tau_{k-1};\,p,\,y_{\tau_{k-1}})-y_i\rVert^2 &&\text{(multiple shooting, $K$ windows),}\label{eq:JK}\\
J_1(p)&=\sum_{i=1}^{N}\lVert \varphi_f(t_i-t_0;\,p,\,y_0)-y_i\rVert^2 &&\text{(single shooting),}\label{eq:J1}\\
J_K^\star(p)&=\sum_{k=1}^{K}\sum_{t_i\in(\tau_{k-1},\tau_k]}\lVert \varphi_f(t_i-\tau_{k-1};\,p,\,x_{\tau_{k-1}})-x_i\rVert^2 &&\text{(noise-free windowed cost).}\label{eq:JKstar}
\end{align}
```
$`J_K^\star`$ starts every window from the *true* state and compares with the true state, so $`J_K^\star(p^\star)=0`$ and $`p^\star=\arg\min J_K^\star`$ for every partition. **Read this:** the earlier draft compared $`J_K`$ with the noise-free *single-shooting* cost $`J^\star=J_1^\star`$; the decomposition used in its proof actually bounds the distance to $`J_K^\star`$, which is the comparator used here. Both vanish at $`p^\star`$, so the conclusions are unchanged. $`p^{(K)}=\arg\min_p J_K(p)`$. For node removal, $`\mathcal I_R\subset\{1,\dots,K-1\}`$ indexes the removed nodes, $`\hat{\mathcal J}_K`$ is the cost of the coarser partition, $`\hat p^{(K)}`$ its minimiser, and for $`k\in\mathcal I_R`$, $`\tau_k^-`$ and $`\tau_k^+`$ are the nearest remaining nodes to the left and right of $`\tau_k`$; $`\Delta T_1=\max_k(\tau_k-\tau_{k-1})`$ is the fine window length and $`\Delta T_2=\max_{k\in\mathcal I_R}(\tau_k-\tau_k^-)`$. $`D_k=\{t_i\in(\tau_k,\tau_{k+1}]\}`$ is the fine window launched at the removed node $`\tau_k`$.

## Constants of the right-hand side

$`J_f(x)=\partial f/\partial x`$. The Lipschitz constants are $`L=\sup_x\lVert J_f(x)\rVert_2`$ (in $`x`$, for fixed $`p`$) and $`\tilde L=\sup\lVert \partial f/\partial p\rVert_2`$ (in $`p`$). The *logarithmic norm* (one-sided Lipschitz constant, Dahlquist) and its lower counterpart are
``` math
\begin{equation}
\mu=\sup_x\lambda_{\max}\!\Big(\tfrac12\big(J_f(x)+J_f(x)^\top\big)\Big),\qquad \mu_-=\inf_x\lambda_{\min}\!\Big(\tfrac12\big(J_f(x)+J_f(x)^\top\big)\Big),
\label{eq:mu}
\end{equation}
```
equivalently $`\mu_-\lVert x-z\rVert^2\le\langle f(x)-f(z),x-z\rangle\le\mu\lVert x-z\rVert^2`$. Always $`-L\le\mu_-\le\mu\le L`$; unlike $`L`$, $`\mu`$ can be zero or negative. The suprema are over the region containing the trajectories considered; for FHN the value of $`\mu`$ is global (§<a href="#sec:lemma1" data-reference-type="ref" data-reference="sec:lemma1">3.1</a>). For a weighted norm $`\lVert x\rVert_D=\lVert Dx\rVert`$ with $`D`$ invertible, $`\mu_D`$ is the logarithmic norm of $`DJ_fD^{-1}`$ and $`\kappa(D)=\lVert D\rVert\lVert D^{-1}\rVert`$ its condition number. The *local* logarithmic norm along a trajectory is $`\mu(t)=\lambda_{\max}(\tfrac12(A(t)+A(t)^\top))`$ for the matrix $`A(t)`$ of the mean-value form, and $`\Lambda_k=\int_{\tau_{k-1}}^{\tau_k}\mu(s)\,\mathrm{d}s`$ is the window exponent. The abbreviation used in the bounds is
``` math
\begin{equation}
\rho_s(p)=\big(1+e^{\mu s}\big)\lVert \eta\rVert_{\max}+\frac{\tilde L}{\mu}\big(e^{\mu s}-1\big)\lVert p-p^\star\rVert,\qquad s\ge0,
\label{eq:rho}
\end{equation}
```
the largest residual a window of length $`s`$ can produce at parameter $`p`$ (Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">3</a>). $`m>0`$ denotes a strong-convexity constant of a cost near its minimiser (the draft wrote $`\mu`$ for it; renamed to avoid the clash with the logarithmic norm).

## Numerical protocol (R002)

The target is FHN, $`\dot v=v-\tfrac13v^3-w+0.5`$, $`\dot w=\tfrac1{12.5}(v+0.7-0.8w)`$, written over the cubic library $`\Theta=\{1,w,w^2,w^3,v,vw,vw^2,v^2,v^2w,v^3\}`$ ($`J=10`$, $`p\in\mathbb R^{20}`$, 7 non-zeros). Data: fixed-step Tsit5 at $`0.01`$ from $`(1,1)`$ to $`T=156`$, every 100th point kept, cropped to $`t\in[50,150]`$ and shifted to $`[0,100]`$ ($`N=101`$, $`\Delta t=1`$), Gaussian noise of standard deviation $`0.05\times`$ each component’s own standard deviation; $`\lVert \eta\rVert_{\max}=0.159`$, noise floor $`\tfrac1N\sum\lVert \eta_i\rVert^2=0.0052`$. The optimised cost is the normalised, sparsity-penalised version of <a href="#eq:JK" data-reference-type="eqref" data-reference="eq:JK">(eq. JK)</a>,
``` math
\begin{equation}
J_\kappa(z)=\frac1N\Big[\lVert x_0-y_0\rVert^2+\sum_{i=1}^{N-1}\lVert \hat x(t_{i+1};p,y_{\tau(i)})-y_{i+1}\rVert^2\Big]+\frac{\gamma}{20}\sum_{j=1}^{20}\operatorname{smooth}\ell_1(p_j),\qquad z=[x_0;p]\in\mathbb R^{22},
\label{eq:Jkappa}
\end{equation}
```
where $`\kappa`$ is the *window size* in data intervals ($`K=\lceil(N-1)/\kappa\rceil`$ windows; $`\kappa=1`$ is one window per interval, $`\kappa=100`$ is single shooting), each interval is integrated with $`S=10`$ Tsit5 sub-steps, $`\operatorname{smooth}\ell_1(x)=\alpha^{-1}[\log(1+e^{-\alpha x})+\log(1+e^{\alpha x})]`$ with $`\alpha=500`$, and $`\gamma=0.05`$. If a state exceeds $`10^3`$ or becomes NaN the loss returns $`10^3`$ (*flat blow-up penalty*; a plateau with zero gradient); the *graded* penalty returns $`10^3(1+\tfrac{N-1-i}{N-1})+J_{\rm partial}/N`$ when the blow-up occurs at index $`i`$. The theory checks use $`\gamma=0`$.

*Arms.* **GP**: sweep $`\kappa`$ upward through the FULL schedule $`\{1,2,3,4,5,6,8,10,12,\allowbreak15,20,25,33,50,75,100\}`$, each stage started from the previous minimiser. **best-GP**: as GP but started from the lowest-cost minimiser seen so far. **Control** (no propagation): every stage started from the same random seed vector. Optimiser: Nelder–Mead, 2500 iterations per stage, unless stated; BFGS/L-BFGS use the exact automatic-differentiation gradient. Seeds $`s=1,\dots,8`$: $`z_{\rm seed}=[y_0;0.1\,\xi]`$, $`\xi\sim\mathcal N(0,I_{20})`$, giving $`\lVert p_0-p^\star\rVert\in[1.50,1.84]`$. Arms are compared at equal budgets (same iterations, schedule and seeds) unless a table says otherwise. Other schedules: SHORT $`\{1,2,5,10,25,50,100\}`$ (variations), DENSE (44 stages), COARSE $`\{1,5,25,100\}`$, JUMP $`\{1,100\}`$.

*Metrics.* $`J_\kappa`$ at the returned minimiser (values $`\ge10^3`$ flag the plateau); the *parameter error* $`\lVert p-p^\star\rVert`$ (Euclidean, 20 coefficients; the identification metric and the primary one here); the *recovery score* in $`[0,1]`$: each coefficient earns credit 1 if it is correctly zero ($`|p_j|<0.05`$ when $`p^\star_j=0`$) or correctly non-zero within 25 % of the truth, averaged over the 20 coefficients; the *paired difference* of $`\lVert p-p^\star\rVert`$ between two arms sharing a seed, reported as the mean with a 95 % percentile bootstrap CI over 2000 resamples of the seeds (the resampling unit is the optimiser seed), with *cellwise* counts of seeds in which one arm is strictly better.

## Symbols and frozen constants

<div class="center">

| symbol | meaning | value | where frozen |
|:---|:---|:---|:---|
| $`N`$, $`\Delta t`$ | samples, spacing | $`101`$, $`1`$ | R002 data recipe |
| $`\kappa`$, $`K`$ | window size (data intervals), number of windows | swept, $`\lceil100/\kappa\rceil`$ | R002 schedules |
| $`\Delta T`$ | window length $`\kappa\Delta t`$ | swept | draft, §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a> |
| $`S`$, $`\delta t`$ | integrator sub-steps per interval, step | $`10`$, $`0.1`$ | R002 driver |
| $`\gamma`$, $`\alpha`$ | sparsity weight, smooth-$`\ell_1`$ sharpness | $`0.05`$, $`500`$ | R002 driver |
| $`\lVert \eta\rVert_{\max}`$ | largest noise norm | $`0.159`$ | R002 `fhn_data_meta` |
| $`L`$ | Lipschitz constant in $`x`$ along the orbit / on $`[-2.5,2.5]\times[-1,2]`$ | $`3.05`$ / $`5.34`$ | R002 `concept_meta` |
| $`\mu`$, $`\mu_-`$ | logarithmic norm and lower counterpart along the orbit | $`1.17`$, $`-2.96`$ | R002 / R003 |
| $`\tilde L`$ | Lipschitz constant in $`p`$ along the orbit | $`10.56`$ | R002 `concept_meta` |
| $`D`$, $`\kappa(D)`$ | weighting $`\mathrm{diag}(1,s)`$, $`s=\sqrt{|J_{12}|/|J_{21}|}`$ | $`s=3.54`$ | R003 |
| $`m`$ | strong-convexity constant near a minimiser | assumed | draft (as “$`\mu`$”) |
| blow-up threshold | $`|x|>10^3`$ or NaN | $`10^3`$ | R002 loss |

</div>

**Read this:** $`L`$ and $`\mu`$ are measured along the true orbit, not assumed. The numerical section shows that the amplification of an initial-state error on the FHN limit cycle never exceeds $`8.5`$; bounds carrying $`e^{L\Delta T}`$ are loose by orders of magnitude for $`\Delta T\gtrsim2`$, bounds carrying $`e^{\mu\Delta T}`$ by much less, and the per-window exponents of §<a href="#sec:refine" data-reference-type="ref" data-reference="sec:refine">5</a> are close to the measurement.

# Convergence analysis of multiple shooting

Throughout, $`f`$ is continuously differentiable with the constants of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a> finite on the region of interest. Statements marked *(revised)* differ from the earlier draft; each is followed by the change and its effect.

## Sensitivity of the flow to the initial state

<div id="lem:state" class="lemma">

**Lemma 1** (flow sensitivity to the initial state; *revised*). *For all $`t\ge0`$, $`p`$ and $`x_1,x_2`$,
``` math
\begin{equation}
e^{\mu_- t}\lVert x_1-x_2\rVert\ \le\ \lVert \varphi_f(t;\,p,\,x_1)-\varphi_f(t;\,p,\,x_2)\rVert\ \le\ e^{\mu t}\lVert x_1-x_2\rVert.
\label{eq:lemma1}
\end{equation}
```
In particular, since $`\mu\le L`$ and $`\mu_-\ge-L`$, the draft’s bounds $`e^{-Lt}\lVert x_1-x_2\rVert\le\lVert \cdot\rVert\le e^{Lt}\lVert x_1-x_2\rVert`$ hold.*

</div>

<div class="proof">

*Proof.* Let $`\hat x_j(t)=\varphi_f(t;\,p,\,x_j)`$ and $`\delta=\hat x_1-\hat x_2`$, so $`\dot\delta=f(\hat x_1;p)-f(\hat x_2;p)`$ with $`\delta(0)=x_1-x_2`$. By the mean-value form, $`f(\hat x_1;p)-f(\hat x_2;p)=A(t)\delta`$ with $`A(t)=\int_0^1J_f(\hat x_2+s\delta)\,\mathrm{d}s`$, hence
``` math
\begin{equation}
\tfrac12\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\lVert \delta\rVert^2=\langle\delta,A(t)\delta\rangle=\big\langle\delta,\tfrac12(A+A^\top)\delta\big\rangle\in\big[\mu_-\lVert \delta\rVert^2,\ \mu\lVert \delta\rVert^2\big].
\label{eq:proof1}
\end{equation}
```
Thus $`\mu_-\lVert \delta\rVert\le\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\lVert \delta\rVert\le\mu\lVert \delta\rVert`$ wherever $`\delta\ne0`$ (and $`\delta\equiv0`$ otherwise by uniqueness), and Grönwall’s inequality gives <a href="#eq:lemma1" data-reference-type="eqref" data-reference="eq:lemma1">(eq. lemma1)</a>. ◻

</div>

*What changed.* The draft bounded $`\lVert \dot\delta\rVert\le L\lVert \delta\rVert`$ and integrated $`\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\ln\lVert \delta\rVert`$. That step applies Cauchy–Schwarz to $`\langle\delta,\dot\delta\rangle`$ and discards the sign information in the inner product; <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> keeps it. On FHN the Jacobian is $`\begin{psmallmatrix}1-v^2&-1\\0.08&-0.064\end{psmallmatrix}`$ and $`1-v^2\le1`$ for every $`v`$, so $`\mu=1.17`$ on all of $`\mathbb R^2`$ (attained at $`v=0`$), whereas $`L`$ grows like $`v^2`$ and is $`3.05`$ on the orbit but $`5.34`$ on the box. The revised lemma therefore holds for perturbations of any size with one constant. At $`t=\Delta T=2`$ the bound falls from $`e^{L\Delta T}=447`$ to $`e^{\mu\Delta T}=10.4`$ against a measured peak of $`6.9`$ (Table <a href="#tab:lemmas" data-reference-type="ref" data-reference="tab:lemmas">1</a>).

## Sensitivity of the flow to the parameters

<div id="lem:param" class="lemma">

**Lemma 2** (flow sensitivity to the parameters; *revised*). *For all $`t\ge0`$, $`x_0`$ and $`p_1,p_2`$,
``` math
\begin{equation}
\lVert \varphi_f(t;\,p_1,\,x_0)-\varphi_f(t;\,p_2,\,x_0)\rVert\le\frac{\tilde L}{\mu}\big(e^{\mu t}-1\big)\lVert p_1-p_2\rVert,
\label{eq:lemma2}
\end{equation}
```
where the right-hand side is read as $`\tilde L\,t\lVert p_1-p_2\rVert`$ when $`\mu=0`$ and as $`\frac{\tilde L}{|\mu|}(1-e^{-|\mu|t})\lVert p_1-p_2\rVert\le\frac{\tilde L}{|\mu|}\lVert p_1-p_2\rVert`$ when $`\mu<0`$.*

</div>

<div class="proof">

*Proof.* With $`\hat x_j(t)=\varphi_f(t;\,p_j,\,x_0)`$ and $`\delta=\hat x_1-\hat x_2`$, $`\delta(0)=0`$, split $`\dot\delta=\big[f(\hat x_1;p_1)-f(\hat x_2;p_1)\big]+\big[f(\hat x_2;p_1)-f(\hat x_2;p_2)\big]`$: the first bracket is the state difference at equal parameters, the second the parameter difference at equal state. Taking the inner product with $`\delta`$, the first bracket contributes at most $`\mu\lVert \delta\rVert^2`$ by <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> and the second at most $`\tilde L\lVert \delta\rVert\lVert p_1-p_2\rVert`$ by the Lipschitz property in $`p`$, so $`\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\lVert \delta\rVert\le\mu\lVert \delta\rVert+\tilde L\lVert p_1-p_2\rVert`$. Multiplying by the integrating factor $`e^{-\mu t}`$ and integrating from $`0`$ to $`t`$ gives <a href="#eq:lemma2" data-reference-type="eqref" data-reference="eq:lemma2">(eq. lemma2)</a>. ◻

</div>

*What changed.* Only the first bracket’s bound, $`L\to\mu`$, as in Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a>. The consequence is qualitative: for a dissipative system ($`\mu<0`$) the revised bound *saturates* at $`\tilde L/|\mu|`$, which is the shape the measurement has (Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a>b), whereas $`\frac{\tilde L}{L}(e^{Lt}-1)`$ can never saturate because a Lipschitz constant is non-negative.

<div id="lem:residual" class="lemma">

**Lemma 3** (largest residual of a window). *Let $`\tau`$ be a node and $`t_i\in(\tau,\tau+s]`$. Then
``` math
\begin{equation}
\lVert \varphi_f(t_i-\tau;\,p,\,y_\tau)-y_i\rVert\ \le\ e^{\mu(t_i-\tau)}\lVert \eta_\tau\rVert+\lVert \varphi_f(t_i-\tau;\,p,\,x_\tau)-x_i\rVert+\lVert \eta_i\rVert\ \le\ \rho_s(p).
\label{eq:residual}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* Write $`\varphi_f(t_i-\tau;\,p,\,y_\tau)-y_i=\big[\varphi_f(t_i-\tau;\,p,\,y_\tau)-\varphi_f(t_i-\tau;\,p,\,x_\tau)\big]+\big[\varphi_f(t_i-\tau;\,p,\,x_\tau)-x_i\big]-\eta_i`$ and apply the triangle inequality; Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a> bounds the first bracket by $`e^{\mu(t_i-\tau)}\lVert \eta_\tau\rVert`$. For the second bracket note $`x_i=\varphi_f(t_i-\tau;\,p^\star,\,x_\tau)`$, so Lemma <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">2</a> bounds it by $`\frac{\tilde L}{\mu}(e^{\mu(t_i-\tau)}-1)\lVert p-p^\star\rVert`$; with $`t_i-\tau\le s`$ and $`\lVert \eta_\tau\rVert,\lVert \eta_i\rVert\le\lVert \eta\rVert_{\max}`$ this is $`\rho_s(p)`$ of <a href="#eq:rho" data-reference-type="eqref" data-reference="eq:rho">(eq. rho)</a>. ◻

</div>

## The windowed cost near the truth

<div id="prop:cost" class="proposition">

**Proposition 1** (cost at the true parameter; *revised*). *For any partition with longest window $`\Delta T`$ and $`n_k`$ data in window $`k`$,
``` math
\begin{align}
J_K(p)&\le\sum_{k=1}^{K}\sum_{t_i\in(\tau_{k-1},\tau_k]}\Big(e^{\mu(t_i-\tau_{k-1})}\lVert \eta_{\tau_{k-1}}\rVert+\lVert \varphi_f(t_i-\tau_{k-1};\,p,\,x_{\tau_{k-1}})-x_i\rVert+\lVert \eta_i\rVert\Big)^2,\label{eq:JK_bound}\\
J_K(p^\star)&\le\lVert \eta\rVert_{\max}^2\sum_{k=1}^{K}\sum_{j=1}^{n_k}\big(1+e^{\mu j\Delta t}\big)^2\ \le\ N\lVert \eta\rVert_{\max}^2\big(1+e^{\mu\Delta T}\big)^2,\label{eq:JK_true}\\
\mathbb E\,J_K(p^\star)&\le d\sigma^2\sum_{k=1}^{K}\sum_{j=1}^{n_k}\big(1+e^{2\mu j\Delta t}\big)\ \le\ N d\sigma^2\big(1+e^{2\mu\Delta T}\big).\label{eq:JK_expect}
\end{align}
```
As $`\eta\to0`$, $`J_K(p)\to J_K^\star(p)`$ uniformly on bounded sets of $`p`$, and $`J_K^\star(p^\star)=0`$. For single shooting the same argument gives $`J_1(p)\le\sum_{i=1}^N\big(e^{\mu(t_i-t_0)}\lVert \eta_0\rVert+\lVert \varphi_f(t_i-t_0;\,p,\,x_0)-x_i\rVert+\lVert \eta_i\rVert\big)^2`$, in which the initial-condition error is amplified over the whole record.*

</div>

<div class="proof">

*Proof.* <a href="#eq:JK_bound" data-reference-type="eqref" data-reference="eq:JK_bound">(eq. JK_bound)</a> is Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">3</a> squared and summed. At $`p^\star`$ the middle term vanishes; the $`j`$-th datum of a window sits at $`t_i-\tau_{k-1}=j\Delta t`$, which gives <a href="#eq:JK_true" data-reference-type="eqref" data-reference="eq:JK_true">(eq. JK_true)</a>, and $`j\Delta t\le\Delta T`$ the crude form. For <a href="#eq:JK_expect" data-reference-type="eqref" data-reference="eq:JK_expect">(eq. JK_expect)</a> expand $`\lVert u-\eta_i\rVert^2=\lVert u\rVert^2-2\langle u,\eta_i\rangle+\lVert \eta_i\rVert^2`$ with $`u=\varphi_f(t_i-\tau;\,p^\star,\,y_\tau)-x_i`$, which depends on $`\eta_\tau`$ only; for $`t_i\ne\tau`$ the noises are independent and $`\mathbb E\eta_i=0`$, so the cross term has zero mean, $`\mathbb E\lVert \eta_i\rVert^2=d\sigma^2`$, and $`\mathbb E\lVert u\rVert^2\le e^{2\mu j\Delta t}\mathbb E\lVert \eta_\tau\rVert^2`$ by Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a>. ◻

</div>

*What changed.* Three things. (i) $`L\to\mu`$. (ii) The draft applied the worst-case factor $`e^{L\Delta T}`$ to every datum in the window; <a href="#eq:JK_true" data-reference-type="eqref" data-reference="eq:JK_true">(eq. JK_true)</a> keeps the actual offset $`j\Delta t`$, and the geometric sum $`\sum_{j\le n_k}e^{2\mu j\Delta t}\approx e^{2\mu\Delta T}/(2\mu\Delta t)`$ saves a factor of order $`\mu\Delta T`$ relative to $`n_ke^{2\mu\Delta T}`$. (iii) The draft replaced every $`\lVert \eta_i\rVert`$ by $`\lVert \eta\rVert_{\max}`$; under the Gaussian model of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>, $`\mathbb E\lVert \eta_i\rVert^2=d\sigma^2`$ while $`\lVert \eta\rVert_{\max}^2\approx\sigma^2(d+2\log N)`$, a factor $`1+2\log N/d`$ ($`4.9`$ measured on the FHN data), and the cross term between node noise and datum noise vanishes in expectation, saving a further factor up to 2. <a href="#eq:JK_expect" data-reference-type="eqref" data-reference="eq:JK_expect">(eq. JK_expect)</a> is the natural input to the next step.

## Bound on the error $`\lVert p^\star-p^{(K)}\rVert`$

<div id="prop:perr" class="proposition">

**Proposition 2** (displacement of the minimiser from the truth). *Suppose $`J_K`$ is strongly convex with constant $`m>0`$ on a convex neighbourhood of $`p^{(K)}`$ containing $`p^\star`$, i.e. $`J_K(q)\ge J_K(p^{(K)})+\tfrac m2\lVert q-p^{(K)}\rVert^2`$ there. Then
``` math
\begin{equation}
\lVert p^\star-p^{(K)}\rVert\ \le\ \sqrt{\frac2m\Big(J_K(p^\star)-J_K(p^{(K)})\Big)}\ \le\ \sqrt{\frac2m\Big(N\lVert \eta\rVert_{\max}^2\big(1+e^{\mu\Delta T}\big)^2-J_K(p^{(K)})\Big)},
\label{eq:perr}
\end{equation}
```
and, replacing $`J_K(p^\star)`$ by its expectation, $`\mathbb E\lVert p^\star-p^{(K)}\rVert^2\le\tfrac2m\big(Nd\sigma^2(1+e^{2\mu\Delta T})-\mathbb E J_K(p^{(K)})\big)`$. For single shooting, $`\lVert p^\star-p^{(1)}\rVert\le\sqrt{\tfrac2m\big(\sum_{i=1}^N(e^{\mu(t_i-t_0)}\lVert \eta_0\rVert+\lVert \eta_i\rVert)^2-J_1(p^{(1)})\big)}`$.*

</div>

<div class="proof">

*Proof.* The gradient vanishes at the minimiser, so the strong-convexity inequality at $`q=p^\star`$ gives the first bound; Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a> gives the second. (The draft’s first version of this bound, via a second-order Taylor expansion with Hessian eigenvalue $`\lambda_{\min}`$, is the same statement with $`m=\lambda_{\min}`$ and the factor $`2`$ absorbed.) ◻

</div>

The bound is sharp in kind, not in size: the first inequality in <a href="#eq:perr" data-reference-type="eqref" data-reference="eq:perr">(eq. perr)</a> is an identity for a quadratic cost, so the whole slack is in the estimate of $`J_K(p^\star)`$, i.e. in the exponential. **Read this:** the strong-convexity premise must hold at the *minimiser*; the numerical section shows that at $`p^\star`$ the noisy cost is indefinite for every $`\kappa\ge2`$ (Figure <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a>) and that the premise is not verified at the iterates Nelder–Mead returns (Figure <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>).

# Propagating the best guess across shooting-node partitions

We now bound how the optimum moves when shooting nodes are removed. Let the fine partition have nodes $`\{\tau_k\}_{k=0}^K`$ and remove the nodes indexed by $`\mathcal I_R`$; the coarse cost is
``` math
\begin{equation}
\hat{\mathcal J}_K(p)=\sum_{k\notin\mathcal I_R}\ \sum_{t_i\in(\tau_k,\tau_k^{+}]}\lVert \varphi_f(t_i-\tau_k;\,p,\,y_{\tau_k})-y_i\rVert^2,
\label{eq:Jhat}
\end{equation}
```
i.e. every datum in a fine window $`D_k`$ launched at a removed node $`\tau_k`$ is now predicted from the nearest remaining node to its left, $`\tau_k^-`$, through the removed node (Figure <a href="#fig:removal" data-reference-type="ref" data-reference="fig:removal">6</a>). We assume the removed nodes’ neighbours are a subset of the fine nodes (the coarse partition is a sub-partition), which is what the algorithm does.

**Figure 6** (`figures/R002_fig05_node_removal.pdf`)

!6(figures/R002_fig05_node_removal.pdf)

***Node removal.** Zoom on $`t\in[19,41]`$ with the wrong parameter of Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a>: the fine partition ($`\kappa=5`$, blue segments) and the coarse one after removing the node at $`t=25`$ (open marker; $`\kappa=10`$, vermilion), with $`\tau_k^-=20`$ and $`\tau_k^+=30`$; the measured gap between the fine and coarse segments at $`t=37`$ is printed. After removal the segment launched at $`\tau_k^-`$ must cover $`[\tau_k^-,\tau_k^+]`$; the cost changes only on the windows adjacent to removed nodes. Replaces `comparison_shooting_nodes.png`. Inherited from R002 Figure 5; the bound printed in the R002 caption is the draft’s version, superseded by Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>.*


<div id="prop:removal" class="proposition">

**Proposition 3** (cost change under node removal; *revised*). *With $`\rho_s`$ as in <a href="#eq:rho" data-reference-type="eqref" data-reference="eq:rho">(eq. rho)</a>, for every $`p`$,
``` math
\begin{equation}
\big|\hat{\mathcal J}_K(p)-J_K(p)\big|\ \le\ 2\,n_{\max}\,|\mathcal I_R|\;e^{\mu\Delta T_1}\;\rho_{\Delta T_2}(p)\;\rho_{\Delta T_1+\Delta T_2}(p)\ =:\ \Delta_K(p).
\label{eq:prop_removal}
\end{equation}
```
At $`p=p^\star`$ this is $`2n_{\max}|\mathcal I_R|\,e^{\mu\Delta T_1}(1+e^{\mu\Delta T_2})(1+e^{\mu(\Delta T_1+\Delta T_2)})\lVert \eta\rVert_{\max}^2`$, quadratic in the noise.*

</div>

<div class="proof">

*Proof.* Only the data in $`D_k`$, $`k\in\mathcal I_R`$, change predictor. For $`t_i\in D_k`$ let $`u_i=\varphi_f(t_i-\tau_k^-;\,p,\,y_{\tau_k^-})`$ (coarse prediction), $`v_i=\varphi_f(t_i-\tau_k;\,p,\,y_{\tau_k})`$ (fine prediction), $`a_i=\lVert u_i-y_i\rVert`$ and $`b_i=\lVert v_i-y_i\rVert`$. Then
``` math
\hat{\mathcal J}_K(p)-J_K(p)=\sum_{k\in\mathcal I_R}\sum_{t_i\in D_k}\big(a_i^2-b_i^2\big),\qquad |a_i^2-b_i^2|=|a_i-b_i|\,(a_i+b_i)\le\lVert u_i-v_i\rVert\,(a_i+b_i)
```
by the reverse triangle inequality. By the flow property $`u_i=\varphi_f(t_i-\tau_k;\,p,\,\varphi_f(\tau_k-\tau_k^-;\,p,\,y_{\tau_k^-}))`$, so Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a> gives $`\lVert u_i-v_i\rVert\le e^{\mu(t_i-\tau_k)}\,g_k\le e^{\mu\Delta T_1}g_k`$ with $`g_k=\lVert \varphi_f(\tau_k-\tau_k^-;\,p,\,y_{\tau_k^-})-y_{\tau_k}\rVert`$ the coarse residual at the removed node, and Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">3</a> gives $`g_k\le\rho_{\Delta T_2}(p)`$. The residuals $`a_i`$ and $`b_i`$ are residuals of windows of length at most $`\Delta T_1+\Delta T_2`$ and $`\Delta T_1`$, so $`a_i+b_i\le2\rho_{\Delta T_1+\Delta T_2}(p)`$ ($`\rho_s`$ is increasing in $`s`$). Summing over the at most $`n_{\max}`$ data of each of the $`|\mathcal I_R|`$ windows gives <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a>. ◻

</div>

*What changed.* (i) $`L\to\mu`$. (ii) The draft bounded $`a_i+b_i`$ through $`\lVert u_i+v_i-2y_i\rVert\le\dots+2\max\lVert y_i\rVert`$, which introduces the size of the data ($`C^{(2)}_k=\max\lVert y_{i+1}\rVert`$, equal to $`2.34`$ on FHN) into the constant $`C_{\max}`$; the bound was therefore linear in the small quantities times the attractor radius. Here $`a_i+b_i`$ is bounded by two residuals, so the bound is quadratic in $`(\lVert \eta\rVert,\lVert p-p^\star\rVert)`$, as the left-hand side is; on FHN this removes a factor $`\lVert y\rVert_{\max}/\lVert \eta\rVert_{\max}\approx15`$. (iii) The draft’s sum over $`t_i\in[\tau_k,\tau_k^+)`$ was collapsed to the factor $`|\mathcal I_R|`$ without the number of data per window; $`n_{\max}`$ restores it. (iv) The draft’s intermediate inequality carried $`e^{2L(\Delta T_1+\Delta T_2)}`$ while its statement had $`e^{L(\Delta T_1+\Delta T_2)}`$; the exponent here is $`e^{\mu\Delta T_1}`$ times the exponents inside the two $`\rho`$ factors, consistently. The design rule of the draft survives unchanged: remove few nodes at a time, prefer low-noise nodes, keep $`\Delta T_2`$ small (Figure <a href="#fig:prop1" data-reference-type="ref" data-reference="fig:prop1">8</a>).

<div id="thm:main" class="theorem">

**Theorem 1** (displacement of the minimiser under node removal; *revised*). *Let $`p^{(K)}=\arg\min J_K`$ and $`\hat p^{(K)}=\arg\min\hat{\mathcal J}_K`$, and suppose $`J_K`$ is strongly convex with constant $`m>0`$ on a convex neighbourhood of $`p^{(K)}`$ that contains $`\hat p^{(K)}`$. Then
``` math
\begin{equation}
\lVert \hat p^{(K)}-p^{(K)}\rVert\ \le\ \sqrt{\frac2m\Big(\Delta_K\big(p^{(K)}\big)+\Delta_K\big(\hat p^{(K)}\big)\Big)},
\label{eq:thm}
\end{equation}
```
with $`\Delta_K`$ from <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a>; each $`\Delta_K(q)`$ is a quadratic polynomial in $`\lVert \eta\rVert_{\max}`$ and $`\lVert q-p^\star\rVert`$ with coefficients $`e^{\mu\Delta T_1}(1+e^{\mu\Delta T_2})(1+e^{\mu(\Delta T_1+\Delta T_2)})`$ and smaller.*

</div>

<div class="proof">

*Proof.* Strong convexity at the minimiser ($`\nabla J_K(p^{(K)})=0`$) gives $`\tfrac m2\lVert \hat p^{(K)}-p^{(K)}\rVert^2\le J_K(\hat p^{(K)})-J_K(p^{(K)})`$. Insert $`\pm\hat{\mathcal J}_K(\hat p^{(K)})`$ and $`\pm\hat{\mathcal J}_K(p^{(K)})`$:
``` math
J_K(\hat p^{(K)})-J_K(p^{(K)})=\big[J_K(\hat p^{(K)})-\hat{\mathcal J}_K(\hat p^{(K)})\big]+\big[\hat{\mathcal J}_K(\hat p^{(K)})-\hat{\mathcal J}_K(p^{(K)})\big]+\big[\hat{\mathcal J}_K(p^{(K)})-J_K(p^{(K)})\big].
```
The middle bracket is $`\le0`$ because $`\hat p^{(K)}`$ minimises $`\hat{\mathcal J}_K`$; the outer brackets are bounded by Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>. ◻

</div>

*What changed.* The structure of the proof is the draft’s; the bound inherits the three improvements of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>. In particular the displacement now scales like the noise (through $`\sqrt{\Delta_K}\sim\lVert \eta\rVert_{\max}`$), not like $`\sqrt{\text{noise}\times\text{attractor size}}`$. The draft also mixed $`L(p)`$, $`L(p^\star)`$ and $`L_K=\max\{L(p^{(K)}),L(\hat p^{(K)})\}`$ in one formula; with $`\mu`$ a single constant on the region of interest the distinction disappears.

# Refinement: trajectory-dependent exponents and a weighted norm

The bounds above are a priori: they hold for every right-hand side with the same constants and are attained by the linear system $`\dot x=\mu x`$, so they are necessarily exponential. Along an attracting orbit the actual growth is far smaller, and the same proof yields a sharper, trajectory-dependent statement.

<div id="cor:window" class="corollary">

**Corollary 1** (per-window exponents). *The identity <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> holds pointwise in time, so with the local logarithmic norm $`\mu(t)`$ of the mean-value matrix $`A(t)`$,
``` math
\begin{equation}
\lVert \varphi_f(t;\,p,\,x_1)-\varphi_f(t;\,p,\,x_2)\rVert\le\exp\!\Big(\int_0^t\mu(s)\,\mathrm{d}s\Big)\lVert x_1-x_2\rVert,
\label{eq:window}
\end{equation}
```
and Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a>–<a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">3</a>, Propositions <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>, <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> and Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> hold with $`e^{\mu\Delta T}`$ replaced by $`\max_ke^{\Lambda_k}`$, $`\Lambda_k=\int_{\tau_{k-1}}^{\tau_k}\mu(s)\,\mathrm{d}s`$. The same proof runs in any weighted norm $`\lVert x\rVert_D=\lVert Dx\rVert`$ with $`\mu_D`$ in place of $`\mu`$, and a bound in $`\lVert \cdot\rVert_D`$ implies the Euclidean bound with an extra factor $`\kappa(D)`$.*

</div>

For small perturbations $`\mu(s)`$ is the logarithmic norm of $`J_f`$ along the reference orbit (first order in $`\lVert \delta\rVert`$; for a rigorous statement take the supremum over the segment joining the two trajectories). On FHN the choice $`D=\mathrm{diag}(1,s)`$ with $`s=\sqrt{|J_{12}|/|J_{21}|}=3.54`$ makes the off-diagonal entries of $`DJ_fD^{-1}`$ equal and opposite, so its symmetric part is diagonal and $`\mu_D(x)=\max(1-v^2,-0.064)`$: *negative* whenever $`|v|>1`$, i.e. on both slow branches of the relaxation oscillation, with global supremum $`1`$. Table <a href="#tab:windows" data-reference-type="ref" data-reference="tab:windows">2</a> evaluates the window exponents along the true orbit: in the weighted norm the worst window has amplification at most $`8.04`$ for every window length tested (times $`\kappa(D)=3.54`$ for a Euclidean statement) against the measured peak of $`8.5`$, and the median window contracts. Two consequences. First, this is the quantitative version of “the restart at each node resets the error”. Second, it gives a node-placement rule sharper than the one after Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>: keep nodes just before the fast jumps, where $`\int\mu`$ accumulates, and remove them freely on the slow branches, where every window is contracting. The price is that $`\Lambda_k`$ is evaluated along the fitted orbit, so the bound is a posteriori. What no Grönwall-type argument can deliver is the observed *plateau* of the amplification (Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a>a): that is a property of the attractor and would require a contraction metric (impossible globally on a limit cycle, whose phase direction is neutral), a Lyapunov function, or a Floquet analysis of the variational equation.

# Numerical results

All results in this section were computed in report R002 (frozen code snapshot, 522 optimisation runs, 18 gates) and R003 (bound evaluation, 8 gates); the figures and tables are copied here byte for byte and every number quoted in the text is regenerated from the copied result files (§<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>). The protocol is that of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>.

## The bounds against measurement

**Figure 7** (`figures/R003_fig01_bounds.pdf`)

!7(figures/R003_fig01_bounds.pdf)

***Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a> and <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">2</a> on FHN.** (a) Amplification $`\lVert \varphi(t;p^\star,x_1)-\varphi(t;p^\star,x_2)\rVert/\lVert x_1-x_2\rVert`$, largest value over 24 probes ($`x_1`$ a datum at four phases of the orbit, $`x_2=x_1+10^{-3}u`$ for six random unit directions; blue), against the draft’s bound $`e^{Lt}`$ (vermilion) and the revised $`e^{\mu t}`$ (green dashed). (b) The same for the parameter sensitivity $`\lVert \varphi(t;p_1,x_0)-\varphi(t;p_2,x_0)\rVert/\lVert p_1-p_2\rVert`$ ($`p_2=p^\star+10^{-3}u`$, $`u\in\mathbb R^{20}`$) against $`\frac{\tilde L}{L}(e^{Lt}-1)`$ and $`\frac{\tilde L}{\mu}(e^{\mu t}-1)`$. Both bounds hold at every probe; the revised one is $`10^8`$ closer at $`t=10`$ but still grows, while the measurement peaks at $`8.5`$ near the fast jump and then contracts because the orbit is attracting. (c) Local rates along one FHN period ($`39.5`$ time units): the local Lipschitz constant $`\lVert \partial f/\partial x\rVert_2`$ (dotted) never drops below 1; the Euclidean logarithmic norm $`\mu(x^\star(t))`$ (green) is near zero on the slow branches and reaches $`1.17`$ only during the two fast jumps; in the weighted norm the local $`\mu_D`$ (purple) is negative for $`75\%`$ of the period. Inherited from R003 Figure 1 (‘figures/R003_fig01_bounds.pdf‘).*


**Table 1.** Draft versus revised bounds on FHN, evaluated at $`t=\Delta T`$, against the largest value observed over the 24 probes of Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a> for $`t\le\Delta T`$. Inherited from R003 Table 1.


| $t=\Delta T$ | Lemma 1 original $e^{L\Delta T}$ | Lemma 1 proposed $e^{\mu\Delta T}$ | Lemma 1 observed peak | Lemma 2 original $\frac{\tilde L}{L}(e^{L\Delta T}-1)$ | Lemma 2 proposed $\frac{\tilde L}{\mu}(e^{\mu\Delta T}-1)$ | Lemma 2 observed peak |
|---|---|---|---|---|---|---|
| 1 | 21.1 | 3.23 | 2.93 | 69.8 | 20.1 | 3.7 |
| 2 | 447 | 10.4 | 6.92 | $1.5\times10^{3}$ | 84.9 | 6.7 |
| 5 | $4.2\times10^{6}$ | 349 | 8.48 | $1.5\times10^{7}$ | $3.1\times10^{3}$ | 12.0 |
| 10 | $1.8\times10^{13}$ | $1.2\times10^{5}$ | 8.48 | $6.2\times10^{13}$ | $1.1\times10^{6}$ | 15.1 |

**Table 2.** Per-window amplification $`e^{\Lambda_k}`$ along the true FHN orbit ($`\Delta t=1`$, windows aligned to $`t_0`$), Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">1</a>: worst and median window, in the Euclidean norm and in the weighted norm $`\lVert Dx\rVert`$; “contracting” is the share of windows with $`\Lambda^D_k<0`$. Weighted values convert to Euclidean statements by a factor at most $`\kappa(D)=3.54`$. Inherited from R003 Table 2.


| $\Delta T$ | windows | a priori $e^{\mu\Delta T}$ | Euclidean $e^{\Lambda_k}$ worst | Euclidean median | weighted $e^{\Lambda^D_k}$ worst | weighted median | contracting windows |
|---|---|---|---|---|---|---|---|
| 1 | 100 | 3.23 | 3.10 | 1.16 | 2.60 | 0.94 | 74 % |
| 2 | 50 | 10.4 | 8.56 | 1.41 | 5.89 | 0.88 | 70 % |
| 5 | 20 | 349 | 34.78 | 3.75 | 8.04 | 0.73 | 55 % |
| 10 | 10 | $1.2\times10^{5}$ | 138.79 | 29.25 | 5.89 | 3.20 | 30 % |

**Figure 8** (`figures/R002_fig10_prop1_node_removal.pdf`)

!8(figures/R002_fig10_prop1_node_removal.pdf)

***Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>: node removal changes the cost by a bounded amount.** Starting from the finest partition ($`K=100`$, $`\gamma=0`$): (a) keep every $`m`$-th node so that $`|\mathcal I_R|=100-\lceil100/m\rceil`$ nodes are removed ($`\Delta T_2=\Delta t`$ fixed, $`\Delta T_1=m\Delta t`$ grows); (b) remove one contiguous block of $`b`$ nodes after node 30 ($`|\mathcal I_R|=b`$, $`\Delta T_2=b\Delta t`$ grows). Shown for $`p=p^\star`$ (green), a vector $`0.06`$ from $`p^\star`$ on six coefficients (blue) and $`p_{\rm wrong}`$ of Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a> (vermilion). $`|\hat{\mathcal J}-J|`$ grows with $`|\mathcal I_R|`$ and with $`\Delta T_2`$, and much faster away from $`p^\star`$, as the proposition says; the rate $`e^{L\Delta T_2}`$ of the draft (grey dashed in (b)) is far steeper than the observed growth, which saturates once the removed block spans a full fast excursion. Inherited from R002 Figure 10.*


## The cost landscape as the window grows

**Figure 9** (`figures/R002_fig11_landscape_2d_v_v3.pdf`)

!9(figures/R002_fig11_landscape_2d_v_v3.pdf)

***Two-dimensional slices of $`J_\kappa`$ in the $`(v,v^3)`$ coefficient plane of the $`\dot v`$ equation.** All other coefficients at their true values, $`x_0=y_0`$, $`\gamma=0.05`$; star = truth $`(1,-1/3)`$; grey = blow-up plateau; colour is $`\log J`$. As $`\kappa`$ grows from 1 to 100 the basin around the truth narrows by an order of magnitude and the plateau expands: any positive cubic coefficient makes $`\dot v\sim cv^3`$ blow up in finite time, and at $`\kappa=100`$ even $`v`$-coefficients above $`1.6`$ do. This is the geometric reason the control arm fails at large $`\kappa`$. Inherited from R002 Figure 11.*


**Figure 10** (`figures/R002_fig13_landscape_1d_slices.pdf`)

!10(figures/R002_fig13_landscape_1d_slices.pdf)

***One-dimensional slices along random directions and the number of local minima.** Top: $`J_\kappa(p^\star+s\,u_d)`$ for three random unit directions $`u_d\in\mathbb R^{20}`$ (light to dark $`=\kappa=1,2,5,10,25,50,100`$; gaps = plateau); top right: local minima per slice, mean over 12 directions (band = min–max), rising from $`1.25`$ at $`\kappa=1`$ to $`7.9`$ at $`\kappa=100`$. Bottom: the full $`s\in[-2,2]`$ range for direction 1. Larger windows sharpen the minimum but add spurious minima and cut the finite region down to $`|s|\lesssim0.5`$. Inherited from R002 Figure 13.*


**Figure 11** (`figures/R002_fig14_hessian_conditioning.pdf`)

!11(figures/R002_fig14_hessian_conditioning.pdf)

***Curvature of the data term at $`p^\star`$ versus window size.** (a) Largest and smallest Hessian eigenvalues at $`p^\star`$ (20 parameters) and the gradient norm: $`\lambda_{\max}`$ grows from $`36`$ ($`\kappa=1`$) to $`\ensuremath{1.2\times 10^{6}}`$ ($`\kappa=100`$) while $`\lVert \nabla J_\kappa(p^\star)\rVert`$ grows from $`0.04`$ to $`96`$; with noisy data the truth is not a stationary point and drifts away from the minimiser as $`\kappa`$ grows. (b) Number of negative eigenvalues: the noisy cost is indefinite at $`p^\star`$ for every $`\kappa\ge2`$ (up to $`6`$ of 20 directions), so the strong-convexity premise of Propositions <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> and Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> can only hold near the *minimiser*, not near $`p^\star`$. (c) Sensitivity to the free initial-condition variable $`v_0`$: a shallow parabola for every $`\kappa`$. Inherited from R002 Figure 14.*


## Guess propagation versus no propagation: the headline experiment

Protocol: $`8`$ optimiser seeds $`\times`$ 3 arms $`\times`$ the 16-stage FULL schedule, Nelder–Mead with 2500 iterations per stage, $`\gamma=0.05`$, $`S=10`$, flat penalty. Table <a href="#tab:T1" data-reference-type="ref" data-reference="tab:T1">3</a> gives the seed medians and the paired statistics per stage; Figures <a href="#fig:headline" data-reference-type="ref" data-reference="fig:headline">12</a>–<a href="#fig:coef" data-reference-type="ref" data-reference="fig:coef">15</a> show them.

**Table 3.** Headline sweep, seed medians per stage: cost $`J`$, parameter error $`\lVert p-p^\star\rVert`$ and recovery score for GP, best-GP and the control; the mean paired difference GP $`-`$ control in $`\lVert p-p^\star\rVert`$ with its 95 % bootstrap CI over the 8 seeds; the number of seeds in which GP is strictly better; the number of GP seeds on the plateau. Inherited from R002 Table T1 (`tables/T1_headline.tex`).


| κ | J GP | J best-GP | J control | ‖p−p*‖ GP | ‖p−p*‖ best-GP | ‖p−p*‖ control | score GP | score control | GP−control | CI lo | CI hi | #GP better | #GP on plateau |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 0.02569 | 0.02569 | 0.02569 | 0.8641 | 0.8641 | 0.8641 | 0.4 | 0.4 | 0 | 0 | 0 | 0 | 3 |
| 2 | 0.02081 | 0.02081 | 0.03726 | 0.3803 | 0.3803 | 1.314 | 0.75 | 0.275 | -0.7724 | -1.112 | -0.4196 | 7 | 1 |
| 3 | 0.01864 | 0.019 | 0.05955 | 0.3748 | 0.3893 | 1.466 | 0.75 | 0.225 | -0.8679 | -1.309 | -0.4865 | 7 | 1 |
| 4 | 0.01549 | 0.01577 | 0.09718 | 0.3325 | 0.3615 | 1.51 | 0.775 | 0.25 | -0.9232 | -1.248 | -0.5652 | 7 | 1 |
| 5 | 0.01906 | 0.01889 | 0.1259 | 0.2218 | 0.221 | 1.543 | 0.775 | 0.2 | -1.133 | -1.654 | -0.6627 | 7 | 1 |
| 6 | 0.02114 | 0.02054 | 0.2428 | 0.3441 | 0.4522 | 1.716 | 0.55 | 0.2 | -1.159 | -1.526 | -0.7401 | 7 | 1 |
| 8 | 0.0161 | 0.0154 | 0.3185 | 0.3593 | 0.3964 | 1.59 | 0.675 | 0.275 | -1.113 | -1.36 | -0.7559 | 7 | 1 |
| 10 | 0.01406 | 0.01438 | 0.3086 | 0.2995 | 0.3347 | 1.737 | 0.8 | 0.2 | -1.234 | -1.634 | -0.7787 | 7 | 1 |
| 12 | 0.01556 | 0.0155 | 0.6561 | 0.278 | 0.3312 | 1.622 | 0.8 | 0.25 | -1.163 | -1.415 | -0.7988 | 7 | 1 |
| 15 | 0.01414 | 0.01409 | 0.5398 | 0.3348 | 0.3402 | 1.574 | 0.725 | 0.275 | -1.109 | -1.327 | -0.7739 | 7 | 1 |
| 20 | 0.01436 | 0.01459 | 0.5358 | 0.2425 | 0.2709 | 1.612 | 0.8 | 0.25 | -1.236 | -1.547 | -0.8214 | 7 | 1 |
| 25 | 0.01345 | 0.01362 | 0.861 | 0.3113 | 0.3455 | 1.534 | 0.85 | 0.25 | -1.111 | -1.401 | -0.7455 | 7 | 1 |
| 33 | 0.01375 | 0.01373 | 0.8233 | 0.3372 | 0.3758 | 1.498 | 0.825 | 0.225 | -0.9927 | -1.358 | -0.5627 | 7 | 1 |
| 50 | 0.01327 | 0.01334 | 1.334 | 0.3353 | 0.3393 | 1.807 | 0.8 | 0.2 | -1.63 | -2.426 | -0.9189 | 7 | 1 |
| 75 | 0.0128 | 0.01289 | 1.392 | 0.3223 | 0.3179 | 1.793 | 0.8 | 0.225 | -1.167 | -1.522 | -0.7642 | 7 | 1 |
| 100 | 0.01273 | 0.01288 | 1.506 | 0.3216 | 0.3105 | 1.649 | 0.8 | 0.25 | -1.138 | -1.426 | -0.7719 | 7 | 1 |

**Figure 12** (`figures/R002_fig15_sweep_headline.pdf`)

!12(figures/R002_fig15_sweep_headline.pdf)

***Guess propagation versus no propagation, $`8`$ seeds.** (a) Cost, (b) parameter error and (c) recovery score at the minimiser of each stage; thin lines = seeds, thick = median; blue circles GP, green triangles best-GP, vermilion squares control. All arms coincide at $`\kappa=1`$ (same start). From $`\kappa=2`$ on, GP drops to $`\lVert p-p^\star\rVert\approx0.22`$–$`0.38`$ and ends single shooting at $`0.32`$ (score $`0.80`$), whereas the control climbs back to the seed distance ($`1.65`$ at $`\kappa=100`$; $`25\%`$ of its seeds end on the plateau, the others in spurious minima). One GP seed starts on the plateau at $`\kappa=1`$ and never moves (flat line at $`1.84`$). The parameter error is not monotone: it is smallest at $`\kappa=5`$ ($`0.22`$) and drifts up to $`0.32`$ while the cost keeps falling, the noise-induced bias of Figure <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a>. Inherited from R002 Figure 15.*


**Figure 13** (`figures/R002_fig16_sweep_paired.pdf`)

!13(figures/R002_fig16_sweep_paired.pdf)

***Paired comparison.** (a) Mean paired difference in $`\lVert p-p^\star\rVert`$ (GP minus control, vermilion; GP minus best-GP, green) with 95 % bootstrap CIs over the 8 seeds: the GP–control difference is $`-1.14`$ $`[-1.43,-0.77]`$ at $`\kappa=100`$ and its CI excludes zero at every $`\kappa\ge2`$; GP and best-GP are statistically indistinguishable. (b) Cellwise counts: GP is strictly better than the control in 7 of 8 seeds at every $`\kappa\ge2`$ and tied in the eighth (the plateau seed). Inherited from R002 Figure 16.*


**Figure 14** (`figures/R002_fig18_sweep_filmstrip.pdf`)

!14(figures/R002_fig18_sweep_filmstrip.pdf)

***Filmstrip of the sweep (seed 2).** Single-shooting simulation of $`v(t)`$ from the minimiser returned at $`\kappa=1,5,25,100`$ (columns) for GP (top, blue) and the control (bottom, vermilion); grey dashed = truth, dots = data; the parameter error is printed in each panel. The GP fit tracks the data at every stage; the control loses the trajectory as soon as the windows are long. Inherited from R002 Figure 18.*


**Figure 15** (`figures/R002_fig20_final_coefficients.pdf`)

!15(figures/R002_fig20_final_coefficients.pdf)

***Coefficients at the end of the sweep ($`\kappa=100`$).** Truth (grey), GP seed-median (blue) and control seed-median (vermilion) with the 8 seeds as dots, in library order. GP recovers the support (7 non-zeros, no false positive in the median) with a shrinkage of the $`w`$ coefficient of $`\dot v`$ due to the smooth-$`\ell_1`$ term at $`\gamma=0.05`$; the control’s median is near zero everywhere because its seeds scatter. Inherited from R002 Figure 20.*


## Variations

Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a> summarises the variation experiments of R002 (SHORT schedule unless stated; the schedule rows change the number of stages by design, and the optimiser rows do different work per iteration, so those comparisons are not at equal budget). Three readings matter for the method. Removing few nodes per step matters: DENSE ends at $`0.30`$, COARSE at $`0.52`$ and JUMP at $`0.76`$ (Figure <a href="#fig:schedule" data-reference-type="ref" data-reference="fig:schedule">16</a>), which is the design rule of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> in practice. The graded blow-up penalty removes the “stuck on the plateau” failure (GP $`0.27`$, no seed on the plateau). With the exact gradient, BFGS with guess propagation is the best and cheapest arm ($`0.21`$, score $`0.95`$; Figure <a href="#fig:opt" data-reference-type="ref" data-reference="fig:opt">17</a>), while BFGS without propagation fails because the gradient vanishes on the plateau. GP is robust up to 10 % noise ($`0.31`$) and fails at 20 % ($`1.88`$); the control fails even at zero noise.

**Table 4.** Selected variation experiments (seed medians at $`\kappa=100`$). Derived from R002 Table T2 (`external_data/tables/T2_variations.csv`); the full table has 55 rows.


| variation (arm) | seeds | $\|p-p^\star\|$ at $\kappa=100$ | score | seeds on plateau |
|---|---|---|---|---|
| schedule DENSE (44 stages) | 8 | 0.30 | 0.80 | 12 % |
| schedule COARSE (4 stages) | 8 | 0.52 | 0.55 | 12 % |
| schedule JUMP (2 stages) | 8 | 0.76 | 0.42 | 12 % |
| graded blow-up penalty, GP | 8 | 0.27 | 0.82 | 0 % |
| graded blow-up penalty, control | 8 | 1.63 | 0.30 | 0 % |
| noise 0 %, GP | 6 | 0.23 | 0.80 | 17 % |
| noise 10 %, GP | 6 | 0.31 | 0.62 | 17 % |
| noise 20 %, GP | 6 | 1.88 | 0.23 | 17 % |
| noise 0 %, control | 6 | 1.63 | 0.25 | 33 % |
| $\gamma=0$, GP | 6 | 0.81 | 0.38 | 17 % |
| $\gamma=0.05$, GP | 6 | 0.22 | 0.80 | 17 % |
| $\gamma=1$, GP | 6 | 1.01 | 0.60 | 17 % |
| BFGS, GP | 8 | 0.21 | 0.95 | 12 % |
| BFGS, control | 8 | 1.65 | 0.23 | 62 % |
| Nelder–Mead 1000 iterations, GP | 4 | 0.61 | 0.57 | 25 % |
| Nelder–Mead 5000 iterations, GP | 4 | 0.20 | 0.90 | 25 % |
| integrator sub-steps $S=1$, GP | 4 | 0.50 | 0.75 | 0 % |
| integrator sub-steps $S=5$, GP | 4 | 0.29 | 0.82 | 25 % |

**Figure 16** (`figures/R002_fig21_schedule.pdf`)

!16(figures/R002_fig21_schedule.pdf)

***Window-size schedule.** Parameter error along the sweep for the DENSE (44 stages), FULL (16), COARSE (4) and JUMP (2) schedules, 8 seeds; removing few nodes per step matters. Inherited from R002 Figure 21.*


**Figure 17** (`figures/R002_fig25_optimizers.pdf`)

!17(figures/R002_fig25_optimizers.pdf)

***Optimisers.** Nelder–Mead versus BFGS and L-BFGS (automatic-differentiation gradient through the windowed loss), with and without guess propagation; wall time per stage is machine-dependent and only comparative. BFGS with propagation is the best and cheapest arm; without propagation the gradient methods die on the plateau. Inherited from R002 Figure 25.*


## Other systems

The implementation is dimension- and degree-agnostic; Table <a href="#tab:T5" data-reference-type="ref" data-reference="tab:T5">5</a> and Figures <a href="#fig:lv" data-reference-type="ref" data-reference="fig:lv">18</a>–<a href="#fig:lorenz" data-reference-type="ref" data-reference="fig:lorenz">19</a> apply the same sweep to Lotka–Volterra ($`\dot x=x-0.5xy`$, $`\dot y=-0.8y+0.3xy`$; quadratic library, 12 coefficients, $`N=101`$, $`\Delta t=0.25`$, 5 % noise, Nelder–Mead, 6 seeds) and Lorenz ($`\sigma=10`$, $`\rho=28`$, $`\beta=8/3`$; quadratic library, 30 coefficients, $`N=101`$, $`\Delta t=0.02`$, 2 % noise, 4 seeds; BFGS and Nelder–Mead). GP recovers Lotka–Volterra ($`0.29`$, score $`0.83`$; the control ends at $`1.41`$ with $`83\%`$ of its seeds on the plateau) and, with BFGS, Lorenz ($`3.9`$ on coefficients of size up to 28, score $`0.83`$); GP with Nelder–Mead does not converge in 33 dimensions ($`18.1`$, score $`0.23`$), so a derivative-free sweep does not scale.

**Table 5.** Other systems, seed medians at $`\kappa=100`$. Inherited from R002 Table T5.


| system | optimizer | arm | n_seeds | ‖p−p*‖ (κ=100) | score | plateau frac |
|---|---|---|---|---|---|---|
| lorenz | bfgs | propagate | 4 | 3.852 | 0.8333 | 0 |
| lorenz | bfgs | reset | 4 | 31.95 | 0.03333 | 1 |
| lorenz | nm | propagate | 4 | 18.11 | 0.2333 | 0 |
| lorenz | nm | reset | 4 | 31.95 | 0.03333 | 1 |
| lv | nm | propagate | 6 | 0.29 | 0.8333 | 0 |
| lv | nm | reset | 6 | 1.408 | 0.2917 | 0.8333 |

**Figure 18** (`figures/R002_fig35_lotka_volterra.pdf`)

!18(figures/R002_fig35_lotka_volterra.pdf)

***Lotka–Volterra.** (a) Parameter error along the sweep (thin: 6 seeds, thick: median), (b) score, (c) the $`x(t)`$ fit at $`\kappa=100`$ for seed 1. Inherited from R002 Figure 35.*


**Figure 19** (`figures/R002_fig36_lorenz.pdf`)

!19(figures/R002_fig36_lorenz.pdf)

***Lorenz (chaotic, 30 coefficients).** (a) Parameter error along the sweep for BFGS (solid) and Nelder–Mead (dashed), GP (blue) and control (vermilion), 4 seeds; (b) score; (c) the $`x(t)`$ fit at $`\kappa=100`$, seed 1; (d) seed-median GP coefficients versus truth. Inherited from R002 Figure 36.*


## The premises of the theory, checked where they are assumed

**Figure 20** (`figures/R002_fig37_post_hessian_path.pdf`)

!20(figures/R002_fig37_post_hessian_path.pdf)

***Premises checked at the minimisers.** (a) Smallest eigenvalue of the data-term Hessian at the GP minimiser returned by Nelder–Mead at every stage, 7 seeds (plateau seed excluded; blue = positive definite, vermilion = at least one negative eigenvalue): the returned point is positive definite in only $`36\%`$ of the (seed, $`\kappa`$) cells, with on average less than one of 20 eigenvalues slightly negative elsewhere, i.e. Nelder–Mead stops short of a stationary point in 22 dimensions and the strong-convexity premise of Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> is *not verified* at its iterates (the BFGS iterates would be the place to test it). (b) The cost of the next stage along the segment from $`p^{(\kappa_i)}`$ to $`p^{(\kappa_{i+1})}`$ (seed 2), relative to its value at the carried guess: it decreases monotonically along every segment, i.e. the carried guess lies inside the basin of the next minimiser, the mechanism Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> describes. (c) Cross-evaluation: the minimiser of $`\kappa_i`$ evaluated at every other $`\kappa_j`$; the near-diagonal structure shows that a minimiser is good only for nearby window sizes, which is why the schedule must be gradual. Inherited from R002 Figure 37.*


# Discussion

The analysis and the experiments agree on the mechanism. Short windows bound the amplification of the initial-condition error by $`e^{\mu\Delta T}`$ (Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a>, Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>), which keeps the landscape smooth (Figures <a href="#fig:gp" data-reference-type="ref" data-reference="fig:gp">5</a>, <a href="#fig:land1d" data-reference-type="ref" data-reference="fig:land1d">10</a>) at the price of weak discrimination between nearby parameters (Figure <a href="#fig:multi" data-reference-type="ref" data-reference="fig:multi">4</a>c); removing few nodes at a time moves the cost, and hence the minimiser, by a bounded amount (Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a>, Figures <a href="#fig:prop1" data-reference-type="ref" data-reference="fig:prop1">8</a>, <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>b), so the optimiser stays in the basin of the truth while the windows grow into single shooting (Figures <a href="#fig:headline" data-reference-type="ref" data-reference="fig:headline">12</a>–<a href="#fig:paired" data-reference-type="ref" data-reference="fig:paired">13</a>). The revised constants are of the right kind (they can saturate, and for FHN they are global), and along the orbit the per-window exponents of Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">1</a> are within a factor of the measurement; the remaining gap to the observed plateau is intrinsic to worst-case analysis.

Three limitations are visible in the data. The strong-convexity premise is not verified at the iterates a derivative-free optimiser returns (Figure <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>a), so Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> explains the observed behaviour rather than certifying it; with noisy data the truth is not a stationary point of any $`J_\kappa`$ and the parameter error is non-monotone along the sweep (Figures <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a>, <a href="#fig:headline" data-reference-type="ref" data-reference="fig:headline">12</a>); and the flat blow-up penalty can trap a start on the plateau at the very first stage, a defect of the penalty rather than of the propagation that the graded penalty removes (Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a>). Gradient optimisers with guess propagation are the recommended configuration (Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a>, Figure <a href="#fig:lorenz" data-reference-type="ref" data-reference="fig:lorenz">19</a>).

# Provenance and gates

This report contains no new numerics. Its 20 figures are byte-identical copies of R002 Figures 1–6, 10, 11, 13–16, 18, 20, 21, 25, 35–37 and R003 Figure 1 (prefixed by their source report in `figures/`); the three hand-drawn figures of the draft (`shooting_partition.png`, `multiple_shooting.png`, `comparison_shooting_nodes.png`) and R002’s schematic Figure 7 (drawn with the draft’s $`e^{Lt}`$ envelope) are retired. Tables <a href="#tab:lemmas" data-reference-type="ref" data-reference="tab:lemmas">1</a>, <a href="#tab:windows" data-reference-type="ref" data-reference="tab:windows">2</a>, <a href="#tab:T1" data-reference-type="ref" data-reference="tab:T1">3</a> and <a href="#tab:T5" data-reference-type="ref" data-reference="tab:T5">5</a> are verbatim copies; Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a> and every number in the prose are regenerated by `analysis/make_tables.py` from the R002/R003 result files copied into `external_data/` (SHA-256 in `INPUTS.md`). `analysis/verify_R004.py` writes `analysis/results/gates_summary.json`:

- G1: every file in `external_data/` matches its hash in `INPUTS.md`, and every hash equals that of the file in the source report’s folder (byte identity with R002/R003).

- G2: every figure in `figures/` is byte-identical to its source figure in R002/R003.

- G3: every figure referenced by `report.tex` exists, and no retired figure is referenced.

- G4: the verbatim tables in `tables/` are byte-identical to their sources.

- G5: `tables/numbers.tex` and `T2_compact.tex` regenerate byte-identically from `external_data/`.

- G6: the prose numbers agree with R002’s own `table_numbers.json` and R003’s `summary.json` to the printed precision.

- G7: Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> is consistent with the draft where they overlap: the exponent factors of the revised bound are $`\le`$ the draft’s for every $`\Delta T_1,\Delta T_2\in\{1,2,5,10\}`$ with the FHN constants.

- G8: the revised Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">1</a>/<a href="#lem:param" data-reference-type="ref" data-reference="lem:param">2</a> bounds hold at all $`2\times2424`$ probe points of R002 (recomputed from the copied CSVs).

- G9: `report.md` contains every section heading, table and figure caption of `report.tex`.

- G10: `report.pdf` exists, is newer than `report.tex`, and its page count is recorded.

Outcome: see `tables/gates_status.tex` (inlined at build time: 9 of 10 gates pass). Regeneration: `REPRODUCE.md`.
