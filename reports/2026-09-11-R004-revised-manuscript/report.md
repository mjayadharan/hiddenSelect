# Sparse Optimization using Multiple Shooting and Guess Propagation — revised manuscript (R004)

**Report ID:** R004 (`reports/2026-09-11-R004-revised-manuscript/`)  
**Date:** 11 September 2026  
**Authoritative source:** `report.tex` / `report.pdf` — this `.md` is a derived, full-content view generated by `analysis/make_report_md.py`; on disagreement the `.tex` wins.  
**Gates:** 10 of 10 gates pass — `analysis/results/gates_summary.json`

Theorem-like statements, matching the PDF: Definition 1 (confinement time); Lemma 1 (conditioning on $E_X$), Lemma 2 (minimiser inequality), Lemma 3 (local minimiser inequality), Lemma 4 (envelope properties), Lemma 5 (flow sensitivity to the state, local), Lemma 6 (flow sensitivity to the parameters, local), Lemma 7 (no escape from the tube), Lemma 8 (largest residual of a window); Proposition 1 (cost at the truth), Proposition 2 (minimiser vs truth), Proposition 3 (node removal, per datum); Theorem 1 (minimiser under node removal); Corollary 1 (full-window estimates), Corollary 2 (uniform-noise form), Corollary 3 (per-node form), Corollary 4 (collapsed form), Corollary 5 (basin retention), Corollary 6 (re-partition), Corollary 7 (per-window exponents); Remark 1 (reconnection), Remark 2 (smooth majorant), Remark 3 (implemented objective), Remark 4 (re-partition numbers), Remark 5 (envelopes), Remark 6 (weighted norms).

# Multiple shooting and guess propagation: a greedy approach

#### Problem to solve.

Discover the system of governing equations
``` math
\begin{equation}
\frac{\,\mathrm{d}\mathbf x}{\,\mathrm{d}t}=\mathbf F(\mathbf x;p),\qquad \mathbf x\in C^1(\mathbb R)^{n},
\label{eq:11}
\end{equation}
```
such that the trajectory matches the observations $`\{\mathbf y_0,\mathbf y_1,\dots,\mathbf y_{N-1}\}`$ at the time points $`\{t_0,t_1,\dots,t_{N-1}\}`$. Suppose there is a library of candidate functions $`\Theta=\{\Theta_1(\mathbf x),\dots,\Theta_J(\mathbf x)\}`$ such that <a href="#eq:11" data-reference-type="eqref" data-reference="eq:11">(eq. 11)</a> can be written as
``` math
\begin{equation}
\frac{\,\mathrm{d}x_i}{\,\mathrm{d}t}=\sum_{j=1}^{J}p_{ij}\,\Theta_j(\mathbf x),\qquad i=1,\dots,n,
\label{eq:12}
\end{equation}
```
or, more generally, with library functions that carry their own parameters, $`\dot x_i=\sum_j p_{ij}\Theta_j(\mathbf x;\alpha_{ij})`$. Model discovery then means finding the coefficients $`\{p_{ij}\}`$ (and $`\{\alpha_{ij}\}`$) that make these equations agree with the observations, with most coefficients equal to zero. Figure <a href="#fig:problem" data-reference-type="ref" data-reference="fig:problem">1</a> shows the instance used throughout the numerical section: the FitzHugh–Nagumo (FHN) oscillator written over the ten cubic monomials in two states, so that $`7`$ of the $`20`$ coefficients are non-zero.

**Figure 1** (`figures/R002_fig01_problem.pdf`)

!1(figures/R002_fig01_problem.pdf)

***The inverse problem.** (a) The FHN states $`v(t)`$ (blue) and $`w(t)`$ (orange) with the $`N=101`$ noisy samples (dots) at spacing $`\Delta t=1`$; the thin dashed line is the exact trajectory. (b) Phase plane with the vector field, the true orbit and the samples. (c) The unknown $`2\times10`$ coefficient matrix over the library $`\{1,w,w^2,w^3,v,vw,vw^2,v^2,v^2w,v^3\}`$: only 7 of 20 entries are non-zero. The task is to recover the non-zero pattern and the values from the dots in (a). Inherited from R002 Figure 1 (‘figures/R002_fig01_problem.pdf‘).*


#### Loss functions.

Assuming all states are measured, the model-only loss is
``` math
\begin{equation}
J(p)=\frac1{N-1}\sum_{k=1}^{N-1}\lVert \mathbf y_k-\mathbf x_{m,k}\rVert^2,\qquad \mathbf x_{m,k}=x_m(t_k;p),
\label{eq:loss_func_1}
\end{equation}
```
where $`x_m(t;p)`$ is the model trajectory. A variant that also accounts for errors in the observations adds a data term,
``` math
\begin{equation}
\mathbf L(p,\mathbf x)=R_1\frac1{N}\sum_{k=0}^{N-1}\lVert \mathbf x_k-\mathbf y_k\rVert^2+R_2\frac1{N-1}\sum_{k=0}^{N-2}\lVert \mathbf x_k-\mathbf x_{m,k}\rVert^2,
\label{eq:loss_fun_2}
\end{equation}
```
which is useful for sparse and noisy data. The analysis below uses <a href="#eq:loss_func_1" data-reference-type="eqref" data-reference="eq:loss_func_1">(eq. loss_func_1)</a>.

#### Integration and the difficulty of single shooting.

Let $`\delta t`$ be the smallest time step of the integration scheme and $`F_{\delta t}`$ its one-step operator, $`x_m(t+\delta t;p)=F_{\delta t}(x_m(t;p))`$. Tracing the trajectory from $`\mathbf x_0\approx\mathbf y_0`$ to $`t_k`$ composes the operator $`S=(t_k-t_0)/\delta t`$ times, $`x_{m,k}=F^{(S)}_{\delta t}(x_0;p)`$. Many compositions introduce local minima into the loss landscape, make the gradient unstable, and let the trajectory blow up for unstable parameter values. Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a> shows the effect on FHN: a parameter vector only $`0.038`$ away from the truth (in the Euclidean norm on the 20 coefficients) slips phase after two periods and then pays the full size of the attractor in every residual, so the single-shooting cost is $`2.60`$ against $`0.0099`$ at the truth.

**Figure 2** (`figures/R002_fig02_single_shooting.pdf`)

!2(figures/R002_fig02_single_shooting.pdf)

***Single shooting.** The model is integrated once from the first datum $`y_0`$ over the whole record with a wrong parameter $`p_{\rm wrong}`$ (cubic coefficient $`-0.30`$ instead of $`-1/3`$, $`\dot w`$ time-scale multiplied by $`1.15`$; $`\lVert p_{\rm wrong}-p^\star\rVert=0.038`$); thin vertical lines are the residuals. The residual (right, log scale) sits at the noise level until the model slips phase near $`t\approx18`$, then grows and saturates at the size of the attractor; the cost is $`J_{100}(p_{\rm wrong})=2.60`$ against $`0.0099`$ at $`p^\star`$. Inherited from R002 Figure 2 (‘figures/R002_fig02_single_shooting.pdf‘).*


#### Multiple shooting.

One way to tackle this is to restart the trajectory from several points of the record. Partition $`[t_0,t_{N-1}]`$ into $`K`$ windows $`[\tau_{k-1},\tau_k]`$ with nodes $`t_0=\tau_0<\tau_1<\dots<\tau_K=t_{N-1}`$; for simplicity we take $`\{\tau_k\}\subset\{t_0,\dots,t_{N-1}\}`$ (Figure <a href="#fig:partition" data-reference-type="ref" data-reference="fig:partition">3</a>). Each node is the initial condition of the model trajectory on its window, and the cost is
``` math
\begin{equation}
J_K(p)=\sum_{k=1}^{K}\ \sum_{t_i\in(\tau_{k-1},\tau_k]}\lVert x_m(t_i;p,y_{\tau_{k-1}})-y_i\rVert^2,\qquad x_m(t_i;p,y_{\tau_{k-1}})=F^{(S_i)}_{\delta t}(y_{\tau_{k-1}};p).
\label{eq:JK_intro}
\end{equation}
```
The node states are the noisy data themselves; they are *not* optimisation variables and no continuity is enforced at the window ends (this differs from textbook multiple shooting). If $`t_i`$ is not an integrator step point the solver interpolates. As the error analysis of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a> shows, restarting resets the propagation of the initial-condition error at every node, so the amplification is paid over one window instead of over the whole record (Figure <a href="#fig:multi" data-reference-type="ref" data-reference="fig:multi">4</a>). Our goal is not to replace a solver that starts at the first observation but to provide it with a good initial guess: the parameters found with windows are handed to the full single-shooting problem, which avoids its stability problems and lands near its global minimum.

**Figure 3** (`figures/R002_fig03_partition.pdf`)

!3(figures/R002_fig03_partition.pdf)

***The shooting partition.** Top: the data times $`t_0,\dots,t_{N-1}`$ (ticks, spacing $`\Delta t`$) and the shooting nodes $`\tau_0<\tau_1<\dots<\tau_K`$ placed at every $`\kappa`$-th datum; the windows $`[\tau_{k-1},\tau_k]`$ of length $`\Delta T=\kappa\Delta t`$ are shaded alternately. Bottom: the coarser partition obtained by removing every other node ($`K/2`$ windows of length $`2\Delta T`$), the elementary step of guess propagation. Schematic; replaces the hand-drawn `shooting_partition.png` of the draft. Inherited from R002 Figure 3.*


**Figure 4** (`figures/R002_fig04_multiple_shooting.pdf`)

!4(figures/R002_fig04_multiple_shooting.pdf)

***Multiple shooting.** The same wrong parameter as Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a>, integrated (a) as single shooting ($`\kappa=100`$, $`J=2.60`$) and (b) restarting from the datum at every 10th sample ($`\kappa=10`$, $`J=0.0140`$); windows shaded, node markers highlighted; (c) the per-datum residual magnitude for $`\kappa=100,10,5`$. Restarting resets the accumulated error at every node. The flip side, visible in (c): short windows keep even a wrong model near the noise floor, i.e. they give a smooth landscape at the price of less power to discriminate $`p`$ from $`p^\star`$, which is why the sweep must end at single shooting. Replaces `multiple_shooting.png`. Inherited from R002 Figure 4.*


#### Guess propagation.

Start with many shooting nodes, solve, remove nodes, and restart the optimisation from the previous optimum (Algorithm <a href="#alg:gp" data-reference-type="ref" data-reference="alg:gp">Algorithm 1</a>, Figure <a href="#fig:gp" data-reference-type="ref" data-reference="fig:gp">5</a>). With one window per data interval and $`\delta t=\Delta t`$ the problem is close to a finite-difference (SINDy-type) regression and nearly convex; as nodes are removed the model is enforced over longer and longer horizons and the landscape acquires local minima, but the optimiser is already inside the right basin. In the later sections we show that, under the assumptions collected in §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>, the optimum moves by a bounded amount at each node removal.

```
<div class="algorithmic">
$`p_{\rm guess}←`$ random $`p^{(K)}←\arg\min_p J_K(p)`$ started from $`p_{\rm guess}`$ $`p_{\rm guess}← p^{(K)}`$ $`p^{(1)}`$ (the single-shooting fit)
</div>
```


**Figure 5** (`figures/R002_fig06_guess_propagation.pdf`)

!5(figures/R002_fig06_guess_propagation.pdf)

***The guess-propagation algorithm.** One-dimensional slices $`J_\kappa(p^\star+s\,u)`$ along one random unit direction $`u`$ (the one with the most local minima at $`\kappa=100`$: $`1,4,8,25`$ local minima at $`\kappa=1,5,25,100`$, circled) on a log scale; hatched band = blow-up plateau; star = global minimum at $`s=0`$. Small windows give a smooth, nearly convex landscape; large windows add local minima and a wide plateau. The algorithm solves the $`\kappa=1`$ problem from a random start and carries each minimiser (arrow) into the next, more rugged landscape, ending with a single-shooting polish. Inherited from R002 Figure 6.*


# Notation and definitions

Everything used in the statements, captions and tables is defined here, including what is inherited from the earlier draft and from reports R002 and R003; the provenance is given with each item. Nothing below is decorative: the assumption labels (A0)–(A3), (B0)–(B2), (C1)–(C2), (R) and the scope tags (S1)–(S4) are cited by name in every statement of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:refine" data-reference-type="ref" data-reference="sec:refine">5</a>.

## Model, flow, admissible region, data and costs

Consider $`\dot x=f(x;p)`$ with $`x\in\mathbb R^{d}`$ and $`p\in\mathbb R^{n_p}`$ (the earlier draft wrote $`p\in\mathbb R`$; nothing below uses a scalar parameter, and $`n_p=20`$ in the numerical section). $`p^\star`$ is the true parameter and $`x^\star(t)`$ the true trajectory. The *flow* $`\varphi_f(t;\,p,\,x_0)`$ is the solution of the initial-value problem at time $`t`$; it satisfies $`\varphi_f(t_1+t_2;\,p,\,x_0)=\varphi_f(t_2;\,p,\,\varphi_f(t_1;\,p,\,x_0))`$. In the analysis the flow replaces the numerical integrator, i.e. discretisation error is ignored. The data are $`N`$ samples $`y_0,\dots,y_{N-1}`$ at times $`t_i=t_0+i\,\Delta t`$; the indexing and the noise model are stated in full below.

#### The labels used below, in one place.

Every label is defined where it is introduced; this list exists so that a reader meeting one in a later section can find it.

- **(A0)** regularity and admissible region ($`X`$, $`P`$, $`\mathcal T`$, $`r_X`$, $`\Delta T_{\max}`$); **(A1)** closure of the admissible region, with the two necessary conditions (A1-i) and (A1-ii); **(A2)** noise model, in the bounded variant (A2-a) and the Gaussian variant (A2-b), together with the admissible event $`E_X`$; **(A3)** the *uniform random strong-convexity assumption* of Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>, under which the realisation-wise displacement bound may be integrated.

- **(S1)–(S4)** the four scopes of statement (local deterministic flow estimates; confinement; pathwise noisy-cost statements on $`E_X`$; expectations). Every theorem environment in §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> opens with its tag.

- **(B0)–(B2)** the hypotheses of the basin-retention Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a>; **(C1)–(C2)** the hypotheses of the penalty-bias bound of Remark <a href="#rem:impl" data-reference-type="ref" data-reference="rem:impl">3</a>(ii); **(R)** the reconnection hypothesis of Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>, which is what would relate the constrained minimisers of the theory to the unconstrained iterates of §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a>.

Assumptions (A0)–(A2) are fixed *before the data are seen*; nothing in them is chosen a posteriori, so none of $`X`$, $`P`$, $`L`$, $`\tilde L`$, $`\mu`$, $`\mu_-`$ is a random object. (A3) and (R), by contrast, are assumptions *about* noise-dependent objects and are flagged as such where they are used.

#### (A0) Regularity and admissible region.

Fix a compact convex parameter set $`P\subset\mathbb R^{n_p}`$ with $`p^\star\in P`$; a tube radius $`r_X>0`$; the closed tube around the true orbit
``` math
\begin{equation}
\mathcal T=\big\{x\in\mathbb R^{d}:\ \operatorname{dist}\big(x,\,x^\star([t_0,t_{N-1}])\big)\le r_X\big\};
\label{eq:tube}
\end{equation}
```
a compact *convex* $`X\subset\mathbb R^{d}`$ with $`\mathcal T\subseteq X`$; and a horizon $`\Delta T_{\max}>0`$ at least as large as the longest window of any partition under discussion — *the coarse partitions included*, i.e.
``` math
\begin{equation}
\Delta T_{\max}\ \ge\ \max_{\text{partitions under discussion}}\ \max_k\,(\tau_k-\tau_{k-1}),
\label{eq:DTmaxdef}
\end{equation}
```
where “partitions under discussion” means every fine partition, every coarse partition obtained from it by a node removal of §<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a>, and every common refinement of Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a>. Assume $`f`$ is $`C^1`$ on an open neighbourhood of $`X\times P`$, and that for every $`(x,p)\in X\times P`$ the solution of $`\dot\xi=f(\xi;p)`$, $`\xi(0)=x`$, exists for as long as it remains in $`X`$, up to time $`\Delta T_{\max}`$. All constants of §<a href="#sec:consts" data-reference-type="ref" data-reference="sec:consts">2.2</a> are suprema or infima over $`X\times P`$. For a removal set $`\mathcal I_R`$ write
``` math
\begin{equation}
\Lambda_R:=\max_{k\in\mathcal I_R}\,(h_k+w_k)
\label{eq:LambdaR}
\end{equation}
```
for the longest elapsed time of a coarse predictor, from its retained launch node to the last datum it predicts, with $`h_k`$ and $`w_k`$ as in Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>. **Read this:** $`\Lambda_R`$ is *not* a new horizon: it is the longest *affected coarse window*, and <a href="#eq:DTmaxdef" data-reference-type="eqref" data-reference="eq:DTmaxdef">(eq. DTmaxdef)</a> already covers it. Indeed, let $`\tau_j,\dots,\tau_{j+n}`$ be a maximal run of consecutively removed nodes, with $`\tau_{j-1}`$ and $`\tau_{j+n+1}`$ retained. For every $`k`$ in the run $`\tau_k^{-}=\tau_{j-1}`$, so $`h_k+w_k=\tau_{k+1}-\tau_{j-1}`$, which increases along the run and is maximal at $`k=j+n`$, where $`\tau_{k+1}=\tau_{j+n+1}`$ is retained and $`h_k+w_k=\tau_{j+n+1}-\tau_{j-1}`$ — the coarse window spanning the run. Hence $`\Lambda_R\le\max_k(\text{coarse window }k)\le\Delta T_{\max}`$ automatically. An earlier version of this paragraph claimed that $`h_k+w_k`$ is not a coarse window for a consecutive run and imposed $`\Delta T_{\max}\ge\Lambda_R`$ as a separate requirement, together with the statement that this makes <a href="#eq:closure" data-reference-type="eqref" data-reference="eq:closure">(eq. closure)</a> strictly harder; both are withdrawn. $`\Lambda_R`$ is retained as notation because §<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a> contrasts it with the collapsed argument $`\Delta T_1+\Delta T_2`$ of Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a>, which can be strictly larger. **Read this:** (A0) is not cosmetic. For the $`20`$-coefficient cubic library the state Jacobian $`J_f(x;p)`$ depends on $`p`$ — the coefficient of $`v`$ in $`\dot v`$ sits on its diagonal and shifts $`\mu`$ one for one — so a constant measured at $`p^\star`$ certifies nothing at $`p_{\rm wrong}`$, $`p^{(K)}`$ or $`\hat p^{(K)}`$. Worse, polynomial candidates with bounded coefficients can blow up in finite time. The analysis is a statement about $`X\times P`$, and the numerical plateau lies outside it. Where the implemented loss returns its flat blow-up value, the *numerical* trajectory produced by the integrator has exceeded $`10^3`$ in some component or returned NaN; this manuscript’s bounds say nothing about such an evaluation, in either direction. In particular it is *not* claimed that the exact flow left $`X`$, nor that the candidate is outside $`P`$: the threshold $`10^3`$ in `ms_loss` is an implementation constant chosen to keep the optimiser numerically alive, it has never been related to $`X`$, the observation is about a Tsit5 trajectory and not about the exact flow, and (A1) and the event $`E_X`$ are unverified for the realised experiment (premise check P1, §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>). Recovering *any* inference from the plateau would require all four: a verified $`E_X`$, a verified (A1), a proof that $`X\subset\{\lVert x\rVert_\infty<10^3\}`$, and a controlled bound on the integrator’s error. None is available here. Finally, the constants quoted in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> are *measurements along the true orbit at $`p^\star`$ over R002’s probe box*, i.e. indicative values, not certified suprema over $`X\times P`$.

#### (A1) Closure of the admissible region.

With $`\mu^{+}=\max(\mu,0)`$ and $`Q`$ as in <a href="#eq:Qdef" data-reference-type="eqref" data-reference="eq:Qdef">(eq. Qdef)</a>, there is $`r'\in(0,r_X]`$ with
``` math
\begin{equation}
e^{\mu^{+}\Delta T_{\max}}\,r' \;+\; Q(\Delta T_{\max})\,\operatorname{diam}(P)\;<\;r_X .
\label{eq:closure}
\end{equation}
```
Since $`r'>0`$, <a href="#eq:closure" data-reference-type="eqref" data-reference="eq:closure">(eq. closure)</a> is satisfiable *if and only if* both
``` math
\begin{equation}
\text{(A1-i)}\quad Q(\Delta T_{\max})\,\operatorname{diam}(P)\;<\;r_X
\qquad\text{and}\qquad
\text{(A1-ii)}\quad r'\;<\;e^{-\mu^{+}\Delta T_{\max}}\Big(r_X-Q(\Delta T_{\max})\operatorname{diam}(P)\Big).
\label{eq:closure_two}
\end{equation}
```
The consequence of (A0)–(A1) that the rest of the manuscript uses is Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> (*no escape from the tube*) and its Corollary <a href="#cor:confined" data-reference-type="ref" data-reference="cor:confined">1</a>; they are proved in §<a href="#sec:lemma1" data-reference-type="ref" data-reference="sec:lemma1">3.1</a>, after the local flow lemmas whose hypotheses they discharge. **Read this:** (A1-i) is the binding one, and it contains no $`r'`$. *(i)* (A1-i) is a condition on the *parameter set*: if it fails, no positive noise radius whatever satisfies <a href="#eq:closure" data-reference-type="eqref" data-reference="eq:closure">(eq. closure)</a>, and the confinement machinery is unavailable for that $`\Delta T_{\max}`$ at any noise level. Describing the long-window failure as “$`r'`$ must be exponentially small” — as an earlier version of this paragraph did — misplaces it. *(ii)* Whether enlarging $`r_X`$ helps cannot be settled in general, and is not settled here. Feasibility of (A1-i) must be determined *jointly* from $`\mu(X\times P)`$, $`\tilde L(X\times P)`$, $`r_X`$ and $`P`$: $`\mu`$ and $`\tilde L`$ are suprema over $`X\times P`$ with $`X\supseteq\mathcal T(r_X)`$, hence non-decreasing in $`r_X`$, and $`Q(\Delta T_{\max})`$ depends exponentially on $`\mu(X\times P)\Delta T_{\max}`$ while the right-hand side of (A1-i) grows only linearly in $`r_X`$; but no general conclusion follows from monotonicity alone, and the constants must be *recomputed* over the larger region rather than carried over. **Read this:** in particular the values $`\mu=1.17`$ and $`\tilde L=10.56`$ *cannot be held fixed while $`X`$ grows*. For the cubic candidate library $`\partial f/\partial p`$ contains monomials through degree three, so $`\tilde L(X)`$ grows without bound as $`X`$ expands; and $`\mu`$ is not uniform over $`P`$, since candidate coefficients alter the Jacobian and may introduce positive growth that is unbounded on a large $`X`$. Two earlier versions of this item are withdrawn: one asserted that the constants are strictly increasing in $`r_X`$ and that (A1-i) is a competition a larger tube necessarily loses; the other asserted the opposite, that inflating the tube to a radius exceeding $`Q(\Delta T_{\max})\operatorname{diam}(P)`$ makes (A1-i) satisfiable for FHN with $`\mu=1.17`$ and $`\tilde L=10.56`$ held fixed. Both used constants that are not global for the candidate family the report actually fits. *(iii)* Orders of magnitude, *indicative only*, obtained by inserting the probe-region constants $`\mu=1.17`$ and $`\tilde L=10.56`$ into $`Q(s)=\frac{\tilde L}{\mu}(e^{\mu s}-1)`$: $`Q(1)=20.1`$, $`Q(2)=84.7`$, $`Q(5)=3.13\cdot10^{3}`$, $`Q(10)=1.09\cdot10^{6}`$, $`Q(75)=1.16\cdot10^{39}`$, $`Q(95)=1.69\cdot10^{49}`$, $`Q(100)=5.86\cdot10^{51}`$. Read with those constants, (A1-i) would require $`\operatorname{diam}(P)<r_X/20`$ at $`\Delta T_{\max}=1`$ and $`\operatorname{diam}(P)<r_X/(5.9\cdot10^{51})`$ at $`\Delta T_{\max}=100`$. *(iv)* **Read this:** nothing in this section is *certified* at any window length. The constants above were not computed over the $`X\times P`$ of (A0), so (A1) has not been evaluated for any $`\Delta T_{\max}`$, short or long; what the orders of magnitude show is that short windows are the only regime in which (A1-i) is not already excluded by many orders of magnitude, and whether it holds there is unknown. An earlier version of this item said that the machinery “certifies short windows only” and that “the theory supports the beginning of the schedule and is silent at its end”; both are withdrawn as unearned. The correct reading is: the theory is conditional throughout the schedule, and the indicative constants motivate — without establishing — the conclusion that the condition is hopeless at its end, the terminal single-shooting stage, which is the stage the headline experiment of §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> reports. *(v)* These constants were measured over the probe box of R002/R003, not over the $`X\times P`$ of (A0), which has never been instantiated numerically. The table above is therefore indicative of magnitude and is *not* a check of (A1); see the premise check P1 in §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>.

#### (A2) Noise model and the admissible event.

$`y_i=x_i+\eta_i`$, $`x_i=x^\star(t_i)`$, with $`\eta_0,\dots,\eta_{N-1}`$ i.i.d., $`\mathbb E\eta_i=0`$ and $`\operatorname{Cov}(\eta_i)=\Sigma=\operatorname{diag}(\sigma_1^2,\dots,\sigma_d^2)`$, so $`\mathbb E\lVert \eta_i\rVert^2=\operatorname{tr}\Sigma`$. Two variants are used:

1.  *bounded*: $`\lVert \eta_i\rVert\le r'`$ almost surely;

2.  *Gaussian*: $`\eta_i\sim\mathcal N(0,\Sigma)`$, the law the data generator of R002 samples.

Define the *admissible event*
``` math
\begin{equation}
E_X=\Big\{\max_{0\le i\le N-1}\lVert \eta_i\rVert\le r'\Big\}.
\label{eq:EX}
\end{equation}
```
Under (A2-a), $`\mathbb P(E_X)=1`$. Under (A2-b), for $`r'>\sqrt{\operatorname{tr}\Sigma}`$,
``` math
\begin{equation}
\mathbb P(E_X^{c})\ \le\ N\min\Big\{\frac{\operatorname{tr}\Sigma}{r'^{\,2}},\
\exp\Big(-\frac{(r'-\sqrt{\operatorname{tr}\Sigma})^{2}}{2\lVert \Sigma\rVert_2}\Big)\Big\}
\label{eq:tail}
\end{equation}
```
(Markov on $`\lVert \eta_i\rVert^2`$; Gaussian concentration of the $`\lVert \Sigma\rVert_2^{1/2}`$-Lipschitz map $`\xi\mapsto\lVert \Sigma^{1/2}\xi\rVert`$ about its mean, together with $`\mathbb E\lVert \eta_i\rVert\le\sqrt{\operatorname{tr}\Sigma}`$). **Read this:** the noise is *anisotropic*. R002 draws component $`j`$ with standard deviation $`\sigma_j=0.05\times\operatorname{std}_t x^\star_j`$, so $`\Sigma\neq\sigma^2 I_d`$ and $`\mathbb E\lVert \eta_i\rVert^2=\operatorname{tr}\Sigma`$, not $`d\sigma^2`$. The isotropic model is the specialisation $`\operatorname{tr}\Sigma=d\sigma^2`$; all statements below are written with $`\operatorname{tr}\Sigma`$. On $`E_X`$ every shooting node satisfies $`\lVert y_i-x_i\rVert\le r'`$, which is exactly the hypothesis Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> needs at every launch. Expectations are unconditional under (A2-a) and *conditional on $`E_X`$* under (A2-b); Lemma <a href="#lem:trunc" data-reference-type="ref" data-reference="lem:trunc">1</a> is what makes the latter carry the same constants. No unconditional expectation is claimed under (A2-b): off $`E_X`$ the constants of (A0) do not apply, a polynomial right-hand side can blow up in finite time, and $`\mathbb E J_K(p^\star)`$ is not asserted to be finite.

<div id="lem:trunc" class="lemma">

**Lemma 1** (conditioning on $`E_X`$ preserves the noise structure). *Assume (A2-b). Conditionally on $`E_X`$, the vectors $`\eta_0,\dots,\eta_{N-1}`$ are independent, $`\mathbb E[\eta_i\mid E_X]=0`$, and $`\mathbb E[\lVert \eta_i\rVert^2\mid E_X]\le\operatorname{tr}\Sigma`$.*

</div>

<div class="proof">

*Proof.* $`E_X=\bigcap_i A_i`$ with $`A_i=\{\lVert \eta_i\rVert\le r'\}`$ is a product event, so the conditional joint law is the product of the conditional marginals: independence is preserved. Each $`A_i`$ and the density of $`\mathcal N(0,\Sigma)`$ are invariant under $`\eta\mapsto-\eta`$, so each conditional law is centrally symmetric and its mean is $`0`$. Finally $`\lVert \eta_i\rVert^2`$ is non-decreasing and $`\mathbf 1_{A_i}`$ non-increasing in the same scalar $`\lVert \eta_i\rVert`$, so $`\operatorname{Cov}(\lVert \eta_i\rVert^2,\mathbf 1_{A_i})\le0`$ by Chebyshev’s association inequality, i.e. $`\mathbb E[\lVert \eta_i\rVert^2\mathbf 1_{A_i}]\le\operatorname{tr}\Sigma\cdot\mathbb P(A_i)`$. ◻

</div>

#### (A3) Uniform random strong convexity (used only for expectations).

There exist a *deterministic* $`m>0`$ and a *deterministic* convex $`U\subseteq P`$ with $`p^\star\in U`$ such that, almost surely on $`E_X`$, $`J_K`$ is $`m`$-strongly convex on $`U`$ and $`p^{(K)}\in U`$, and $`p^{(K)}`$ is measurable. (A3) is invoked in exactly one place, the expectation form of Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>; it is listed here because it is an assumption of the same standing as (A0)–(A2) and is named in the scope paragraph below. **Read this:** what the previous revision called “(A2)” in Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> is this (A3); (A2) is the noise model. (A3) is an assumption about the *noise-dependent* objects $`J_K`$, $`p^{(K)}`$, $`m`$ and $`U`$, and it is *not* verified for the FHN data (Figures <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a> and <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>).

#### Scope of statements.

Four kinds of statement appear below and they are not interchangeable. Each theorem environment in §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> opens with its tag.

<div class="description">

Hypothesis: (A0), together with the requirement that the two trajectories named in the statement, *and the chord joining them*, remain in $`X`$ on the interval in question — formally $`t\le T_X`$ of Definition <a href="#def:Tx" data-reference-type="ref" data-reference="def:Tx">1</a>. No noise model, no event, no (A1). *Members:* Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a>, Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">7</a>(i)–(ii).

Hypothesis: (A0)–(A1) and a launch within $`r'`$ of the true orbit. Conclusion: $`T_X`$ is the whole window, so (S1) applies on the full window to every such pair. Still deterministic. *Members:* Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> and Corollary <a href="#cor:confined" data-reference-type="ref" data-reference="cor:confined">1</a>.

Hypothesis: (A0)–(A2), and the event $`E_X`$ of <a href="#eq:EX" data-reference-type="eqref" data-reference="eq:EX">(eq. EX)</a>, which is exactly what makes every shooting node an admissible launch for (S2). The conclusions are deterministic inequalities, asserted realisation by realisation on $`E_X`$ and nowhere else. *Members:* Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">8</a>, Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>(a), the pathwise bound of Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>, Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> and Corollaries <a href="#cor:removal_rho" data-reference-type="ref" data-reference="cor:removal_rho">2</a>–<a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a>, Corollaries <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> and <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a>.

Unconditional under the bounded model (A2-a), where $`\mathbb P(E_X)=1`$; *conditional on $`E_X`$* under the Gaussian model (A2-b), with the same constants by Lemma <a href="#lem:trunc" data-reference-type="ref" data-reference="lem:trunc">1</a> and $`\mathbb P(E_X^{c})`$ bounded by <a href="#eq:tail" data-reference-type="eqref" data-reference="eq:tail">(eq. tail)</a>. The displacement expectation additionally requires (A3). No unconditional expectation is claimed under (A2-b). *Members:* Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>(b) and the expectation form of Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>.

</div>

**Read this:** the distinction between (S1) and (S3) is the one a previous revision collapsed, by declaring that “every inequality in §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> holds on $`E_X`$”. Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> contain no noise: their hypothesis is a *geometric* containment condition, and stamping a probabilistic event on them hides the dependency that Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> exists to establish — it is that hiding which made an earlier version of Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> circular, since it invoked globally-quantified versions of the very lemmas whose hypotheses its own conclusion supplies. The order is: (S1) is proved from (A0) alone; (S2) uses (S1) to prove confinement; (S3) uses (S2) on the event where the launches are admissible; (S4) integrates (S3).

#### Data and indexing (one convention throughout).

There are $`N`$ samples $`y_0,\dots,y_{N-1}`$ at times $`t_i=t_0+i\,\Delta t`$, $`i=0,\dots,N-1`$; hence $`N-1`$ data intervals and $`N-1`$ residuals. (In §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a>, $`N=101`$, $`\Delta t=1`$, $`t_0=0`$, $`t_{N-1}=100`$.) The observation model is <a href="#eq:EX" data-reference-type="eqref" data-reference="eq:EX">(eq. EX)</a>’s (A2); we write $`\lVert \eta\rVert_{\max}=\max_i\lVert \eta_i\rVert`$.

#### Partitions.

Shooting nodes are data times $`\tau_0=t_0<\tau_1<\dots<\tau_{K-1}`$, *from which windows are launched*, together with the right end of the record $`\tau_K=t_{N-1}`$, which is not a launching node. Window $`k`$ is $`(\tau_{k-1},\tau_k]`$, $`k=1,\dots,K`$; $`\tau(i)`$ is the last node at or before $`t_i`$; $`n_k=\#\{i:\,t_i\in(\tau_{k-1},\tau_k]\}`$, so $`\sum_{k=1}^{K}n_k=N-1`$ *exactly*; $`n_{\max}=\max_k n_k`$ and $`\Delta T=\max_k(\tau_k-\tau_{k-1})`$. For the uniform partition used in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a>, $`\tau_k=t_{k\kappa}`$ for $`k\le K-1`$ with $`K=\lceil (N-1)/\kappa\rceil`$; e.g. $`\kappa=3`$, $`N=101`$ gives $`K=34`$, nodes at data indices $`0,3,\dots,99`$ and a last window $`(t_{99},t_{100}]`$ holding one datum. The costs are
``` math
\begin{align}
J_K(p)&=\sum_{k=1}^{K}\sum_{t_i\in(\tau_{k-1},\tau_k]}
  \lVert \varphi_f(t_i-\tau_{k-1};\,p,\,y_{\tau_{k-1}})-y_i\rVert^2
  &&\text{($K$ windows; $N-1$ terms),}\label{eq:JK}\\
J_1(p)&=\sum_{i=1}^{N-1}\lVert \varphi_f(t_i-t_0;\,p,\,y_0)-y_i\rVert^2 &&\text{(single shooting),}\label{eq:J1}\\
J_K^\star(p)&=\sum_{k=1}^{K}\sum_{t_i\in(\tau_{k-1},\tau_k]}
  \lVert \varphi_f(t_i-\tau_{k-1};\,p,\,x_{\tau_{k-1}})-x_i\rVert^2 &&\text{(noise-free windowed cost).}
\label{eq:JKstar}
\end{align}
```
$`J_K^\star`$ launches every window from the *true* state and compares with the true state, so $`J_K^\star(p^\star)=0`$ and hence $`p^\star\in\arg\min J_K^\star`$ for every partition. **Read this:** membership, not equality. $`J_K^\star(p^\star)=0`$ says $`p^\star`$ is *a* minimiser; with $`n_p=20`$ library coefficients fitted from one $`101`$-sample record of a two-state system there may be others, and no persistence-of-excitation or identifiability condition is assumed or verified anywhere in this report. Every statement below is about the *displacement between minimisers*, not about recovering a unique $`p^\star`$. Note also that the earlier draft compared $`J_K`$ with the noise-free *single-shooting* cost $`J^\star=J_1^\star`$; the decomposition used in its proof actually bounds the distance to $`J_K^\star`$, which is the comparator used here.

#### Minimisers, and the only optimality condition used.

All minimisers are taken over the compact set $`P`$ of (A0):
``` math
\begin{equation}
p^{(K)}\in\arg\min_{p\in P}J_K(p),\qquad
\hat p^{(K)}\in\arg\min_{p\in P}\hat{\mathcal J}_K(p),\qquad
p^{\rm pen}\in\arg\min_{p\in P}\big(J_K/N+\text{penalty}\big),
\label{eq:minimisers}
\end{equation}
```
each of which exists because the objective is continuous and $`P`$ compact — the draft’s $`\arg\min_p`$ over $`\mathbb R^{n_p}`$ guaranteed neither existence nor validity of the constants.

<div id="lem:vi" class="lemma">

**Lemma 2** (minimiser inequality). *Let $`P`$ be convex, $`F`$ differentiable on a neighbourhood of $`P`$, $`p\in\arg\min_{P}F`$, and let $`U\subseteq P`$ be convex with $`p\in U`$ and $`F`$ $`m`$-strongly convex on $`U`$. Then
``` math
\begin{equation}
F(q)\ \ge\ F(p)+\tfrac m2\lVert q-p\rVert^2\qquad\text{for all }q\in U.
\label{eq:vi}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* First-order optimality on a convex set gives $`\langle\nabla F(p),q-p\rangle\ge0`$ for every $`q\in P`$, hence for every $`q\in U`$. Strong convexity on $`U`$ gives $`F(q)\ge F(p)+\langle\nabla F(p),q-p\rangle+\tfrac m2\lVert q-p\rVert^2`$. Add. ◻

</div>

**Read this:** <a href="#eq:vi" data-reference-type="eqref" data-reference="eq:vi">(eq. vi)</a> is the *only* consequence of optimality used in Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a>, Corollaries <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> and <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> and Remark <a href="#rem:impl" data-reference-type="ref" data-reference="rem:impl">3</a>(ii). The draft, and the previous revision, wrote $`\nabla J_K(p^{(K)})=0`$ in each of those proofs, which is false at a minimiser on $`\partial P`$. With <a href="#eq:vi" data-reference-type="eqref" data-reference="eq:vi">(eq. vi)</a> every one of those proofs holds verbatim for minimisers on $`\partial P`$ and no minimiser is required to be interior. This is adopted as *insurance*, not as a response to a boundary phenomenon known to occur. An earlier version of this paragraph asserted that the $`\gamma=0.05`$ shrinkage makes boundary and near-boundary solutions typical; that claim is withdrawn. The shrinkage pulls coefficients towards $`0`$, which is a statement about coordinates, not about $`\partial P`$; $`P`$ has never been instantiated, so nothing is known about where its boundary is or whether any reported solution is near it. The variational inequality is used because it is never weaker than $`\nabla J_K=0`$ and costs nothing, and interiority is needed in exactly one place — the stationarity sentence of Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> — where it is obtained from (B0) rather than assumed.

<div id="lem:vi_local" class="lemma">

**Lemma 3** (local minimiser inequality). *Let $`U\subseteq\mathbb R^{n_p}`$ be convex, $`F`$ differentiable on a neighbourhood of $`U`$, and let $`p\in U`$ satisfy either $`\nabla F(p)=0`$, or $`\langle\nabla F(p),q-p\rangle\ge0`$ for all $`q\in U`$. If $`F`$ is $`m`$-strongly convex on $`U`$ then
``` math
\begin{equation}
F(q)\ \ge\ F(p)+\tfrac m2\lVert q-p\rVert^2\qquad\text{for all }q\in U .
\label{eq:vi_local}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* Strong convexity on $`U`$ gives $`F(q)\ge F(p)+\langle\nabla F(p),q-p\rangle+\tfrac m2\lVert q-p\rVert^2`$; the hypothesis makes the middle term non-negative. ◻

</div>

<div id="rem:reconnect" class="remark">

*Remark 1* (reconnection to the unconstrained computation). The statements of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> concern minimisers of the *constrained* problems <a href="#eq:minimisers" data-reference-type="eqref" data-reference="eq:minimisers">(eq. minimisers)</a>, taken over the compact convex $`P`$ of (A0). The runs reported in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> solve *unconstrained* problems: Nelder–Mead and BFGS over $`p\in\mathbb R^{n_p}`$, with no projection, no feasibility test, and no instantiation of $`P`$ anywhere in the implementation. A point $`p_{\rm num}`$ returned by such a run is therefore *not*, without further hypotheses, one of the objects any theorem below bounds.

The hypothesis that would reconnect them is

> **(R)** There exist a compact convex $`P`$ satisfying (A0)–(A1) and a convex $`U\subseteq P`$ with $`p_{\rm num}\in\operatorname{int}U`$, such that $`\nabla J_K(p_{\rm num})=0`$ and $`J_K`$ is $`m`$-strongly convex on $`U`$ for some $`m>0`$.

It is *not* verified anywhere in this report (premise check P2, §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>). Under (R), Lemma <a href="#lem:vi_local" data-reference-type="ref" data-reference="lem:vi_local">3</a> gives <a href="#eq:vi_local" data-reference-type="eqref" data-reference="eq:vi_local">(eq. vi_local)</a> at $`p=p_{\rm num}`$ on $`U`$, which is the *only* consequence of optimality used in Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and Corollaries <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> and <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a>; and $`p_{\rm num}`$ is a local minimiser of $`\min_{p\in P}J_K`$. Consequently, under (R):

1.  Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> holds verbatim with $`p^{(K)}`$ replaced by $`p_{\rm num}`$, provided $`\bar B\subseteq U`$: its proof uses $`p^{(K)}\in\arg\min_P J_K`$ only through Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a> on $`\bar B`$, and <a href="#eq:vi_local" data-reference-type="eqref" data-reference="eq:vi_local">(eq. vi_local)</a> supplies exactly that. This is the *only* one of the three that runs under (R) as stated.

2.  Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>’s displacement bound holds with $`p_{\rm num}`$ in place of $`p^{(K)}`$ *provided $`p^\star\in U`$ as well* — the proposition applies the minimiser inequality at $`q=p^\star`$, so without $`p^\star\in U`$ neither $`J_K(p^\star)\ge J_K(p_{\rm num})+\tfrac m2\lVert p^\star-p_{\rm num}\rVert^2`$ nor the parameter-displacement bound follows. We therefore state the result-specific variant **(R$`^{p^\star}`$)** $`=`$ (R) together with $`p^\star\in U`$, and use it for Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> only; basin retention (item (i)) keeps the weaker (R).

3.  Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> holds in its *restricted* form only. Its proof also requires $`\hat{\mathcal J}_K(\hat p)\le\hat{\mathcal J}_K(p_{\rm num})`$, i.e. that the coarse minimiser be taken over a set containing $`p_{\rm num}`$; with $`p_{\rm num}`$ merely a local minimiser the usable statement is <a href="#eq:basin" data-reference-type="eqref" data-reference="eq:basin">(eq. basin)</a> of Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a>, over $`\bar B`$.

**Read this:** (R) alone does *not* reconnect all aspects of the numerical experiment, and this remark does not claim that it does. Three further qualifications are needed, each stated elsewhere in this manuscript and none of them verified here. *(a) The event.* Every statement of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> is asserted only on the realised event $`E_X`$ of <a href="#eq:EX" data-reference-type="eqref" data-reference="eq:EX">(eq. EX)</a>; a reported run is covered only if its realised noise satisfies $`\max_i\lVert \eta_i\rVert\le r'`$ for the $`r'`$ of (A1), which premise check P1 records as unevaluated. *(b) The penalty.* The objective actually minimised in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> is the penalised <a href="#eq:Jkappa" data-reference-type="eqref" data-reference="eq:Jkappa">(eq. Jkappa)</a>, not $`J_K`$. Stationarity of the *penalised* numerical objective does *not* imply the stationarity of $`J_K`$ that (R) asserts: the two gradients differ by $`\nabla G`$, and Remark <a href="#rem:impl" data-reference-type="ref" data-reference="rem:impl">3</a>(ii) bounds the resulting displacement only under the further hypotheses (C1)–(C2). *(c) The discretisation.* The runs integrate with a fixed-step Tsit5 scheme, whereas every bound below is written for the exact flow; no bound on the integrator’s error is available here. Finally, no claim is made that $`p_{\rm num}`$ is a *global* minimiser of $`J_K`$ over $`P`$, nor that the minimisers of $`\hat{\mathcal J}_K`$ found in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> are global. (R) is stronger than what §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> establishes and weaker than what a fully rigorous reconnection would need: it does not require exhibiting $`P`$ explicitly, only its existence together with the local convexity, but it does require both, and neither has been checked. The displacement, basin-retention and penalty-bias statements below should therefore be read as *conditional on (R), on $`E_X`$, and on (b)–(c)* whenever they are applied to a number reported in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a>.

</div>

#### Node removal.

$`\mathcal I_R\subset\{1,\dots,K-1\}`$ indexes the removed nodes ($`\tau_0`$ and $`\tau_K`$ are never removed) and $`R=\{0,1,\dots,K\}\setminus\mathcal I_R`$ the retained ones. For $`r\in R\setminus\{K\}`$ let $`r^{+}=\min\{q\in R:\ q>r\}`$ be the next retained index, and for $`k\in\mathcal I_R`$ let $`\tau_k^{-}`$ and $`\tau_k^{+}`$ be the nearest retained nodes to the left and to the right of $`\tau_k`$. $`\hat{\mathcal J}_K`$ is the cost of the coarse partition <a href="#eq:Jhat" data-reference-type="eqref" data-reference="eq:Jhat">(eq. Jhat)</a> and $`\hat p^{(K)}`$ its minimiser. $`\Delta T_1=\max_k(\tau_k-\tau_{k-1})`$ is the longest window of the *fine* partition and $`\Delta T_2=\max_{k\in\mathcal I_R}(\tau_k-\tau_k^{-})`$ is the largest distance from a removed node to the nearest node retained to its left. $`D_k=\{t_i:\ t_i\in(\tau_k,\tau_{k+1}]\}`$ is the fine window launched at the removed node $`\tau_k`$. **Read this:** $`\Delta T`$ and $`\Delta T_1`$ denote the same object — the longest window of the partition under discussion. The subscript is used only in §<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a>, where two partitions are in play: $`\Delta T_1`$ is the *fine* one’s longest window and $`\Delta T_2`$ is the removed-node offset, so that $`\Delta T_1+\Delta T_2`$ bounds a coarse window. The draft gave $`\Delta T`$ and $`\Delta T_1`$ textually identical definitions, which made $`\Delta T_2`$ look like a second window length; it is not.

## Constants of the right-hand side

$`J_f(x;p)=\partial f/\partial x`$. Over the admissible region of (A0),
``` math
\begin{equation}
\begin{aligned}
L&=\sup_{X\times P}\lVert J_f(x;p)\rVert_2, &\qquad
\tilde L&=\sup_{X\times P}\lVert \partial f/\partial p\rVert_2,\\
\mu&=\sup_{X\times P}\lambda_{\max}\!\Big(\tfrac12(J_f+J_f^\top)\Big), &\qquad
\mu_-&=\inf_{X\times P}\lambda_{\min}\!\Big(\tfrac12(J_f+J_f^\top)\Big),
\end{aligned}
\label{eq:mu}
\end{equation}
```
equivalently $`\mu_-\lVert x-z\rVert^2\le\langle f(x;p)-f(z;p),x-z\rangle\le\mu\lVert x-z\rVert^2`$ for $`x,z\in X`$, $`p\in P`$. Always $`-L\le\mu_-\le\mu\le L`$; unlike $`L`$, $`\mu`$ can be zero or negative. We write $`\mu^{+}=\max(\mu,0)`$. **Read this:** $`\mu_-`$ is *region-restricted by necessity*, not by convention. For the true FHN field the symmetric part of $`J_f`$ is $`\begin{psmallmatrix}1-v^2&-0.46\\-0.46&-0.064\end{psmallmatrix}`$, whose smallest eigenvalue tends to $`-\infty`$ as $`|v|\to\infty`$: the global lower logarithmic norm is $`-\infty`$ and no finite global $`\mu_-`$ exists. The value $`-2.96`$ quoted in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> is an infimum over the sampled true orbit and certifies the lower bound of Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> only for perturbations that remain in the region where it was measured. By contrast $`\lambda_{\max}`$ of the same matrix is maximised at $`v=0`$ with value $`1.17`$ and decreases thereafter, so the *upper* constant $`\mu=1.17`$ is global in $`x`$ for the true FHN field — at the single parameter $`p^\star`$, and *not* over a set $`P`$ of candidate coefficient vectors.

For a weighted norm $`\lVert x\rVert_D=\lVert Dx\rVert`$ with $`D`$ invertible, $`\mu_D`$ is the logarithmic norm of $`DJ_fD^{-1}`$ and $`\mathrm{cond}(D)=\lVert D\rVert\lVert D^{-1}\rVert`$ its condition number (written $`\mathrm{cond}`$, not $`\kappa`$, because $`\kappa`$ is the window size throughout this report). The *local* logarithmic norm along a pair of trajectories is $`\mu(t)=\lambda_{\max}(\tfrac12(A(t)+A(t)^\top))`$ for the mean-value matrix $`A(t)`$ of <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a>; $`\Lambda_k=\int_{\tau_{k-1}}^{\tau_k}\mu(s)\,\mathrm{d}s`$ is the window *endpoint* exponent and
``` math
\begin{equation}
M_k=\sup_{\tau_{k-1}\le a\le b\le\tau_k}\exp\Big(\int_a^b\mu(r)\,\mathrm{d}r\Big)\ \ge\ \max\{1,\,e^{\Lambda_k}\}
\label{eq:Mk}
\end{equation}
```
the window *transition* bound (Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">7</a>, Remark <a href="#rem:envelopes" data-reference-type="ref" data-reference="rem:envelopes">5</a>). Write
``` math
\begin{equation}
Q(s)=\frac{\tilde L}{\mu}\big(e^{\mu s}-1\big),\qquad\text{read as }\ \tilde L\,s\ \text{ when }\mu=0,
\label{eq:Qdef}
\end{equation}
```
the continuous extension $`\lim_{\mu\to0}(e^{\mu s}-1)/\mu=s`$. For every sign of $`\mu`$, $`Q(s)\ge0`$ and $`s\mapsto Q(s)`$ is non-decreasing, with $`Q(s)\le\tilde L s`$ when $`\mu\le0`$ and $`Q(s)\le\tilde L/|\mu|`$ when $`\mu<0`$. The residual abbreviations are
``` math
\begin{equation}
\rho_s(p)=\big(1+e^{\mu s}\big)\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert,
\qquad
\bar\rho_s(p)=\max\big\{2\lVert \eta\rVert_{\max},\ \rho_s(p)\big\},\qquad s\ge0 ,
\label{eq:rho}
\end{equation}
```
and, for a launch time $`\sigma`$ and a datum time $`t\ge\sigma`$ with $`t-\sigma\le\Delta T_{\max}`$,
``` math
\begin{equation}
r_{\sigma,t}(p)\ =\ e^{\mu(t-\sigma)}\lVert \eta_\sigma\rVert+Q(t-\sigma)\lVert p-p^\star\rVert+\lVert \eta_t\rVert,
\label{eq:rdatum}
\end{equation}
```
the *per-datum residual bound*: it carries the noise realised at the launch node and at the datum, not the global $`\lVert \eta\rVert_{\max}`$. Replacing both by $`\lVert \eta\rVert_{\max}`$ gives $`r_{\sigma,t}(p)\le\rho_{t-\sigma}(p)`$, and Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">8</a> is exactly the statement $`\lVert \varphi_f(t-\sigma;\,p,\,y_\sigma)-y_t\rVert\le r_{\sigma,t}(p)\le\rho_{t-\sigma}(p)\le\bar\rho_s(p)`$ for $`t-\sigma\le s`$.

<div id="lem:rhobar" class="lemma">

**Lemma 4** (properties of the envelope). *For every $`s\ge0`$, every $`p\in P`$ and every sign of $`\mu`$: *(i)* $`\bar\rho_s(p)=\sup_{0\le r\le s}\rho_r(p)`$, so $`\rho_\sigma\le\bar\rho_s`$ whenever $`0\le\sigma\le s`$; *(ii)* $`s\mapsto\bar\rho_s(p)`$ is non-decreasing; *(iii)* $`\delta\mapsto\bar\rho_s`$ is non-decreasing, where $`\delta=\lVert p-p^\star\rVert`$, and $`E\mapsto\bar\rho_s`$ is non-decreasing, where $`E=\lVert \eta\rVert_{\max}`$; *(iv)* $`\bar\rho_s(p^\star)=\big(1+e^{\mu^{+}s}\big)\lVert \eta\rVert_{\max}`$; *(v)* if $`\mu\ge0`$ then $`\bar\rho_s=\rho_s`$ for every $`s\ge0`$; if $`\mu<0`$ then $`\bar\rho_0=\rho_0=2\lVert \eta\rVert_{\max}`$ for every $`p`$, while for every $`s>0`$
``` math
\bar\rho_s=2\lVert \eta\rVert_{\max}\iff\tilde L\lVert p-p^\star\rVert\le|\mu|\,\lVert \eta\rVert_{\max},
\qquad\text{and}\qquad \bar\rho_s=\rho_s\ \text{otherwise},
```
the branch being independent of $`s`$ on $`s>0`$.*

</div>

<div class="proof">

*Proof.* (i) $`\partial_r\rho_r=e^{\mu r}\big(\mu\lVert \eta\rVert_{\max}+\tilde L\lVert p-p^\star\rVert\big)`$ has a sign independent of $`r`$, so $`\sup_{0\le r\le s}\rho_r=\max\{\rho_0,\rho_s\}`$ and $`\rho_0=2\lVert \eta\rVert_{\max}`$. (ii) is immediate from (i). (iii) A maximum of two functions non-decreasing in $`\delta`$ (resp. $`E`$) is non-decreasing. (iv) At $`p=p^\star`$, $`\bar\rho_s=\max\{2,1+e^{\mu s}\}\lVert \eta\rVert_{\max}=(1+e^{\mu^{+}s})\lVert \eta\rVert_{\max}`$. (v) For $`\mu\ge0`$, $`\rho_s\ge\rho_0=2\lVert \eta\rVert_{\max}`$ by (i). For $`\mu<0`$ write $`Q(s)=\frac{\tilde L}{|\mu|}(1-e^{-|\mu|s})`$ and $`e^{\mu s}-1=-(1-e^{-|\mu|s})`$, so
``` math
\rho_s-2\lVert \eta\rVert_{\max}=\big(1-e^{-|\mu|s}\big)
\Big(\tfrac{\tilde L}{|\mu|}\lVert p-p^\star\rVert-\lVert \eta\rVert_{\max}\Big).
```
For $`s>0`$ the first factor is strictly positive, so the sign is that of $`\tilde L\lVert p-p^\star\rVert-|\mu|\lVert \eta\rVert_{\max}`$ and does not depend on $`s`$; at $`s=0`$ the first factor vanishes and $`\rho_0=2\lVert \eta\rVert_{\max}`$ regardless of $`p`$. ◻

</div>

**Read this:** item (v) is a statement about $`s>0`$. An earlier version asserted it for all $`s\ge0`$, which is false in the “only if” direction at $`s=0`$, where $`\bar\rho_0=2\lVert \eta\rVert_{\max}`$ for *every* parameter distance. Note also that $`\rho_s`$ is *not* monotone in $`s`$: for $`\mu<0`$ and $`p=p^\star`$ it strictly *de*creases, so every step that bounds a shorter window’s residual by a longer window’s uses $`\bar\rho`$, never $`\rho`$.

<div id="rem:rhoplus" class="remark">

*Remark 2* (the smooth quadratic majorant, and why it is not the primary bound). Put $`\rho^{+}_s(p)=\big(1+e^{\mu^{+}s}\big)\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert`$. Then $`\bar\rho_s\le\rho^{+}_s`$ for every $`s\ge0`$ and every $`p`$, with equality *exactly* when
``` math
\mu\ge0,\quad\text{or}\quad s=0,\quad\text{or}\quad\lVert \eta\rVert_{\max}=0,
\quad\text{or}\quad Q(s)\lVert p-p^\star\rVert=0\ \ (\text{i.e.\ }\tilde L=0\ \text{or}\ p=p^\star),
```
and strict inequality otherwise. \[*Proof.* For $`\mu\ge0`$, $`\mu^{+}=\mu`$ and $`\bar\rho_s=\rho_s=\rho^{+}_s`$ by Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(v). For $`\mu<0`$, $`\rho^{+}_s=2\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert`$ and $`\bar\rho_s=\max\{2\lVert \eta\rVert_{\max},(1+e^{\mu s})\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert\}`$: if $`s=0`$, or $`\tilde L=0`$, or $`p=p^\star`$ — equivalently $`Q(s)\lVert p-p^\star\rVert=0`$ — both sides equal $`2\lVert \eta\rVert_{\max}`$; if $`\lVert \eta\rVert_{\max}=0`$ both equal $`Q(s)\lVert p-p^\star\rVert`$; otherwise $`s>0`$, $`\lVert \eta\rVert_{\max}>0`$ and $`Q(s)\lVert p-p^\star\rVert>0`$, and either $`\bar\rho_s=(1+e^{\mu s})\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert<\rho^{+}_s`$ because $`e^{\mu s}<1`$, or $`\bar\rho_s=2\lVert \eta\rVert_{\max}<\rho^{+}_s`$ because $`Q(s)\lVert p-p^\star\rVert>0`$.\] So every bound below remains true with $`\bar\rho`$ replaced by $`\rho^{+}`$; $`\rho^{+}`$ is a single affine form in $`(\lVert \eta\rVert_{\max},\lVert p-p^\star\rVert)`$ with non-negative deterministic coefficients $`A_s=1+e^{\mu^{+}s}`$, $`B_s=Q(s)`$, which makes $`\Delta_K`$ of <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> a homogeneous quadratic form rather than a piecewise quadratic one. **Read this:** $`\rho^{+}`$ is a *convenience*, not the bound, and the regime where the difference matters is *not* exhibited anywhere in this report. *(i) Where $`\bar\rho`$ is strictly better.* Let the system be uniformly contractive on $`X\times P`$, i.e. $`\lambda_{\max}(\operatorname{sym}J_f(x;p))\le\mu<0`$ for all $`(x,p)\in X\times P`$ — for instance a linear field $`\dot x=Ax+B(p)`$ with $`\operatorname{sym}A\prec0`$, or the gradient flow of a potential that is uniformly strongly convex on $`X`$. Then for every $`s>0`$ and every $`p`$ with $`0<\tilde L\lVert p-p^\star\rVert\le|\mu|\lVert \eta\rVert_{\max}`$, Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(v) gives $`\bar\rho_s=2\lVert \eta\rVert_{\max}`$ while $`\rho^{+}_s=2\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert`$, larger by up to $`\tilde L\lVert p-p^\star\rVert/|\mu|`$. Since the whole point of replacing $`L`$ by a one-sided constant is to exploit contraction, the primary statements carry $`\bar\rho`$, and $`\Delta_K`$ is described as *piecewise* quadratic — the truthful description — with the branch switching on the sign of $`\tilde L\lVert q-p^\star\rVert-|\mu|\lVert \eta\rVert_{\max}`$. This is an illustration by construction, not an instance drawn from §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a>. *(ii) FHN exhibits none of this.* The global constant measured in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> is $`\mu=1.17>0`$, and for $`\mu\ge0`$ Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(v) makes $`\rho`$, $`\bar\rho`$ and $`\rho^{+}`$ *identically equal*. So no number in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> — at $`p^\star`$, at the off-truth perturbation, or at $`p_{\rm wrong}`$ — distinguishes the three, and this report contains *no* numerical evidence bearing on the choice; no experiment here exhibits the $`\bar\rho`$ branch at all. The negative per-window exponents $`\Lambda^D_k`$ reported in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> are *not* instances of $`\mu<0`$: they are linearised, weighted, endpoint exponents along the true orbit at the true parameter, and Remark <a href="#rem:envelopes" data-reference-type="ref" data-reference="rem:envelopes">5</a> gives four independent reasons why they may not be substituted for the uniform $`\mu`$ of $`X\times P`$. An earlier version of this remark cited them as the regime where the $`\bar\rho/\rho^{+}`$ distinction “lives”, which commits exactly the substitution Remark <a href="#rem:envelopes" data-reference-type="ref" data-reference="rem:envelopes">5</a> forbids; that citation is withdrawn. *(iii) A still earlier version* made $`\rho^{+}`$ primary and justified it by claiming the choice “costs nothing where every number in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> is evaluated”. That claim was doubly wrong: Figure <a href="#fig:prop1" data-reference-type="ref" data-reference="fig:prop1">8</a> evaluates off-truth as well as at $`p^\star`$, and, more fundamentally, §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> cannot distinguish the alternatives at all.

</div>

$`m>0`$ denotes a strong-convexity constant of a cost near its minimiser (the draft wrote $`\mu`$ for it, and wrote $`m`$ for the parameter dimension; here $`n_p`$ is the parameter dimension, $`m`$ is the strong-convexity constant, $`\mu`$ is the logarithmic norm and $`\kappa`$ is the window size).

## Numerical protocol (R002)

The target is FHN, $`\dot v=v-\tfrac13v^3-w+0.5`$, $`\dot w=\tfrac1{12.5}(v+0.7-0.8w)`$, written over the cubic library $`\Theta=\{1,w,w^2,w^3,v,vw,vw^2,v^2,v^2w,v^3\}`$ ($`J=10`$, $`p\in\mathbb R^{20}`$, 7 non-zeros). Data: fixed-step Tsit5 at $`0.01`$ from $`(1,1)`$ to $`T=156`$, every 100th point kept, cropped to $`t\in[50,150]`$ and shifted to $`[0,100]`$ ($`N=101`$, $`\Delta t=1`$), Gaussian noise of standard deviation $`0.05\times`$ each component’s own standard deviation; $`\lVert \eta\rVert_{\max}=0.159`$, noise floor $`\tfrac1{N-1}\sum\lVert \eta_i\rVert^2=0.0052`$. The optimised cost is the normalised, sparsity-penalised version of <a href="#eq:JK" data-reference-type="eqref" data-reference="eq:JK">(eq. JK)</a>. With $`z=[x_0;p]\in\mathbb R^{d+n_p}`$ and $`\tau(i)=t_{\kappa\lfloor i/\kappa\rfloor}`$,
``` math
\begin{equation}
J_\kappa(z)=\frac1N\Big[\lVert x_0-y_0\rVert^2
 +\sum_{i=0}^{N-2}\big\lVert\hat x\big(t_{i+1}-\tau(i);\,p,\,y_{\tau(i)}\big)-y_{i+1}\big\rVert^2\Big]
 +\frac{\gamma}{n_p}\sum_{j=1}^{n_p}\operatorname{smooth}\ell_1(p_j),
\label{eq:Jkappa}
\end{equation}
```
where $`\hat x(s;p,x)`$ is the $`S`$-substep fixed-step Tsit5 approximation of $`\varphi_f(s;\,p,\,x)`$, $`\kappa`$ is the window size in data intervals ($`K=\lceil(N-1)/\kappa\rceil`$ windows launched at the data indices $`0,\kappa,2\kappa,\dots`$; $`\kappa=1`$ is one window per interval, $`\kappa=N-1=100`$ is single shooting), each interval is integrated with $`S=10`$ substeps of length $`\delta t=\Delta t/S`$, $`\operatorname{smooth}\ell_1(x)=\alpha^{-1}[\log(1+e^{-\alpha x})+\log(1+e^{\alpha x})]`$ with $`\alpha=500`$, and $`\gamma=0.05`$. If a state exceeds $`10^3`$ in magnitude or becomes NaN the loss returns the constant $`10^3`$ (*flat blow-up penalty*, a plateau with zero gradient); the *graded* penalty returns $`10^3(1+\tfrac{N-1-i}{N-1})+J_{\rm partial}/N`$ when the blow-up occurs at index $`i`$. The theory checks use $`\gamma=0`$.

**Read this:** $`x_0`$ is a *dummy* in <a href="#eq:Jkappa" data-reference-type="eqref" data-reference="eq:Jkappa">(eq. Jkappa)</a>. The implementation overwrites the running state with the datum $`y_{\tau(i)}`$ whenever $`i\equiv0\pmod\kappa`$, and $`i=0`$ satisfies this for *every* $`\kappa`$, so the first integration step already starts from $`y_0`$ and $`x_0`$ never enters an integrator — not even at $`\kappa=N-1`$. Its only appearance is the term $`\lVert x_0-y_0\rVert^2/N`$, whose minimiser is $`x_0=y_0`$ and whose Hessian block is $`(2/N)I_d`$ *independently of $`\kappa`$*. Consequently (i) the optimisation is effectively over $`p`$ with $`d`$ inert coordinates carried along; (ii) the Hessian of <a href="#eq:Jkappa" data-reference-type="eqref" data-reference="eq:Jkappa">(eq. Jkappa)</a> in $`z`$ is block diagonal, $`\operatorname{diag}\big((2/N)I_d,\ \nabla^2_p J_\kappa\big)`$, so strong convexity in $`z`$ is equivalent to strong convexity in $`p`$; and (iii) the “sensitivity to $`v_0`$” panel of Figure <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a> is exactly the parabola $`\lVert x_0-y_0\rVert^2/N`$, which is why it is the same shallow parabola at every window size. The multiple-shooting formulation of <a href="#eq:JK" data-reference-type="eqref" data-reference="eq:JK">(eq. JK)</a> — node states fixed at the data, not optimised — is what the code implements; the free $`x_0`$ is vestigial.

<div id="rem:impl" class="remark">

*Remark 3* (what transfers from $`J_K`$ to the implemented objective). Write $`J^{\rm impl}_\kappa(z)=\frac1N\big[\lVert x_0-y_0\rVert^2+J_K(p)\big]+\frac{\gamma}{n_p}\sum_j\operatorname{smooth}\ell_1(p_j)`$, the exact content of <a href="#eq:Jkappa" data-reference-type="eqref" data-reference="eq:Jkappa">(eq. Jkappa)</a> up to discretisation. The first and third terms do not depend on the partition. Hence:

1.  $`\hat{\mathcal J}^{\rm impl}_\kappa-J^{\rm impl}_\kappa=\tfrac1N\big(\hat{\mathcal J}_K-J_K\big)`$, so Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> holds verbatim for the implemented objective with every bound divided by $`N`$, and Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and Corollaries <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a>–<a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> hold with the same substitution *provided $`m`$ is a strong-convexity constant of the penalised cost*.

2.  Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> does *not* transfer. Its comparator $`p^\star`$ minimises $`J_K^\star`$ but is not a minimiser of $`J_K/N+\text{penalty}`$. Write $`F=J_K/N`$ and $`G(p)=\frac{\gamma}{n_p}\sum_j\operatorname{smooth}\ell_1(p_j)`$, and *assume*:

    1.  $`p^{(K)}\in\arg\min_{P}F`$ and $`p^{\rm pen}\in\arg\min_{P}(F+G)`$ (both exist, $`P`$ compact);

    2.  there is a convex $`U\subseteq P`$ containing $`p^{(K)}`$, $`p^{\rm pen}`$ *and the segment joining them*, on which $`F`$ is $`m`$-strongly convex.

    Then
    ``` math
    \begin{equation}
    \lVert p^{\rm pen}-p^{(K)}\rVert\ \le\ \frac{1}{m}\,\big\lVert\nabla G(p^{\rm pen})\big\rVert
    \ \le\ \frac{\gamma}{m\sqrt{n_p}} .
    \label{eq:penbias}
    \end{equation}
    ```
    *Proof.* Adding the two first-order optimality inequalities $`\langle\nabla F(p^{(K)}),\,p^{\rm pen}-p^{(K)}\rangle\ge0`$ and $`\langle\nabla F(p^{\rm pen})+\nabla G(p^{\rm pen}),\,p^{(K)}-p^{\rm pen}\rangle\ge0`$ gives $`\langle\nabla F(p^{\rm pen})-\nabla F(p^{(K)}),\,p^{\rm pen}-p^{(K)}\rangle\le\lVert \nabla G(p^{\rm pen})\rVert\,\lVert p^{\rm pen}-p^{(K)}\rVert`$; strong monotonicity of $`\nabla F`$ on $`U`$ (equivalent to $`m`$-strong convexity there, and requiring the segment to lie in $`U`$, whence (C2)) bounds the left side below by $`m\lVert p^{\rm pen}-p^{(K)}\rVert^2`$. For the gradient, $`\frac{\,\mathrm{d}}{\,\mathrm{d}x}\operatorname{smooth}\ell_1(x)=2\sigma(\alpha x)-1=\tanh(\alpha x/2)\in(-1,1)`$, so $`\lVert \nabla G\rVert_2\le(\gamma/n_p)\sqrt{n_p}=\gamma/\sqrt{n_p}`$, uniformly in $`p`$ and independently of $`\alpha`$. $`\square`$ **Read this:** (C1)–(C2) are *assumptions*, not facts about the FHN runs, and <a href="#eq:penbias" data-reference-type="eqref" data-reference="eq:penbias">(eq. penbias)</a> is therefore conditional — and, when applied to a reported number, conditional also on (R) of Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>. “$`F`$ is strongly convex near its own minimiser” is *not* enough: the penalised minimiser may lie outside that neighbourhood, which is the same circularity flagged for Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a>. Note also that the smoothing sharpness $`\alpha=500`$ does not appear in <a href="#eq:penbias" data-reference-type="eqref" data-reference="eq:penbias">(eq. penbias)</a> at all. This shrinkage bias must be added to any statement about $`\lVert p^\star-\cdot\rVert`$; it is the shrinkage visible in Figure <a href="#fig:coef" data-reference-type="ref" data-reference="fig:coef">15</a>.

3.  Curvature diagnostics computed with $`\gamma=0`$ are diagnostics of the *data term*, not of the optimised objective; see Figures <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a> and <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>.

4.  The blow-up replacement is outside the analysis entirely: where it fires, the loss returned is not $`\sum_i\lVert \varphi_f(\cdot;\,p,\,\cdot)-y_i\rVert^2`$ for any flow this manuscript bounds, so no statement of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> applies to that evaluation. No conclusion about $`p`$, $`P`$ or $`\partial P`$ is drawn from it; see the (A0) discussion above.

</div>

*Arms.* **GP**: sweep $`\kappa`$ upward through the FULL schedule $`\{1,2,3,4,5,6,8,10,12,\allowbreak15,20,25,33,50,75,100\}`$, each stage started from the previous minimiser. **best-GP**: as GP but started from the lowest-cost minimiser seen so far. **Control** (no propagation): every stage started from the same random seed vector. Optimiser: Nelder–Mead, 2500 iterations per stage, unless stated; BFGS/L-BFGS use the exact automatic-differentiation gradient. Seeds $`s=1,\dots,8`$: $`z_{\rm seed}=[y_0;0.1\,\xi]`$, $`\xi\sim\mathcal N(0,I_{20})`$, giving $`\lVert p_0-p^\star\rVert\in[1.50,1.84]`$. Arms are compared at equal budgets (same iterations, schedule and seeds) unless a table says otherwise. Other schedules: SHORT $`\{1,2,5,10,25,50,100\}`$ (variations), DENSE (44 stages), COARSE $`\{1,5,25,100\}`$, JUMP $`\{1,100\}`$.

**Read this:** the sweep schedules are in general *not* nested, so the stages of Algorithm <a href="#alg:gp" data-reference-type="ref" data-reference="alg:gp">Algorithm 1</a> are *not* node removals. The node set at window size $`\kappa`$ is $`S_\kappa=L_\kappa\cup\{M\}`$ with $`L_\kappa=\{j\kappa:\ j\kappa<M\}`$ the launch nodes and $`M=N-1=100`$ the common terminal node, which belongs to every partition. Hence the stage $`\kappa_a\to\kappa_b`$ is a node removal iff $`S_{\kappa_b}\subseteq S_{\kappa_a}`$, i.e. iff $`L_{\kappa_b}\subseteq L_{\kappa_a}`$, and for increasing $`\kappa_a<\kappa_b\le M`$ this holds
``` math
\begin{equation}
\text{iff}\qquad \kappa_a\mid\kappa_b\quad\text{or}\quad\kappa_b=M
\label{eq:nesting}
\end{equation}
```
(for $`\kappa_b<M`$ the element $`\kappa_b\in L_{\kappa_b}`$ forces $`\kappa_a\mid\kappa_b`$, and conversely divisibility gives inclusion; for $`\kappa_b=M`$, $`L_M=\{0\}`$ and inclusion is automatic). Counting transitions with <a href="#eq:nesting" data-reference-type="eqref" data-reference="eq:nesting">(eq. nesting)</a>: FULL $`\{1,2,3,4,5,6,8,10,12,15,20,25,33,50,75,100\}`$ has *two* of $`15`$ nested ($`1\to2`$ and $`75\to100`$); DENSE ($`44`$ stages) *two* of $`43`$ ($`1\to2`$ and $`95\to100`$); SHORT $`\{1,2,5,10,25,50,100\}`$ four of six ($`1\to2`$, $`5\to10`$, $`25\to50`$, $`50\to100`$; the failures are $`2\to5`$ and $`10\to25`$); COARSE $`\{1,5,25,100\}`$ three of three and JUMP $`\{1,100\}`$ one of one. The earlier version of this manuscript asserted that the sub-partition hypothesis “is what the algorithm does”; that assertion was false and is withdrawn, and so is a later version’s criterion “$`\kappa_a`$ divides $`\kappa_b`$” with one nested transition in FULL and one in DENSE, which omitted the $`\kappa_b=M`$ case of <a href="#eq:nesting" data-reference-type="eqref" data-reference="eq:nesting">(eq. nesting)</a>. The general case is covered by Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a>, which compares two arbitrary partitions through their common refinement.

*Read this:* the second nested transition of FULL and of DENSE is in each case the terminal $`\kappa_b=M`$ stage, i.e. the collapse to single shooting, which is a node removal only in the degenerate sense that *every* interior node is removed. The exact quantities, from the definitions of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> and Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a>:

<div class="center">

|  | FULL $`75\to100`$ | DENSE $`95\to100`$ |
|:---|:--:|:--:|
| fine node set $`S_{\kappa}=L_\kappa\cup\{M\}`$ | $`\{0,75,100\}`$ | $`\{0,95,100\}`$ |
| coarse node set | $`\{0,100\}`$ | $`\{0,100\}`$ |
| removed set $`\mathcal I_R`$ | $`\{75\}`$, $`|\mathcal I_R|=1=K-1`$ | $`\{95\}`$, $`|\mathcal I_R|=1=K-1`$ |
| $`\Delta T_1=\max_k(\tau_k-\tau_{k-1})`$ | $`\max\{75,25\}=75`$ | $`\max\{95,5\}=95`$ |
| $`\Delta T_2=\max_{k\in\mathcal I_R}w_k`$ | $`75`$ | $`95`$ |
| $`h_k`$, $`|D_k|`$ | $`25`$, $`25`$ | $`5`$, $`5`$ |
| collapsed argument $`\Delta T_1+\Delta T_2`$ | $`150`$ | $`190`$ |
| actual coarse span $`\Lambda_R=\max_{k}(h_k+w_k)`$ | $`100`$ | $`100`$ |

</div>

So $`\Delta_K`$ of <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> carries $`e^{\mu^{+}\Delta T_1}`$ times two envelopes with arguments $`75`$ and $`150`$, i.e. a leading factor $`e^{300\mu^{+}}`$ for FULL and $`e^{380\mu^{+}}`$ for DENSE. Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> therefore covers two of FULL’s fifteen stages, one of which it covers vacuously. **Read this:** two corrections to an earlier version of this paragraph, which reported $`\Delta T_2=100`$ and a factor $`e^{100\mu^{+}}`$. $`\Delta T_2`$ is the offset of the removed node to its retained left neighbour, which is $`75`$ (FULL) and $`95`$ (DENSE), not $`100`$; and the composite argument $`\Delta T_1+\Delta T_2`$ is $`150`$ and $`190`$, so the earlier factor was understated, not overstated. The *actual* elapsed time in the primary bound <a href="#eq:prop_removal_datum" data-reference-type="eqref" data-reference="eq:prop_removal_datum">(eq. prop_removal_datum)</a> never exceeds $`\Lambda_R=100`$ in either case, which is the coarse window $`\{0,100\}`$ itself — so it is covered by <a href="#eq:DTmaxdef" data-reference-type="eqref" data-reference="eq:DTmaxdef">(eq. DTmaxdef)</a> with no separate requirement, the coarse partitions being among the partitions under discussion. The gap between $`\Lambda_R=100`$ and the collapsed argument $`\Delta T_1+\Delta T_2=150`$ (FULL) or $`190`$ (DENSE) is entirely the price of collapsing $`h_k`$ and $`w_k`$ to independent maxima in Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a>. On these transitions the per-datum bound carries $`e^{\mu(t_i-75)}\le e^{25\mu^{+}}`$ and residual arguments at most $`100`$, against the collapsed $`e^{75\mu^{+}}`$ and $`\bar\rho_{150}`$.

*Metrics.* $`J_\kappa`$ at the returned minimiser (values $`\ge10^3`$ flag the plateau); the *parameter error* $`\lVert p-p^\star\rVert`$ (Euclidean, 20 coefficients; the identification metric and the primary one here); the *recovery score* in $`[0,1]`$: each coefficient earns credit 1 if it is correctly zero ($`|p_j|<0.05`$ when $`p^\star_j=0`$) or correctly non-zero within 25 % of the truth, averaged over the 20 coefficients; the *paired difference* of $`\lVert p-p^\star\rVert`$ between two arms sharing a seed, reported as the mean with a 95 % percentile bootstrap CI over 2000 resamples of the seeds (the resampling unit is the optimiser seed), with *cellwise* counts of seeds in which one arm is strictly better.

## Symbols and frozen constants

<div class="center">

| symbol | meaning | value | where frozen |
|:---|:---|:---|:---|
| $`N`$, $`\Delta t`$ | samples, spacing ($`N-1`$ residuals) | $`101`$, $`1`$ | R002 data recipe |
| $`d`$, $`n_p`$ | state dimension, parameter dimension | $`2`$, $`20`$ | §<a href="#sec:model" data-reference-type="ref" data-reference="sec:model">2.1</a> |
| $`\kappa`$, $`K`$ | window size (data intervals), number of windows | swept, $`\lceil100/\kappa\rceil`$ | R002 schedules |
| $`\Delta T`$, $`\Delta T_1`$, $`\Delta T_2`$ | longest window of the partition under discussion; of the fine partition; largest removed-node offset $`\tau_k-\tau_k^{-}`$ | swept | draft, §<a href="#sec:model" data-reference-type="ref" data-reference="sec:model">2.1</a> |
| $`\Delta T_{\max}`$, $`\Lambda_R`$ | horizon of (A0); longest affected coarse span <a href="#eq:LambdaR" data-reference-type="eqref" data-reference="eq:LambdaR">(eq. LambdaR)</a> | not instantiated | §<a href="#sec:model" data-reference-type="ref" data-reference="sec:model">2.1</a> |
| $`X`$, $`P`$, $`\mathcal T`$, $`r_X`$, $`r'`$ | state region, parameter set, tube, tube radius, noise radius (A0)–(A1) | not instantiated | §<a href="#sec:model" data-reference-type="ref" data-reference="sec:model">2.1</a> |
| $`E_X`$ | admissible event $`\{\max_i\lVert \eta_i\rVert\le r'\}`$ <a href="#eq:EX" data-reference-type="eqref" data-reference="eq:EX">(eq. EX)</a> | not instantiated | §<a href="#sec:model" data-reference-type="ref" data-reference="sec:model">2.1</a> |
| $`S`$, $`\delta t`$ | integrator sub-steps per interval, step | $`10`$, $`0.1`$ | R002 driver |
| $`\gamma`$, $`\alpha`$ | sparsity weight, smooth-$`\ell_1`$ sharpness | $`0.05`$, $`500`$ | R002 driver |
| $`\lVert \eta\rVert_{\max}`$, $`\Sigma`$ | largest noise norm; noise covariance (anisotropic) | $`0.159`$ | R002 `fhn_data_meta` |
| $`L`$ | Lipschitz constant in $`x`$ along the orbit / on $`[-2.5,2.5]\times[-1,2]`$ | $`3.05`$ / $`5.34`$ | R002 `concept_meta` |
| $`\mu`$, $`\mu_-`$, $`\mu^{+}`$ | logarithmic norm along the orbit, lower counterpart, $`\max(\mu,0)`$ | $`1.17`$, $`-2.96`$ | R002 / R003 |
| $`\tilde L`$ | Lipschitz constant in $`p`$ along the orbit | $`10.56`$ | R002 `concept_meta` |
| $`Q(s)`$, $`\rho_s`$, $`\bar\rho_s`$, $`\rho^{+}_s`$, $`r_{\sigma,t}`$ | parameter-sensitivity factor <a href="#eq:Qdef" data-reference-type="eqref" data-reference="eq:Qdef">(eq. Qdef)</a>; residual bound, its monotone envelope, its smooth majorant, its per-datum form <a href="#eq:rdatum" data-reference-type="eqref" data-reference="eq:rdatum">(eq. rdatum)</a> | symbolic | §<a href="#sec:consts" data-reference-type="ref" data-reference="sec:consts">2.2</a> |
| $`\Lambda_k`$, $`M_k`$ | window endpoint exponent; window transition bound <a href="#eq:Mk" data-reference-type="eqref" data-reference="eq:Mk">(eq. Mk)</a> | Table <a href="#tab:windows" data-reference-type="ref" data-reference="tab:windows">2</a> | R003 |
| $`D`$, $`\mathrm{cond}(D)`$ | weighting $`\mathrm{diag}(1,s)`$, $`s=\sqrt{|J_{12}|/|J_{21}|}`$; its condition number | $`s=3.54`$ | R003 |
| $`m`$ | strong-convexity constant near a minimiser | assumed | draft (as “$`\mu`$”) |
| blow-up threshold | $`|x|>10^3`$ or NaN | $`10^3`$ | R002 loss |

</div>

**Read this:** $`L`$ and $`\mu`$ are measured along the true orbit at $`p^\star`$ over R002’s probe box, not assumed — and not certified as the suprema over $`X\times P`$ that (A0) requires, since $`X\times P`$ has never been instantiated (premise check P1, §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>). The numerical section shows that the amplification of an initial-state error on the FHN limit cycle never exceeds $`8.5`$; bounds carrying $`e^{L\Delta T}`$ are loose by orders of magnitude for $`\Delta T\gtrsim2`$, bounds carrying $`e^{\mu\Delta T}`$ by much less, and the linearised per-window endpoint exponents of §<a href="#sec:refine" data-reference-type="ref" data-reference="sec:refine">5</a> are close to the measurement.

# Convergence analysis of multiple shooting

Throughout, $`f`$ satisfies (A0) and the constants of §<a href="#sec:consts" data-reference-type="ref" data-reference="sec:consts">2.2</a> are finite on $`X\times P`$. Every statement opens with its scope tag (S1)–(S4) of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>. Statements marked *(revised)* differ from the earlier draft; each is followed by the change and its effect.

## Sensitivity of the flow to the initial state and to the parameters

<div id="def:Tx" class="definition">

**Definition 1** (confinement time of a pair). *For $`x_1,x_2\in X`$ and $`p_1,p_2\in P`$ write $`\xi_j(s)=\varphi_f(s;\,p_j,\,x_j)`$ and let $`[u,v]=\{u+\theta(v-u):\theta\in[0,1]\}`$. Set
``` math
T_X\big((x_1,p_1),(x_2,p_2)\big)
=\sup\Big\{t\in[0,\Delta T_{\max}]:\ \xi_1,\xi_2\ \text{exist on }[0,t]\ \text{and}\
[\xi_1(s),\xi_2(s)]\subset X\ \ \forall s\in[0,t]\Big\}.
```*

</div>

Three remarks. The chord contains its endpoints, so the displayed condition already requires $`\xi_1(s),\xi_2(s)\in X`$. $`T_X\ge0`$ always, since $`x_1,x_2\in X`$ and $`X`$ is convex. And the defining set is closed, so the condition holds on all of $`[0,T_X]`$: by (A0) each $`\xi_j`$ exists while it remains in the compact set $`X`$, hence extends continuously to $`T_X`$ with $`\xi_j(T_X)\in X`$, and $`X`$ closed and convex gives $`[\xi_1(T_X),\xi_2(T_X)]\subset X`$.

<div id="lem:state" class="lemma">

**Lemma 5** (flow sensitivity to the initial state, local; *revised*). **Scope (S1).* Assume (A0). Let $`p\in P`$, $`x_1,x_2\in X`$, and let $`T=T_X((x_1,p),(x_2,p))`$. Then for every $`t\in[0,T]`$,
``` math
\begin{equation}
e^{\mu_- t}\lVert x_1-x_2\rVert\ \le\ \lVert \varphi_f(t;\,p,\,x_1)-\varphi_f(t;\,p,\,x_2)\rVert\ \le\ e^{\mu t}\lVert x_1-x_2\rVert.
\label{eq:lemma1}
\end{equation}
```
In particular, since $`\mu\le L`$ and $`\mu_-\ge-L`$, the draft’s bounds $`e^{-Lt}\lVert x_1-x_2\rVert\le\lVert \cdot\rVert\le e^{Lt}\lVert x_1-x_2\rVert`$ hold on $`[0,T]`$.*

</div>

<div class="proof">

*Proof.* Let $`\hat x_j(t)=\varphi_f(t;\,p,\,x_j)`$ and $`\delta=\hat x_1-\hat x_2`$, so $`\dot\delta=f(\hat x_1;p)-f(\hat x_2;p)`$ with $`\delta(0)=x_1-x_2`$. For $`t<T`$ the chord $`\hat x_2(t)+s\delta(t)`$, $`s\in[0,1]`$, lies in $`X`$ by Definition <a href="#def:Tx" data-reference-type="ref" data-reference="def:Tx">1</a>, so the mean-value form $`f(\hat x_1;p)-f(\hat x_2;p)=A(t)\delta`$ with $`A(t)=\int_0^1J_f(\hat x_2+s\delta;p)\,\mathrm{d}s`$ is valid and every $`J_f`$ in the integrand is evaluated at a point of $`X`$; hence
``` math
\begin{equation}
\tfrac12\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\lVert \delta\rVert^2=\langle\delta,A(t)\delta\rangle
 =\big\langle\delta,\tfrac12(A+A^\top)\delta\big\rangle\in\big[\mu_-\lVert \delta\rVert^2,\ \mu\lVert \delta\rVert^2\big]
\label{eq:proof1}
\end{equation}
```
on $`[0,T)`$. If $`x_1=x_2`$ both sides of <a href="#eq:lemma1" data-reference-type="eqref" data-reference="eq:lemma1">(eq. lemma1)</a> vanish. If $`x_1\ne x_2`$ then $`\delta(t)\ne0`$ on $`[0,T)`$: two solutions of the same initial-value problem with the same parameter that agree at one time agree at all times by uniqueness (guaranteed by (A0)), so $`\delta(t_\ast)=0`$ would force $`\delta\equiv0`$ and contradict $`\delta(0)\ne0`$. On $`\{\delta\neq0\}`$, $`\lVert \delta\rVert`$ is differentiable and <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> gives $`\mu_-\lVert \delta\rVert\le\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\lVert \delta\rVert\le\mu\lVert \delta\rVert`$; Grönwall in both directions gives <a href="#eq:lemma1" data-reference-type="eqref" data-reference="eq:lemma1">(eq. lemma1)</a> on $`[0,T)`$, and both sides are continuous at $`t=T`$. ◻

</div>

**Read this:** the quantifier. This lemma is *not* asserted for all $`t\le\Delta T_{\max}`$: under (A0) alone a trajectory launched in $`X`$ may leave $`X`$, after which the mean-value matrix integrates $`J_f`$ over a chord outside $`X`$ and $`\lambda_{\max}(\operatorname{sym}A)\le\mu`$ has no justification — and for a polynomial $`f`$ the solution may cease to exist. A previous revision stated <a href="#eq:lemma1" data-reference-type="eqref" data-reference="eq:lemma1">(eq. lemma1)</a> for all $`x_1,x_2\in X`$ and all $`t\le\Delta T_{\max}`$ while simultaneously removing forward invariance from (A0), which made the statement false and the proof of Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> circular. Full-window versions are Corollary <a href="#cor:confined" data-reference-type="ref" data-reference="cor:confined">1</a>, obtained *after* Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a>. *What changed.* The draft bounded $`\lVert \dot\delta\rVert\le L\lVert \delta\rVert`$ and integrated $`\tfrac{\,\mathrm{d}}{\,\mathrm{d}t}\ln\lVert \delta\rVert`$. That step applies Cauchy–Schwarz to $`\langle\delta,\dot\delta\rangle`$ and discards the sign information in the inner product; <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> keeps it. On FHN the Jacobian is $`\begin{psmallmatrix}1-v^2&-1\\0.08&-0.064\end{psmallmatrix}`$ and $`1-v^2\le1`$ for every $`v`$, so $`\mu=1.17`$ on all of $`\mathbb R^2`$ at $`p^\star`$ (attained at $`v=0`$), whereas $`L`$ grows like $`v^2`$ and is $`3.05`$ on the orbit but $`5.34`$ on the box. At $`t=\Delta T=2`$ the bound falls from $`e^{L\Delta T}=447`$ to $`e^{\mu\Delta T}=10.4`$ against a measured peak of $`6.9`$ (Table <a href="#tab:lemmas" data-reference-type="ref" data-reference="tab:lemmas">1</a>).

<div id="lem:param" class="lemma">

**Lemma 6** (flow sensitivity to the parameters, local; *revised*). **Scope (S1).* Assume (A0). Let $`x_0\in X`$, $`p_1,p_2\in P`$, and let $`T=T_X((x_0,p_1),(x_0,p_2))`$. Then for every $`t\in[0,T]`$,
``` math
\begin{equation}
\lVert \varphi_f(t;\,p_1,\,x_0)-\varphi_f(t;\,p_2,\,x_0)\rVert\ \le\ Q(t)\,\lVert p_1-p_2\rVert,
\qquad Q(t)=\frac{\tilde L}{\mu}\big(e^{\mu t}-1\big)
\label{eq:lemma2}
\end{equation}
```
with the conventions of <a href="#eq:Qdef" data-reference-type="eqref" data-reference="eq:Qdef">(eq. Qdef)</a>.*

</div>

<div class="proof">

*Proof.* Let $`\hat x_j(t)=\varphi_f(t;\,p_j,\,x_0)`$ and $`\delta=\hat x_1-\hat x_2`$, so $`\delta(0)=0`$ and
``` math
\dot\delta=\underbrace{\big[f(\hat x_1;p_1)-f(\hat x_2;p_1)\big]}_{\text{state difference, equal parameter}}
+\underbrace{\big[f(\hat x_2;p_1)-f(\hat x_2;p_2)\big]}_{\text{parameter difference, equal state}} .
```
For $`t<T`$ the chord $`[\hat x_1(t),\hat x_2(t)]`$ lies in $`X`$ by Definition <a href="#def:Tx" data-reference-type="ref" data-reference="def:Tx">1</a>, so the first bracket equals $`A(t)\delta`$ with $`A`$ as in <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> and $`\lambda_{\max}(\operatorname{sym}A)\le\mu`$; and $`\hat x_2(t)\in X`$, $`p_1,p_2\in P`$, so the second bracket has norm at most $`\tilde L\lVert p_1-p_2\rVert`$ by the definition of $`\tilde L`$ as a supremum over $`X\times P`$.

*The differential inequality, including at the zeros of $`\delta`$.* Here $`\delta(0)=0`$ and, unlike in Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a>, uniqueness does *not* exclude later zeros: two trajectories with different parameters may meet at $`t>0`$ without coinciding. We therefore work with the upper right Dini derivative $`D^{+}\phi(t)=\limsup_{h\downarrow0}\big(\phi(t+h)-\phi(t)\big)/h`$ of $`\phi=\lVert \delta\rVert`$.

- If $`\delta(t)\ne0`$ then $`\phi`$ is differentiable at $`t`$ and $`D^{+}\phi=\dot\phi=\langle\delta,\dot\delta\rangle/\lVert \delta\rVert\le\mu\lVert \delta\rVert+\tilde L\lVert p_1-p_2\rVert`$ by <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> and Cauchy–Schwarz on the second bracket.

- If $`\delta(t)=0`$ then $`\hat x_1(t)=\hat x_2(t)=:\bar x`$ and $`\phi(t+h)=\lVert h\dot\delta(t)+o(h)\rVert`$, so $`D^{+}\phi(t)=\lVert \dot\delta(t)\rVert=\lVert f(\bar x;p_1)-f(\bar x;p_2)\rVert\le\tilde L\lVert p_1-p_2\rVert=\mu\,\phi(t)+\tilde L\lVert p_1-p_2\rVert`$.

Hence $`D^{+}\lVert \delta\rVert\le\mu\lVert \delta\rVert+\tilde L\lVert p_1-p_2\rVert`$ on $`[0,T)`$. The comparison lemma for Dini derivatives (any continuous $`\phi`$ with $`D^{+}\phi\le a\phi+b`$ and $`\phi(0)=\phi_0`$ satisfies $`\phi(t)\le e^{at}\phi_0+\tfrac{b}{a}(e^{at}-1)`$) with $`\phi_0=0`$ gives <a href="#eq:lemma2" data-reference-type="eqref" data-reference="eq:lemma2">(eq. lemma2)</a> on $`[0,T)`$; both sides are continuous at $`t=T`$. ◻

</div>

*Alternative (regularisation).* With $`\phi_\varepsilon=(\lVert \delta\rVert^2+\varepsilon^2)^{1/2}\ge\varepsilon>0`$ one has $`\dot\phi_\varepsilon=\langle\delta,\dot\delta\rangle/\phi_\varepsilon\le\mu\lVert \delta\rVert^2/\phi_\varepsilon+\tilde L\lVert p_1-p_2\rVert`$, and since $`\lVert \delta\rVert^2/\phi_\varepsilon=\phi_\varepsilon-\varepsilon^2/\phi_\varepsilon\in[\phi_\varepsilon-\varepsilon,\phi_\varepsilon]`$, $`\dot\phi_\varepsilon\le\mu\phi_\varepsilon+\tilde L\lVert p_1-p_2\rVert+|\mu|\varepsilon`$; Grönwall and $`\varepsilon\downarrow0`$ give the same conclusion. (The remainder $`|\mu|\varepsilon`$ is needed only when $`\mu<0`$.) *What changed.* The first bracket’s bound, $`L\to\mu`$, as in Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a>; the removal of the division by $`\lVert \delta\rVert`$ at $`\delta=0`$, which the draft’s argument performed silently at $`t=0`$; and the restriction to $`t\le T_X`$. The consequence of $`L\to\mu`$ is qualitative: for a dissipative system ($`\mu<0`$) the revised bound *saturates* at $`\tilde L/|\mu|`$, which is the shape the measurement has (Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a>b), whereas $`\frac{\tilde L}{L}(e^{Lt}-1)`$ can never saturate because a Lipschitz constant is non-negative.

<div id="lem:noescape" class="lemma">

**Lemma 7** (no escape from the tube). **Scope (S2).* Assume (A0)–(A1). Let $`\tau\in[t_0,t_{N-1}]`$, $`x_\tau=x^\star(\tau)`$, let $`z\in\mathbb R^{d}`$ with $`\lVert z-x_\tau\rVert\le r'`$, and put $`S=\min\{\Delta T_{\max},\,t_{N-1}-\tau\}`$. Then for every $`p\in P`$ and every $`s\in[0,S]`$,
``` math
\varphi_f(s;\,p,\,x_\tau)\in\mathcal T\subseteq X
\qquad\text{and}\qquad
\varphi_f(s;\,p,\,z)\in\mathcal T\subseteq X .
```*

</div>

<div class="proof">

*Proof.* Two exit-time bootstraps; the first is needed to license the second. Both use only Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a>, and only at times at which the relevant chord has already been placed in $`X`$.

*Step 1 (clean launch).* Let $`T=\sup\{t\in[0,S]:\ \varphi_f(s;\,p,\,x_\tau)\in\mathcal T\ \forall s\le t\}`$; $`T\ge0`$ since $`x_\tau\in\mathcal T`$. Fix $`s<T`$. Then $`\varphi_f(s;\,p,\,x_\tau)\in\mathcal T\subseteq X`$, and $`x^\star(\tau+s)=\varphi_f(s;\,p^\star,\,x_\tau)\in\mathcal T\subseteq X`$ because it lies on the orbit; $`X`$ is convex, so the chord between them lies in $`X`$. Hence $`T\le T_X((x_\tau,p),(x_\tau,p^\star))`$ and Lemma <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> applies on $`[0,T]`$, giving
``` math
\begin{equation}
\begin{aligned}
\operatorname{dist}\big(\varphi_f(s;\,p,\,x_\tau),\,x^\star([t_0,t_{N-1}])\big)
&\le\lVert \varphi_f(s;\,p,\,x_\tau)-x^\star(\tau+s)\rVert\\
&\le Q(s)\lVert p-p^\star\rVert\ \le\ Q(\Delta T_{\max})\operatorname{diam}(P)\ <\ r_X
\end{aligned}
\label{eq:clean_conf}
\end{equation}
```
for all $`s\le T`$, the last inequality being *strict* by <a href="#eq:closure" data-reference-type="eqref" data-reference="eq:closure">(eq. closure)</a> (which subtracts a positive $`e^{\mu^{+}\Delta T_{\max}}r'`$). Suppose $`T<S`$. The trajectory remains in the compact set $`\mathcal T\subseteq X`$ on $`[0,T)`$, so by (A0) it extends to $`T`$ with $`\varphi_f(T;\,p,\,x_\tau)\in X`$, and <a href="#eq:clean_conf" data-reference-type="eqref" data-reference="eq:clean_conf">(eq. clean_conf)</a> holds at $`s=T`$ with strict inequality; by continuity of $`s\mapsto\operatorname{dist}(\varphi_f(s;\,p,\,x_\tau),x^\star([t_0,t_{N-1}]))`$ there is $`\varepsilon>0`$ with the distance $`<r_X`$, hence the trajectory in $`\mathcal T`$, on $`[0,T+\varepsilon]\cap[0,S]`$ — contradicting the definition of $`T`$ as a supremum. Therefore $`T=S`$, and <a href="#eq:clean_conf" data-reference-type="eqref" data-reference="eq:clean_conf">(eq. clean_conf)</a> holds on $`[0,S]`$.

*Step 2 (perturbed launch).* Let $`T'=\sup\{t\in[0,S]:\ \varphi_f(s;\,p,\,z)\in\mathcal T\ \forall s\le t\}`$; $`T'\ge0`$ since $`\lVert z-x_\tau\rVert\le r'\le r_X`$. Fix $`s<T'`$. Then $`\varphi_f(s;\,p,\,z)\in\mathcal T\subseteq X`$ and, by Step 1, $`\varphi_f(s;\,p,\,x_\tau)\in\mathcal T\subseteq X`$; $`X`$ convex puts the chord in $`X`$, so $`T'\le T_X((z,p),(x_\tau,p))`$ and Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> applies on $`[0,T']`$:
``` math
\lVert \varphi_f(s;\,p,\,z)-\varphi_f(s;\,p,\,x_\tau)\rVert\le e^{\mu s}\lVert z-x_\tau\rVert\le e^{\mu^{+}\Delta T_{\max}}r' ,
```
using $`e^{\mu s}\le e^{\mu^{+}\Delta T_{\max}}`$ for both signs of $`\mu`$. With <a href="#eq:clean_conf" data-reference-type="eqref" data-reference="eq:clean_conf">(eq. clean_conf)</a>,
``` math
\operatorname{dist}\big(\varphi_f(s;\,p,\,z),\,x^\star([t_0,t_{N-1}])\big)
\ \le\ e^{\mu^{+}\Delta T_{\max}}r'+Q(\Delta T_{\max})\operatorname{diam}(P)\ <\ r_X
```
by <a href="#eq:closure" data-reference-type="eqref" data-reference="eq:closure">(eq. closure)</a>, again *strictly*. The same extension-and-continuity argument as in Step 1 gives $`T'=S`$. ◻

</div>

<div id="cor:confined" class="corollary">

**Corollary 1** (full-window estimates for admissible launches). **Scope (S2).* Assume (A0)–(A1). Let $`\tau\in[t_0,t_{N-1}]`$, $`S=\min\{\Delta T_{\max},t_{N-1}-\tau\}`$, and let $`z,z_1,z_2`$ satisfy $`\lVert \cdot-x^\star(\tau)\rVert\le r'`$. Then for all $`p,p_1,p_2\in P`$ and all $`s\in[0,S]`$: all the trajectories below lie in $`\mathcal T\subseteq X`$, every chord between two of them lies in $`X`$, and
``` math
\begin{align}
e^{\mu_- s}\lVert z_1-z_2\rVert\ \le\ \lVert \varphi_f(s;\,p,\,z_1)-\varphi_f(s;\,p,\,z_2)\rVert\ &\le\ e^{\mu s}\lVert z_1-z_2\rVert,
\label{eq:conf_state}\\
\lVert \varphi_f(s;\,p_1,\,z)-\varphi_f(s;\,p_2,\,z)\rVert\ &\le\ Q(s)\lVert p_1-p_2\rVert.
\label{eq:conf_param}
\end{align}
```
If moreover $`\sigma+s\le S`$, then $`\varphi_f(s;\,p,\,\varphi_f(\sigma;\,p,\,z))=\varphi_f(\sigma+s;\,p,\,z)`$ is one of the trajectories already confined above, so <a href="#eq:conf_state" data-reference-type="eqref" data-reference="eq:conf_state">(eq. conf_state)</a> applies to any *pair* of such same-parameter continuations. No statement is made about <a href="#eq:conf_param" data-reference-type="eqref" data-reference="eq:conf_param">(eq. conf_param)</a> for launches from a reachable state.*

</div>

<div class="proof">

*Proof.* Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> places each trajectory in $`\mathcal T\subseteq X`$ for all $`s\le S`$; $`X`$ is convex, so every chord lies in $`X`$; hence $`T_X=S`$ for each pair, and Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> apply on the whole of $`[0,S]`$. The final sentence is the flow property together with the confinement already established: the continuation is the same trajectory, re-based. ◻

</div>

**Read this:** §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> cite *this corollary*, never Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a>/<a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> directly; the lemmas are local and their hypothesis $`t\le T_X`$ is discharged once and for all here. An earlier version of this corollary added a blanket clause asserting that all conclusions — including the parameter-sensitivity estimate <a href="#eq:conf_param" data-reference-type="eqref" data-reference="eq:conf_param">(eq. conf_param)</a> — survive replacing $`z`$ by any reachable state $`\varphi_f(\sigma;\,p_0,\,z)`$. That is *false as stated*: the flow property confines a continuation under the *same* parameter, but $`\varphi_f(s;\,p_1,\,\varphi_f(\sigma;\,p_0,\,z))`$ and $`\varphi_f(s;\,p_2,\,\varphi_f(\sigma;\,p_0,\,z))`$ for $`p_1\ne p_2`$ are launches from a state that Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> places within $`r_X`$ — not within $`r'`$ — of the orbit, which is not a hypothesis this corollary has. The clause is withdrawn; Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> does not need it, and argues directly instead (see its proof).

<div id="lem:residual" class="lemma">

**Lemma 8** (largest residual of a window; *revised*). **Scope (S3).* Assume (A0)–(A2) and work on the event $`E_X`$ of <a href="#eq:EX" data-reference-type="eqref" data-reference="eq:EX">(eq. EX)</a>. Let $`\tau`$ be a node, let $`s\in(0,\Delta T_{\max}]`$ and let $`t_i\in(\tau,\tau+s]`$. Then, with $`\sigma_i=t_i-\tau`$ and every $`p\in P`$,
``` math
\begin{equation}
\lVert \varphi_f(\sigma_i;\,p,\,y_\tau)-y_i\rVert\ \le\ r_{\tau,t_i}(p)
\ =\ e^{\mu\sigma_i}\lVert \eta_\tau\rVert+Q(\sigma_i)\lVert p-p^\star\rVert+\lVert \eta_i\rVert
\ \le\ \rho_{\sigma_i}(p)\ \le\ \bar\rho_{s}(p).
\label{eq:residual}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* On $`E_X`$, $`\lVert \eta_\tau\rVert\le r'`$, so Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> places $`\varphi_f(r;\,p,\,y_\tau)`$, $`\varphi_f(r;\,p,\,x_\tau)`$ and $`x^\star(\tau+r)`$ in $`\mathcal T\subseteq X`$ for all $`r\le\sigma_i`$, and all chords in $`X`$ by convexity; Corollary <a href="#cor:confined" data-reference-type="ref" data-reference="cor:confined">1</a> therefore applies to every step below. Split $`\varphi_f(\sigma_i;\,p,\,y_\tau)-y_i=[\varphi_f(\sigma_i;\,p,\,y_\tau)-\varphi_f(\sigma_i;\,p,\,x_\tau)]+[\varphi_f(\sigma_i;\,p,\,x_\tau)-x_i]-\eta_i`$. Estimate <a href="#eq:conf_state" data-reference-type="eqref" data-reference="eq:conf_state">(eq. conf_state)</a> bounds the first bracket by $`e^{\mu\sigma_i}\lVert \eta_\tau\rVert`$; since $`x_i=\varphi_f(\sigma_i;\,p^\star,\,x_\tau)`$, <a href="#eq:conf_param" data-reference-type="eqref" data-reference="eq:conf_param">(eq. conf_param)</a> bounds the second by $`Q(\sigma_i)\lVert p-p^\star\rVert`$. This is $`r_{\tau,t_i}(p)`$ of <a href="#eq:rdatum" data-reference-type="eqref" data-reference="eq:rdatum">(eq. rdatum)</a>; with $`\lVert \eta_\tau\rVert,\lVert \eta_i\rVert\le\lVert \eta\rVert_{\max}`$ it is at most $`\rho_{\sigma_i}(p)`$, and $`\rho_{\sigma_i}\le\bar\rho_s`$ by Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(i). ◻

</div>

**Read this:** the last step is *not* monotonicity of $`\rho`$, which fails for $`\mu<0`$; it is Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(i), i.e. the definition of the envelope $`\bar\rho`$.

## The windowed cost near the truth

<div id="prop:cost" class="proposition">

**Proposition 1** (cost at the true parameter; *revised*). **Scope (S3) for (a) and (c), (S4) for (b).* Assume (A0)–(A2). Consider a partition with longest window $`\Delta T\le\Delta T_{\max}`$ and $`n_k`$ data in window $`k`$, $`\sum_k n_k=N-1`$.*

**(a) Realisation-wise, on $`E_X`$:* for every $`p\in P`$,
``` math
\begin{align}
J_K(p)&\le\sum_{k=1}^{K}\sum_{t_i\in(\tau_{k-1},\tau_k]}
 \Big(e^{\mu(t_i-\tau_{k-1})}\lVert \eta_{\tau_{k-1}}\rVert
 +Q(t_i-\tau_{k-1})\lVert p-p^\star\rVert+\lVert \eta_i\rVert\Big)^2,\label{eq:JK_bound}\\
J_K(p^\star)&\le\lVert \eta\rVert_{\max}^2\sum_{k=1}^{K}\sum_{j=1}^{n_k}\big(1+e^{\mu j\Delta t}\big)^2
 \ \le\ (N-1)\,\lVert \eta\rVert_{\max}^2\big(1+e^{\mu^{+}\Delta T}\big)^2 .\label{eq:JK_true}
\end{align}
```
Moreover, with $`\beta:=Q(\Delta T)\operatorname{diam}(P)`$,
``` math
\begin{equation}
\sup_{p\in P}\big|J_K(p)-J_K^\star(p)\big|\ \le\
(N-1)\big(1+e^{\mu^{+}\Delta T}\big)\lVert \eta\rVert_{\max}
\Big(2\beta+\big(1+e^{\mu^{+}\Delta T}\big)\lVert \eta\rVert_{\max}\Big),
\label{eq:JK_unif}
\end{equation}
```
so $`J_K\to J_K^\star`$ uniformly on $`P`$ as $`\lVert \eta\rVert_{\max}\to0`$, and $`J_K^\star(p^\star)=0`$.*

**(b) In expectation.* Under (A2-a),
``` math
\begin{equation}
\mathbb E\,J_K(p^\star)\le\operatorname{tr}\Sigma\sum_{k=1}^{K}\sum_{j=1}^{n_k}\big(1+e^{2\mu j\Delta t}\big)
 \ \le\ (N-1)\operatorname{tr}\Sigma\big(1+e^{2\mu^{+}\Delta T}\big).
\label{eq:JK_expect}
\end{equation}
```
Under (A2-b) the same two bounds hold for $`\mathbb E\big[J_K(p^\star)\mid E_X\big]`$, and $`\mathbb P(E_X^{c})`$ is bounded by <a href="#eq:tail" data-reference-type="eqref" data-reference="eq:tail">(eq. tail)</a>. No unconditional expectation is claimed under (A2-b).*

**(c) Single shooting.* On $`E_X`$, $`J_1(p)\le\sum_{i=1}^{N-1}\big(e^{\mu(t_i-t_0)}\lVert \eta_0\rVert+Q(t_i-t_0)\lVert p-p^\star\rVert+\lVert \eta_i\rVert\big)^2`$, in which the initial-condition error is amplified over the whole record.*

</div>

<div class="proof">

*Proof.* *(a)* <a href="#eq:JK_bound" data-reference-type="eqref" data-reference="eq:JK_bound">(eq. JK_bound)</a> is <a href="#eq:residual" data-reference-type="eqref" data-reference="eq:residual">(eq. residual)</a> squared and summed. At $`p^\star`$ the middle term vanishes and the $`j`$-th datum of a window sits at $`t_i-\tau_{k-1}=j\Delta t`$, giving the first inequality of <a href="#eq:JK_true" data-reference-type="eqref" data-reference="eq:JK_true">(eq. JK_true)</a>; for the second, $`e^{\mu j\Delta t}\le e^{\mu^{+}\Delta T}`$ in both cases ($`\mu\ge0`$: $`\mu j\Delta t\le\mu\Delta T`$; $`\mu<0`$: $`e^{\mu j\Delta t}\le1=e^{\mu^{+}\Delta T}`$), and $`\sum_k n_k=N-1`$. For <a href="#eq:JK_unif" data-reference-type="eqref" data-reference="eq:JK_unif">(eq. JK_unif)</a>, write $`a=\lVert \varphi_f(\sigma;\,p,\,y_\tau)-y_i\rVert`$ and $`b=\lVert \varphi_f(\sigma;\,p,\,x_\tau)-x_i\rVert`$ for the same index; then $`|a^2-b^2|\le|a-b|(a+b)`$, $`|a-b|\le e^{\mu\sigma}\lVert \eta_\tau\rVert+\lVert \eta_i\rVert\le(1+e^{\mu^{+}\Delta T})\lVert \eta\rVert_{\max}`$ and $`b\le Q(\sigma)\lVert p-p^\star\rVert\le\beta`$ by <a href="#eq:conf_param" data-reference-type="eqref" data-reference="eq:conf_param">(eq. conf_param)</a>, so $`a+b\le2\beta+(1+e^{\mu^{+}\Delta T})\lVert \eta\rVert_{\max}`$; sum over the $`N-1`$ indices. The bound is uniform in $`p\in P`$ and vanishes linearly in $`\lVert \eta\rVert_{\max}`$.

*(b)* Expand $`\lVert u-\eta_i\rVert^2=\lVert u\rVert^2-2\langle u,\eta_i\rangle+\lVert \eta_i\rVert^2`$ with $`u=\varphi_f(t_i-\tau_{k-1};\,p^\star,\,y_{\tau_{k-1}})-x_i`$, a function of $`\eta_{\tau_{k-1}}`$ alone. Since $`t_i\in(\tau_{k-1},\tau_k]`$ we have $`t_i\neq\tau_{k-1}`$, so $`\eta_i`$ and $`\eta_{\tau_{k-1}}`$ are distinct members of the i.i.d. family; independence and $`\mathbb E\eta_i=0`$ kill the cross term *termwise* (unaffected by $`\eta_{\tau_k}`$ appearing both as a residual noise in window $`k`$ and as a launch noise in window $`k+1`$: those are different summands). Then $`\mathbb E\lVert \eta_i\rVert^2=\operatorname{tr}\Sigma`$ and $`\mathbb E\lVert u\rVert^2\le e^{2\mu j\Delta t}\mathbb E\lVert \eta_{\tau_{k-1}}\rVert^2`$ by <a href="#eq:conf_state" data-reference-type="eqref" data-reference="eq:conf_state">(eq. conf_state)</a>, which under (A2-a) applies surely because $`\mathbb P(E_X)=1`$. Under (A2-b) run the identical computation under $`\mathbb P(\cdot\mid E_X)`$: by Lemma <a href="#lem:trunc" data-reference-type="ref" data-reference="lem:trunc">1</a> the $`\eta_i`$ are still independent and mean zero, so the cross term still vanishes, and $`\mathbb E[\lVert \eta_i\rVert^2\mid E_X]\le\operatorname{tr}\Sigma`$ gives the same right-hand side. ◻

</div>

*What changed.* (i) $`L\to\mu`$. (ii) The draft applied $`e^{L\Delta T}`$ to every datum in a window; <a href="#eq:JK_true" data-reference-type="eqref" data-reference="eq:JK_true">(eq. JK_true)</a> keeps the offset $`j\Delta t`$, and the geometric sum $`\sum_{j\le n_k}e^{2\mu j\Delta t}`$ saves a factor of order $`\mu\Delta T`$ relative to $`n_ke^{2\mu\Delta T}`$ when $`\mu>0`$. (iii) $`\lVert \eta\rVert_{\max}\to\operatorname{tr}\Sigma`$ in expectation. On the FHN data the realised ratio $`\lVert \eta\rVert_{\max}^2/\big(\tfrac1{N-1}\sum_i\lVert \eta_i\rVert^2\big)`$ is $`4.9`$ (a direct measurement from the residual array; the isotropic Gaussian heuristic $`1+2\log N/d`$ gives $`5.6`$, a different quantity that the data happen to sit near and that does not apply to the anisotropic $`\Sigma`$). (iv) $`\mu^{+}`$ and $`N-1`$ in the collapsed bounds: for $`\mu\le0`$ they read $`4(N-1)\lVert \eta\rVert_{\max}^2`$ and $`2(N-1)\operatorname{tr}\Sigma`$, whereas the draft’s $`e^{\mu\Delta T}`$ form reversed the inequality. (v) The scope is now explicit: (a) is pathwise on $`E_X`$, (b) is unconditional only under bounded noise. (vi) The draft’s “as $`\Sigma\to0`$” has been replaced by the deterministic limit $`\lVert \eta\rVert_{\max}\to0`$ with the explicit rate <a href="#eq:JK_unif" data-reference-type="eqref" data-reference="eq:JK_unif">(eq. JK_unif)</a>; a covariance does not index a pathwise sequence without a coupling.

## Bound on the error $`\lVert p^\star-p^{(K)}\rVert`$

<div id="prop:perr" class="proposition">

**Proposition 2** (displacement of the minimiser from the truth). **Scope (S3) for <a href="#eq:perr" data-reference-type="eqref" data-reference="eq:perr">(eq. perr)</a>, (S4) for the expectation form.* Assume (A0)–(A2) and $`p^{(K)}\in\arg\min_{P}J_K`$ as in <a href="#eq:minimisers" data-reference-type="eqref" data-reference="eq:minimisers">(eq. minimisers)</a>. Suppose $`J_K`$ is $`m`$-strongly convex, $`m>0`$, on a convex $`U\subseteq P`$ containing $`p^{(K)}`$ *and* $`p^\star`$. Then, on $`E_X`$, realisation by realisation,
``` math
\begin{equation}
\lVert p^\star-p^{(K)}\rVert\ \le\ \sqrt{\frac2m\Big(J_K(p^\star)-J_K(p^{(K)})\Big)}
\ \le\ \sqrt{\frac2m\Big((N-1)\lVert \eta\rVert_{\max}^2\big(1+e^{\mu^{+}\Delta T}\big)^2-J_K(p^{(K)})\Big)}.
\label{eq:perr}
\end{equation}
```
If in addition (A3) holds, then, under (A2-a), $`\mathbb E\lVert p^\star-p^{(K)}\rVert^2\le\tfrac2m\big((N-1)\operatorname{tr}\Sigma(1+e^{2\mu^{+}\Delta T})-\mathbb E J_K(p^{(K)})\big)`$, and under (A2-b) the same bound holds for $`\mathbb E\big[\lVert p^\star-p^{(K)}\rVert^2\mid E_X\big]`$ with $`\mathbb E[J_K(p^{(K)})\mid E_X]`$ on the right. For single shooting, $`\lVert p^\star-p^{(1)}\rVert\le\sqrt{\tfrac2m\big(\sum_{i=1}^{N-1}(e^{\mu(t_i-t_0)}\lVert \eta_0\rVert+\lVert \eta_i\rVert)^2-J_1(p^{(1)})\big)}`$.*

</div>

<div class="proof">

*Proof.* Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a> with $`F=J_K`$, $`p=p^{(K)}`$, $`q=p^\star\in U`$ gives the first bound; Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>(a) gives the second. Under (A3) the pointwise inequality holds on a set of probability one (respectively, of full conditional probability given $`E_X`$) with a common constant and a common neighbourhood, so expectations may be taken on both sides, and Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>(b) supplies the right-hand side. ◻

</div>

**Read this:** four caveats. *First*, what was called “(A2)” in a previous revision of this proposition is the assumption listed as *(A3)* in §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>; (A2) is the noise model. *Second*, (A3) is an assumption about the *noise-dependent* objects $`J_K`$, $`p^{(K)}`$, $`m`$ and $`U`$, and it is *not* verified for the FHN data: Figure <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a> shows the noisy data term is indefinite at $`p^\star`$ for every $`\kappa\ge2`$, and Figure <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>(a) shows the points a derivative-free optimiser returns are positive definite in only $`36\%`$ of the (seed, $`\kappa`$) cells. Without (A3) the expectation form is a modelling statement, not a consequence of <a href="#eq:perr" data-reference-type="eqref" data-reference="eq:perr">(eq. perr)</a>. *Third*, the proof no longer uses $`\nabla J_K(p^{(K)})=0`$ — which is false when the minimiser lies on $`\partial P`$ — but Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a>. The hypothesis $`p^\star\in U`$ is essential and is stated: the inequality is applied at $`q=p^\star`$. When this proposition is transferred to an unconstrained iterate through Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>, the variant needed is (R$`^{p^\star}`$), i.e. (R) together with $`p^\star\in U`$; the weaker (R) suffices only for basin retention. *Fourth*, see Remark <a href="#rem:impl" data-reference-type="ref" data-reference="rem:impl">3</a>(ii): this proposition compares with $`p^\star`$ and therefore does not apply to the penalised objective actually optimised, whose minimiser is displaced from $`p^{(K)}`$ by up to $`\gamma/(m\sqrt{n_p})`$ under the containment hypotheses stated there. The bound is sharp in kind, not in size: the first inequality in <a href="#eq:perr" data-reference-type="eqref" data-reference="eq:perr">(eq. perr)</a> is an identity for a quadratic cost, so the whole slack is in the estimate of $`J_K(p^\star)`$, i.e. in the exponential.

# Propagating the best guess across shooting-node partitions

We now bound how the optimum moves when shooting nodes are removed. With $`R=\{0,\dots,K\}\setminus\mathcal I_R`$ and $`r^{+}=\min\{q\in R:q>r\}`$ as in §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>, the coarse cost is
``` math
\begin{equation}
\hat{\mathcal J}_K(p)=\sum_{r\in R\setminus\{K\}}\ \sum_{t_i\in(\tau_r,\tau_{r^{+}}]}
 \lVert \varphi_f(t_i-\tau_r;\,p,\,y_{\tau_r})-y_i\rVert^2 .
\label{eq:Jhat}
\end{equation}
```
The intervals $`(\tau_r,\tau_{r^{+}}]`$, $`r\in R\setminus\{K\}`$, partition $`(\tau_0,\tau_K]`$, so <a href="#eq:Jhat" data-reference-type="eqref" data-reference="eq:Jhat">(eq. Jhat)</a> has the same $`N-1`$ residual indices as <a href="#eq:JK" data-reference-type="eqref" data-reference="eq:JK">(eq. JK)</a>; only the launching node of some of them changes. Every datum in a fine window $`D_k`$ launched at a removed node $`\tau_k`$ is now predicted from the nearest retained node to its left, $`\tau_k^{-}`$, through $`\tau_k`$ (Figure <a href="#fig:removal" data-reference-type="ref" data-reference="fig:removal">6</a>). Throughout this section the coarse partition is a *sub-partition* of the fine one. **Read this:** that hypothesis is *not* satisfied by most stages of Algorithm <a href="#alg:gp" data-reference-type="ref" data-reference="alg:gp">Algorithm 1</a>: by <a href="#eq:nesting" data-reference-type="eqref" data-reference="eq:nesting">(eq. nesting)</a>, only two of FULL’s fifteen transitions are node removals, and one of those is the degenerate terminal stage. The earlier version of this manuscript claimed the sub-partition hypothesis “is what the algorithm does”; it is withdrawn (§<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a>). The general case is Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a>.

**Figure 6** (`figures/R002_fig05_node_removal.pdf`)

!6(figures/R002_fig05_node_removal.pdf)

***Node removal.** Zoom on $`t\in[19,41]`$ with the wrong parameter of Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a>: the fine partition ($`\kappa=5`$, blue segments) and the coarse one after removing the node at $`t=25`$ (open marker; $`\kappa=10`$, vermilion), with $`\tau_k^-=20`$ and $`\tau_k^+=30`$; the measured gap between the fine and coarse segments at $`t=37`$ is printed. After removal the segment launched at $`\tau_k^-`$ must cover $`[\tau_k^-,\tau_k^+]`$; the cost changes only on the windows adjacent to removed nodes. Replaces `comparison_shooting_nodes.png`. Inherited from R002 Figure 5; the bound printed in the R002 caption is the draft’s version, superseded by Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>.*


<div id="prop:removal" class="proposition">

**Proposition 3** (cost change under node removal, per datum; *revised*). **Scope (S3).* Assume (A0)–(A2) and work on $`E_X`$. Let the coarse partition be a sub-partition of the fine one, with removed indices $`\mathcal I_R\subset\{1,\dots,K-1\}`$. For $`k\in\mathcal I_R`$ put
``` math
h_k=\tau_{k+1}-\tau_k,\qquad w_k=\tau_k-\tau_k^{-},\qquad
D_k=\{t_i\in(\tau_k,\tau_{k+1}]\},\qquad
g_k(p)=\big\lVert\varphi_f(w_k;\,p,\,y_{\tau_k^{-}})-y_{\tau_k}\big\rVert ,
```
where $`\tau_k^{-}`$ is the nearest *retained* node to the left of $`\tau_k`$. Then for every $`p\in P`$
``` math
\begin{equation}
\big|\hat{\mathcal J}_K(p)-J_K(p)\big|\ \le\
\sum_{k\in\mathcal I_R}\ \sum_{t_i\in D_k}
e^{\mu(t_i-\tau_k)}\;g_k(p)\;\Big[r_{\tau_k^{-},t_i}(p)+r_{\tau_k,t_i}(p)\Big],
\label{eq:prop_removal_datum}
\end{equation}
```
and the node factor obeys
``` math
\begin{equation}
g_k(p)\ \le\ r_{\tau_k^{-},\tau_k}(p)
\ =\ e^{\mu w_k}\lVert \eta_{\tau_k^{-}}\rVert+Q(w_k)\lVert p-p^\star\rVert+\lVert \eta_{\tau_k}\rVert
\ \le\ \rho_{w_k}(p).
\label{eq:gk}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* *Which data change predictor.* In $`J_K`$ the datum $`t_i\in(\tau_{k-1},\tau_k]`$ is launched from $`\tau_{k-1}`$; in $`\hat{\mathcal J}_K`$ from the last *retained* node at or before $`\tau_{k-1}`$. So $`t_i`$ changes predictor iff its fine launching node is removed, i.e. exactly for $`t_i\in\bigcup_{k\in\mathcal I_R}D_k`$. The half-open convention handles the two apparent off-by-one cases: with $`\tau_k`$ removed and $`\tau_{k-1}`$ retained, the datum at time $`\tau_k`$ lies in $`(\tau_{k-1},\tau_k]`$, keeps its predictor, and is correctly *not* in $`D_k`$; with $`\tau_{k-1}`$ and $`\tau_k`$ both removed, the datum at $`\tau_k`$ lies in $`D_{k-1}`$, does change predictor, and is correctly counted there. The $`D_k`$ are pairwise disjoint.

*Admissibility of every flow evaluation, argued pairwise.* Fix $`k\in\mathcal I_R`$ and $`t_i\in D_k`$, and put
``` math
u_i=\varphi_f(t_i-\tau_k^{-};\,p,\,y_{\tau_k^{-}}),\qquad
v_i=\varphi_f(t_i-\tau_k;\,p,\,y_{\tau_k}),\qquad
a_i=\lVert u_i-y_i\rVert,\qquad b_i=\lVert v_i-y_i\rVert.
```
On $`E_X`$ we have $`\lVert y_{\tau_k^{-}}-x^\star(\tau_k^{-})\rVert\le r'`$ and $`\lVert y_{\tau_k}-x^\star(\tau_k)\rVert\le r'`$, so Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> applies to each launch *separately*: the trajectory $`s\mapsto\varphi_f(s;\,p,\,y_{\tau_k^{-}})`$ lies in $`\mathcal T\subseteq X`$ for $`s\le t_i-\tau_k^{-}\le\Delta T_{\max}`$, and $`s\mapsto\varphi_f(s;\,p,\,y_{\tau_k})`$ lies in $`\mathcal T\subseteq X`$ for $`s\le t_i-\tau_k`$. By the flow property the first of these is the coarse continuation $`\varphi_f(s-\tau_k;\,p,\,\varphi_f(w_k;\,p,\,y_{\tau_k^{-}}))`$ for $`s\ge\tau_k`$ — a *same-parameter* continuation, which is why no statement about launches from reachable states is needed. Both trajectories therefore lie in $`X`$ on $`[\tau_k,t_i]`$ and, $`X`$ being convex, so does the chord joining them at each time; hence $`T_X\big((\varphi_f(w_k;\,p,\,y_{\tau_k^{-}}),p),(y_{\tau_k},p)\big)\ge t_i-\tau_k`$ and Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> applies to that pair on $`[0,t_i-\tau_k]`$.

*The per-datum estimate.* $`\hat{\mathcal J}_K-J_K=\sum_{k\in\mathcal I_R}\sum_{t_i\in D_k}(a_i^2-b_i^2)`$ and $`|a_i^2-b_i^2|\le\lVert u_i-v_i\rVert(a_i+b_i)`$ by the reverse triangle inequality. Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> at the *actual* elapsed time gives
``` math
\lVert u_i-v_i\rVert\ \le\ e^{\mu(t_i-\tau_k)}\,\big\lVert\varphi_f(w_k;\,p,\,y_{\tau_k^{-}})-y_{\tau_k}\big\rVert
\ =\ e^{\mu(t_i-\tau_k)}g_k(p),
```
with the actual $`\mu`$ and no enlargement of the exponent, hence no monotonicity requirement and no $`\mu^{+}`$. Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">8</a> applied at node $`\tau_k^{-}`$ and datum $`t_i`$ gives $`a_i\le r_{\tau_k^{-},t_i}(p)`$, and at node $`\tau_k`$ and datum $`t_i`$ gives $`b_i\le r_{\tau_k,t_i}(p)`$ — each at its own elapsed time and with its own realised noise, so neither an envelope nor the global $`\lVert \eta\rVert_{\max}`$ is needed. Summing gives <a href="#eq:prop_removal_datum" data-reference-type="eqref" data-reference="eq:prop_removal_datum">(eq. prop_removal_datum)</a>. <a href="#eq:gk" data-reference-type="eqref" data-reference="eq:gk">(eq. gk)</a> is Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">8</a> applied to the single datum $`y_{\tau_k}`$ launched from $`y_{\tau_k^{-}}`$ over elapsed time $`w_k`$. ◻

</div>

<div id="cor:removal_rho" class="corollary">

**Corollary 2** (uniform-noise form). *Under the hypotheses of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>, replacing every realised noise norm by $`\lVert \eta\rVert_{\max}`$,
``` math
\begin{equation}
\big|\hat{\mathcal J}_K(p)-J_K(p)\big|\ \le\
\sum_{k\in\mathcal I_R}\ \sum_{t_i\in D_k}
e^{\mu(t_i-\tau_k)}\;g_k(p)\;\Big[\rho_{t_i-\tau_k^{-}}(p)+\rho_{t_i-\tau_k}(p)\Big].
\label{eq:prop_removal_node}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* $`r_{\sigma,t}\le\rho_{t-\sigma}`$ by <a href="#eq:rdatum" data-reference-type="eqref" data-reference="eq:rdatum">(eq. rdatum)</a> and $`\lVert \eta_\bullet\rVert\le\lVert \eta\rVert_{\max}`$. ◻

</div>

<div id="cor:removal_node" class="corollary">

**Corollary 3** (per-node form). *Under the same hypotheses,
``` math
\begin{equation}
\big|\hat{\mathcal J}_K(p)-J_K(p)\big|\ \le\
\sum_{k\in\mathcal I_R}|D_k|\;e^{\mu^{+}h_k}\;g_k(p)\;
\Big[\bar\rho_{h_k+w_k}(p)+\bar\rho_{h_k}(p)\Big].
\label{eq:prop_removal_pernode}
\end{equation}
```*

</div>

<div class="proof">

*Proof.* For $`t_i\in D_k`$: $`t_i-\tau_k\le h_k`$, so $`e^{\mu(t_i-\tau_k)}\le e^{\mu^{+}h_k}`$ (with $`\mu^{+}`$, because for $`\mu<0`$ the correct majorant of $`e^{\mu(t_i-\tau_k)}`$ over $`t_i-\tau_k\in[0,h_k]`$ is $`e^{0}=1`$); $`t_i-\tau_k^{-}\le h_k+w_k`$ and $`t_i-\tau_k\le h_k`$, so $`\rho_{t_i-\tau_k^{-}}\le\bar\rho_{h_k+w_k}`$ and $`\rho_{t_i-\tau_k}\le\bar\rho_{h_k}`$ by Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(i). The inner sum has $`|D_k|`$ terms. ◻

</div>

<div id="cor:removal_global" class="corollary">

**Corollary 4** (collapsed form). *Under the same hypotheses, with $`n_{\max}=\max_{k\in\mathcal I_R}|D_k|`$, $`\Delta T_1=\max_k(\tau_k-\tau_{k-1})`$ over the fine partition and $`\Delta T_2=\max_{k\in\mathcal I_R}w_k`$,
``` math
\begin{equation}
\big|\hat{\mathcal J}_K(p)-J_K(p)\big|\ \le\
n_{\max}\,|\mathcal I_R|\;e^{\mu^{+}\Delta T_1}\;\bar\rho_{\Delta T_2}(p)\;
\Big[\bar\rho_{\Delta T_1+\Delta T_2}(p)+\bar\rho_{\Delta T_1}(p)\Big]\ =:\ \Delta_K(p).
\label{eq:prop_removal}
\end{equation}
```
At $`p=p^\star`$ this is $`n_{\max}|\mathcal I_R|e^{\mu^{+}\Delta T_1}\big(1+e^{\mu^{+}\Delta T_2}\big)
\big[\big(1+e^{\mu^{+}(\Delta T_1+\Delta T_2)}\big)+\big(1+e^{\mu^{+}\Delta T_1}\big)\big]\lVert \eta\rVert_{\max}^{2}`$, quadratic in the noise. Moreover $`\Delta_K`$ is non-decreasing in $`\delta=\lVert p-p^\star\rVert`$, so $`\sup_{q\in U}\Delta_K(q)=\Delta_K`$ evaluated at $`\delta=R_U:=\sup_{q\in U}\lVert q-p^\star\rVert`$.*

</div>

<div class="proof">

*Proof.* $`h_k\le\Delta T_1`$, $`w_k\le\Delta T_2`$, $`|D_k|\le n_{\max}`$, $`g_k\le\rho_{w_k}\le\bar\rho_{\Delta T_2}`$ by <a href="#eq:gk" data-reference-type="eqref" data-reference="eq:gk">(eq. gk)</a> and Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(i), and $`\bar\rho_{h_k+w_k}\le\bar\rho_{\Delta T_1+\Delta T_2}`$, $`\bar\rho_{h_k}\le\bar\rho_{\Delta T_1}`$ by Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(ii); the sum has $`|\mathcal I_R|`$ terms. The value at $`p^\star`$ is Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(iv) and the monotonicity in $`\delta`$ is Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(iii). ◻

</div>

**Read this:** four levels, each obtained from the one above by a single named enlargement, and each reversible by stepping back. *(a)* <a href="#eq:prop_removal_datum" data-reference-type="eqref" data-reference="eq:prop_removal_datum">(eq. prop_removal_datum)</a> is the sharpest statement the Grönwall machinery of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a> supports: the actual $`\mu`$, the actual per-datum offsets, the two residual terms kept separately, and the *realised* noise at each launch node and each datum rather than the global $`\lVert \eta\rVert_{\max}`$. It is a theoretical bound, not an evaluable one: $`\lVert \eta_\sigma\rVert`$ and $`p^\star`$ are unknown in a discovery problem. The evaluable object is the score of <a href="#eq:score" data-reference-type="eqref" data-reference="eq:score">(eq. score)</a> below, which is a different thing and is not claimed to be this one. *(b)* <a href="#eq:prop_removal_node" data-reference-type="eqref" data-reference="eq:prop_removal_node">(eq. prop_removal_node)</a> substitutes $`\lVert \eta\rVert_{\max}`$ for realised noise; <a href="#eq:prop_removal_pernode" data-reference-type="eqref" data-reference="eq:prop_removal_pernode">(eq. prop_removal_pernode)</a> enlarges the elapsed times; <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> maximises over $`k`$. A previous revision stated <a href="#eq:prop_removal_node" data-reference-type="eqref" data-reference="eq:prop_removal_node">(eq. prop_removal_node)</a> as the primary bound, which discarded level (a) silently, and a still earlier one bounded $`a_i+b_i`$ by $`2\bar\rho_{h_k+w_k}`$, discarding the shorter of the two envelopes for a factor of two. *(c)* <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> is the form used in Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and Corollaries <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a>–<a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a>, because those need a bound *uniform over $`q`$ in a set*; it contains $`|\mathcal I_R|`$ but not *which* nodes, and the global $`\lVert \eta\rVert_{\max}`$ but not the noise at any particular node. *(d)* By Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(v), $`\Delta_K`$ is *piecewise* quadratic in $`(\lVert \eta\rVert_{\max},\delta)`$, the branch switching on the sign of $`\tilde L\delta-|\mu|\lVert \eta\rVert_{\max}`$ when $`\mu<0`$ and being the single quadratic branch throughout when $`\mu\ge0`$; see <a href="#eq:DeltaK_quad" data-reference-type="eqref" data-reference="eq:DeltaK_quad">(eq. DeltaK_quad)</a>. *What changed relative to the draft.* (i) $`L\to\mu`$, and $`\mu\to\mu^{+}`$ wherever an exponent is enlarged to a window length. (ii) The draft bounded $`a_i+b_i`$ through $`\lVert u_i+v_i-2y_i\rVert\le\dots+2\max\lVert y_i\rVert`$, which injects the size of the data ($`2.34`$ on FHN) into the constant; bounding $`a_i+b_i`$ by two residuals makes the bound quadratic in $`(\lVert \eta\rVert,\lVert p-p^\star\rVert)`$, as the left-hand side is, and removes a factor $`\lVert y\rVert_{\max}/\lVert \eta\rVert_{\max}\approx15`$. (iii) The draft collapsed the sum over $`t_i`$ to $`|\mathcal I_R|`$ without the number of data per window; $`n_{\max}`$ restores it. (iv) The draft’s intermediate inequality carried $`e^{2L(\Delta T_1+\Delta T_2)}`$ while its statement had $`e^{L(\Delta T_1+\Delta T_2)}`$. (v) $`\rho\to\bar\rho`$ wherever an argument is enlarged, without which the steps bounding a shorter window’s residual by a longer window’s are false for $`\mu<0`$.

## Node selection: what is computable, and what it costs

For a proposed removal set $`R`$ (a subset of the interior fine nodes) write $`\tau_k^{-}(R)`$ for the nearest node of $`S\setminus R`$ to the left of $`\tau_k`$, $`w_k(R)=\tau_k-\tau_k^{-}(R)`$, $`g_k(R;p)=\lVert \varphi_f(w_k(R);\,p,\,y_{\tau_k^{-}(R)})-y_{\tau_k}\rVert`$, and, for $`t_i\in D_k`$,
``` math
a_i(R;p)=\big\lVert\varphi_f(t_i-\tau_k^{-}(R);\,p,\,y_{\tau_k^{-}(R)})-y_i\big\rVert,
\qquad
b_i(p)=\big\lVert\varphi_f(t_i-\tau_k;\,p,\,y_{\tau_k})-y_i\big\rVert ,
```
the *proposed coarse* and *current fine* residuals. The proof of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> bounds $`|a_i^2-b_i^2|`$ by $`e^{\mu(t_i-\tau_k)}g_k(R;p)(a_i+b_i)`$ before any residual is bounded by $`r`$, $`\rho`$ or $`\bar\rho`$, so
``` math
\begin{equation}
S(R;p)=\sum_{k\in R}\ \sum_{t_i\in D_k}
e^{\mu(t_i-\tau_k)}\,g_k(R;p)\,\big(a_i(R;p)+b_i(p)\big)\ \ge\ \big|\hat{\mathcal J}_K(p)-J_K(p)\big|
\label{eq:score}
\end{equation}
```
is a valid upper bound every factor of which is computed from quantities the integrator returns, given a numerical value for $`\mu`$ (any upper bound on $`\mu`$ may be used, since $`t_i\ge\tau_k`$; $`\mu`$ is a constant of (A0) and is itself not instantiated in this report, see P1 in §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>). Two rules: *(i)* score complete proposed removal sets by $`S(R;p)`$ at the current iterate and take the smallest; *(ii)* *sequential greedy*: $`R_0=\emptyset`$ and $`R_j=R_{j-1}\cup\{\arg\min_c S(R_{j-1}\cup\{c\};p)\}`$ over retained interior candidates $`c`$, *recomputing* $`\tau_k^{-}`$, $`w_k`$, $`g_k`$ and the $`a_i`$ of the whole current set after each removal. Qualitatively, <a href="#eq:score" data-reference-type="eqref" data-reference="eq:score">(eq. score)</a> prefers nodes that launch few data ($`|D_k|`$ small), sit close to their retained left neighbour ($`w_k`$ small), and above all have a small coarse residual $`g_k`$.

**Read this:** three things this paragraph says that a previous revision did not. *(1) The score a previous revision proposed was not observable.* It read $`\sum\sum e^{\mu(t_i-\tau_k)}g_k(R;p)[\rho_{t_i-\tau_k^{-}(R)}(p)+\rho_{t_i-\tau_k}(p)]`$, and $`\rho_s(p)=(1+e^{\mu s})\lVert \eta\rVert_{\max}+Q(s)\lVert p-p^\star\rVert`$ contains the noise sup-norm and the distance to the truth, neither of which exists in a discovery problem. Making $`g_k`$ observable does not make the product observable. That form is withdrawn in favour of <a href="#eq:score" data-reference-type="eqref" data-reference="eq:score">(eq. score)</a>. *(2) The exact gap is observable at the same cost, and dominates the score for ranking — in absolute value.* The integration that produces $`a_i`$ produces $`a_i^2-b_i^2`$, so $`\hat{\mathcal J}_K(p)-J_K(p)=\sum_{k\in R}\sum_{t_i\in D_k}(a_i^2-b_i^2)`$ is available at exactly the cost of <a href="#eq:score" data-reference-type="eqref" data-reference="eq:score">(eq. score)</a>, with no slack and no $`\mu`$. *Rank by $`\big|\hat{\mathcal J}_K(p)-J_K(p)\big|`$, not by the signed gap.* The perturbation theory controls the absolute value, and every consumer of it — Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a>, Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> — consumes a two-sided bound; ranking by the signed quantity would prefer a large *negative* change of objective, which is a different and much larger perturbation, not a small one. If a large negative change of objective is deliberately wanted (a heuristic that pursues descent rather than partition continuity), that is a separate rule, unrelated to minimising the perturbation bound, and must be stated as such. <a href="#eq:score" data-reference-type="eqref" data-reference="eq:score">(eq. score)</a> and, above it, $`\Delta_K`$ of <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> earn their place only where a bound *uniform over $`q`$ in a set* is required — which is what Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> consume and what a pointwise evaluation cannot supply. *(3) There is no set-independent per-node score.* $`\tau_k^{-}`$ is the nearest *retained* node, so removing one node changes $`w_k`$, $`\tau_k^{-}`$, $`g_k`$ and every $`a_i`$ for the already-removed nodes in the same consecutive run to its right; a per-node term is a contribution *given the current retained set*, not a marginal cost that can be ranked once. Ranking once and removing the top $`|\mathcal I_R|`$ can select a set whose bound is far from the sum of its pre-removal scores, most severely for block removals — the case of Figure <a href="#fig:prop1" data-reference-type="ref" data-reference="fig:prop1">8</a>(b).

#### Cost.

Let $`R`$ be the current removal set and $`C`$ the number of retained interior candidates. Evaluating <a href="#eq:score" data-reference-type="eqref" data-reference="eq:score">(eq. score)</a> for a given $`R`$ costs one coarse-window integration per $`k\in R`$, over $`[\tau_k^{-}(R),\max D_k]`$ (length $`w_k+h_k`$), each of which yields $`g_k`$ and all the $`a_i`$ of that node as by-products; the $`b_i`$ are residuals the fine objective already computes. Hence:

- **per candidate $`c`$:** $`O(1)`$ coarse-window integrations for $`c`$ itself, *plus* a re-integration for every already-removed $`k\in R`$ whose retained left neighbour changes when $`c`$ is removed — none if $`c`$ is isolated from $`R`$, and the whole consecutive run to the right of $`c`$ in the worst case, i.e. $`O(|R|)`$;

- **per greedy step:** $`O(C\,|R|)`$ window integrations in the worst case, $`O(C)`$ when removals stay isolated;

- **whole greedy schedule** of $`|\mathcal I_R|`$ removals: $`O(|\mathcal I_R|\,C\,|R|)=O(C\,|\mathcal I_R|^{2})`$ in the worst case.

**Read this:** caching, which is a remark and not the headline figure. Removing $`c`$ changes $`\tau_k^{-}`$ only for nodes in the removed run immediately to its right, so contributions cached by the key $`(\tau_k^{-}(R),\tau_k)`$ remain valid for every other candidate and a greedy step re-evaluates only the candidates whose left retained neighbour changed at the previous step — $`O(1)`$ candidates for isolated removals. The worst-case figures above stand. Standing caveat: all of this minimises a *bound* on the cost gap (or, for the exact gap, the absolute cost gap itself at one $`p`$), which is not the same as minimising the displacement of the minimiser; that step needs the strong-convexity premise of Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and the reconnection hypothesis (R) of Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>. The design rule of the draft survives in this weakened form: remove few nodes at a time, keep $`w_k`$ small, prefer nodes with a small measured $`g_k`$ (Figure <a href="#fig:prop1" data-reference-type="ref" data-reference="fig:prop1">8</a>). “Prefer low-noise nodes” does *not* survive as an implementable rule, because $`\lVert \eta_{\tau_k}\rVert`$ in <a href="#eq:gk" data-reference-type="eqref" data-reference="eq:gk">(eq. gk)</a> is not observable.

## Displacement of the minimiser

<div id="thm:main" class="theorem">

**Theorem 1** (a posteriori, conditional displacement of the minimiser under node removal; *revised*). **Scope (S3).* Assume (A0)–(A2) and work on $`E_X`$. Let $`p^{(K)}\in\arg\min_{P}J_K`$ and $`\hat p^{(K)}\in\arg\min_{P}\hat{\mathcal J}_K`$ as in <a href="#eq:minimisers" data-reference-type="eqref" data-reference="eq:minimisers">(eq. minimisers)</a>, and suppose $`J_K`$ is $`m`$-strongly convex, $`m>0`$, on a convex $`U\subseteq P`$ containing both. Then
``` math
\begin{equation}
\lVert \hat p^{(K)}-p^{(K)}\rVert\ \le\
\sqrt{\frac2m\Big(\Delta_K\big(p^{(K)}\big)+\Delta_K\big(\hat p^{(K)}\big)\Big)}
\ \le\ \sqrt{\frac4m\,\sup_{q\in U}\Delta_K(q)},
\label{eq:thm}
\end{equation}
```
with $`\Delta_K`$ from <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a>.*

</div>

<div class="proof">

*Proof.* Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a> with $`F=J_K`$, $`p=p^{(K)}`$, $`q=\hat p^{(K)}\in U`$ gives $`\tfrac m2\lVert \hat p^{(K)}-p^{(K)}\rVert^2\le J_K(\hat p^{(K)})-J_K(p^{(K)})`$. Insert $`\pm\hat{\mathcal J}_K(\hat p^{(K)})`$ and $`\pm\hat{\mathcal J}_K(p^{(K)})`$:
``` math
J_K(\hat p^{(K)})-J_K(p^{(K)})=
\big[J_K(\hat p^{(K)})-\hat{\mathcal J}_K(\hat p^{(K)})\big]
+\big[\hat{\mathcal J}_K(\hat p^{(K)})-\hat{\mathcal J}_K(p^{(K)})\big]
+\big[\hat{\mathcal J}_K(p^{(K)})-J_K(p^{(K)})\big].
```
The middle bracket is $`\le0`$ because $`\hat p^{(K)}`$ minimises $`\hat{\mathcal J}_K`$ over $`P`$ and $`p^{(K)}\in P`$; the outer brackets are bounded by Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a>. The second inequality of <a href="#eq:thm" data-reference-type="eqref" data-reference="eq:thm">(eq. thm)</a> replaces each $`\Delta_K`$ by its supremum over $`U`$. ◻

</div>

Writing $`E=\lVert \eta\rVert_{\max}`$, $`\delta=\lVert q-p^\star\rVert`$ and $`c_0=n_{\max}|\mathcal I_R|e^{\mu^{+}\Delta T_1}`$, the bound of <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> is
``` math
\begin{equation}
\Delta_K(q)=c_0\,\bar\rho_{\Delta T_2}(q)\Big[\bar\rho_{\Delta T_1+\Delta T_2}(q)
+\bar\rho_{\Delta T_1}(q)\Big],
\label{eq:DeltaK_quad}
\end{equation}
```
a *piecewise* homogeneous quadratic in $`(E,\delta)`$ with non-negative deterministic coefficients on each branch. By Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(v), and assuming $`\Delta T_1,\Delta T_2>0`$:

- if $`\mu\ge0`$, or if $`\mu<0`$ and $`\tilde L\delta>|\mu|E`$, every $`\bar\rho`$ equals the corresponding $`\rho`$ and $`\Delta_K=c_0\,\rho_{\Delta T_2}\big[\rho_{\Delta T_1+\Delta T_2}+\rho_{\Delta T_1}\big]`$;

- if $`\mu<0`$ and $`\tilde L\delta\le|\mu|E`$, then $`\bar\rho_s=2E`$ for *all three* arguments, so
  ``` math
  \begin{equation}
  \Delta_K=c_0\,(2E)\,\big[2E+2E\big]=8c_0E^{2}.
  \label{eq:DeltaK_flat}
  \end{equation}
  ```

**Read this:** the factor in <a href="#eq:DeltaK_flat" data-reference-type="eqref" data-reference="eq:DeltaK_flat">(eq. DeltaK_flat)</a> is $`8`$, not $`4`$: the bracket of <a href="#eq:DeltaK_quad" data-reference-type="eqref" data-reference="eq:DeltaK_quad">(eq. DeltaK_quad)</a> holds *two* envelopes, each equal to $`2E`$ on that branch, so the product is $`2E\cdot4E`$. An earlier version wrote $`4c_0E^{2}`$, having collapsed the bracket as if it held one. The check that settles it is the agreement with Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a> evaluated at $`p=p^\star`$, which must lie on this branch when $`\mu<0`$ since $`\delta=0`$: there $`\Delta_K(p^\star)=c_0(1+e^{\mu^{+}\Delta T_2})[(1+e^{\mu^{+}(\Delta T_1+\Delta T_2)})+(1+e^{\mu^{+}\Delta T_1})]E^{2}=c_0\cdot2\cdot4\,E^{2}=8c_0E^{2}`$ for $`\mu<0`$. The two routes must agree, and any future change to <a href="#eq:prop_removal" data-reference-type="eqref" data-reference="eq:prop_removal">(eq. prop_removal)</a> must be checked against both. On every branch $`\Delta_K`$ is non-decreasing in $`\delta`$, so $`\sup_{q\in U}\Delta_K(q)`$ is $`\Delta_K`$ evaluated at $`\delta=R_U:=\sup_{q\in U}\lVert q-p^\star\rVert`$: the supremum in <a href="#eq:thm" data-reference-type="eqref" data-reference="eq:thm">(eq. thm)</a> is computable, not merely finite. On the branch <a href="#eq:DeltaK_flat" data-reference-type="eqref" data-reference="eq:DeltaK_flat">(eq. DeltaK_flat)</a> the displacement bound <a href="#eq:thm" data-reference-type="eqref" data-reference="eq:thm">(eq. thm)</a> reads $`\lVert \hat p^{(K)}-p^{(K)}\rVert\le\sqrt{32c_0E^{2}/m}=4E\sqrt{2c_0/m}`$ and the smallness condition (B2) of Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> reads $`8c_0E^{2}<mr^{2}/4`$, i.e. $`E<(r/4)\sqrt{m/(2c_0)}`$. A smooth quadratic majorant $`\Delta_K\le\Delta_K^{+}`$, obtained by replacing each $`\bar\rho`$ with $`\rho^{+}`$, is given in Remark <a href="#rem:rhoplus" data-reference-type="ref" data-reference="rem:rhoplus">2</a>; it is convenient (for instance for solving (B2) for $`r`$ in closed form) and strictly loose for $`\mu<0`$ away from $`p^\star`$. **Read this:** Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> is a *conditional a posteriori* estimate, not a proof that guess propagation stays in the right basin. It assumes what a displacement theorem is supposed to help establish — that $`\hat p^{(K)}`$ already lies in the old strong-convexity neighbourhood — and in its first form the right-hand side contains $`\hat p^{(K)}`$, so the inequality is implicit; the second form is explicit but needs $`U`$ to be named, and $`R_U`$ finite. Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> supplies the smallness condition under which the premise can be dispensed with, at the price of speaking about the minimiser of $`\hat{\mathcal J}_K`$ *restricted to a ball* rather than its global minimiser. The proof no longer uses $`\nabla J_K(p^{(K)})=0`$, so the statement covers minimisers on $`\partial P`$; when it is applied to a number of §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> it is conditional on (R) of Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>, and then only in its restricted form. *What changed.* The structure of the proof is the draft’s; the bound inherits the improvements of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>, so the displacement now scales like the noise (through $`\sqrt{\Delta_K}\sim\lVert \eta\rVert_{\max}`$), not like $`\sqrt{\text{noise}\times\text{attractor size}}`$. The draft mixed $`L(p)`$, $`L(p^\star)`$ and $`L_K=\max\{L(p^{(K)}),L(\hat p^{(K)})\}`$ in one formula; with the constants taken over $`X\times P`$ the distinction disappears. A previous revision called $`\Delta_K`$ “a quadratic polynomial …with coefficients …and smaller”, which was wrong on both counts: with $`\bar\rho`$ it is only piecewise quadratic, and the third coefficient was left vague.

<div id="cor:basin" class="corollary">

**Corollary 5** (basin retention under node removal). **Scope (S3).* Assume (A0)–(A2) and work on $`E_X`$. Let $`r>0`$ and $`\bar B=\{q:\lVert q-p^{(K)}\rVert\le r\}`$. Suppose*

1.  *$`\bar B\subseteq P`$;*

2.  *$`J_K`$ is $`m`$-strongly convex on $`\bar B`$ with $`m>0`$, and $`p^{(K)}\in\arg\min_{P}J_K`$;*

3.  *$`\displaystyle\bar\Delta:=\sup_{q\in\bar B}\Delta_K(q)<\frac{m r^2}{4}`$.*

*Then the minimum of $`\hat{\mathcal J}_K`$ over $`\bar B`$ is attained at an interior point $`\hat p`$ of $`\bar B`$, and
``` math
\begin{equation}
\lVert \hat p-p^{(K)}\rVert\ \le\ 2\sqrt{\frac{\bar\Delta}{m}}\ <\ r .
\label{eq:basin}
\end{equation}
```
Moreover $`\hat p`$ is a stationary point of $`\hat{\mathcal J}_K`$, $`\nabla\hat{\mathcal J}_K(\hat p)=0`$, and hence a local minimiser of $`\hat{\mathcal J}_K`$ on $`P`$.*

</div>

<div class="proof">

*Proof.* $`\hat{\mathcal J}_K`$ is continuous and $`\bar B`$ compact, so the minimum over $`\bar B`$ is attained. Let $`q\in\partial\bar B`$, i.e. $`\lVert q-p^{(K)}\rVert=r`$; by (B0) $`q\in P`$, so Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a> with $`U=\bar B`$ gives $`J_K(q)\ge J_K(p^{(K)})+\tfrac m2r^2`$. Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a> gives $`|\hat{\mathcal J}_K-J_K|\le\bar\Delta`$ on $`\bar B`$ (legitimate by (B0), which puts $`\bar B`$ inside the region where the constants of (A0) hold), so
``` math
\hat{\mathcal J}_K(q)\ \ge\ J_K(q)-\bar\Delta\ \ge\ J_K(p^{(K)})+\tfrac m2r^2-\bar\Delta
\ \ge\ \hat{\mathcal J}_K(p^{(K)})+\tfrac m2r^2-2\bar\Delta\ >\ \hat{\mathcal J}_K(p^{(K)})
```
by (B2). Hence every boundary point of $`\bar B`$ has strictly larger $`\hat{\mathcal J}_K`$ than the interior point $`p^{(K)}`$, so the minimiser $`\hat p`$ over $`\bar B`$ is interior to $`\bar B`$. For the bound, $`\hat p,p^{(K)}\in\bar B`$ and, by Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a> again,
``` math
\begin{aligned}
\tfrac m2\lVert \hat p-p^{(K)}\rVert^2\ \le\ J_K(\hat p)-J_K(p^{(K)})
=\ &\underbrace{\big[J_K(\hat p)-\hat{\mathcal J}_K(\hat p)\big]}_{\le\bar\Delta}
+\underbrace{\big[\hat{\mathcal J}_K(\hat p)-\hat{\mathcal J}_K(p^{(K)})\big]}_{\le0}\\
&+\underbrace{\big[\hat{\mathcal J}_K(p^{(K)})-J_K(p^{(K)})\big]}_{\le\bar\Delta}\ \le\ 2\bar\Delta,
\end{aligned}
```
the middle bracket being $`\le0`$ because $`\hat p`$ minimises $`\hat{\mathcal J}_K`$ over $`\bar B\ni p^{(K)}`$. Finally, $`\hat p`$ is interior to $`\bar B`$ and $`\bar B\subseteq P`$ by (B0), so there is $`\varepsilon>0`$ with $`B(\hat p,\varepsilon)\subseteq\bar B\subseteq P`$; hence $`\hat p\in\operatorname{int}P`$, the unconstrained first-order condition applies, and $`\nabla\hat{\mathcal J}_K(\hat p)=0`$. ◻

</div>

**Read this:** what this does and does not say, and what the hypotheses cost. *(B0) is not decorative*: without $`\bar B\subseteq P`$ the quantity $`\sup_{\bar B}\Delta_K`$ is a supremum of a bound whose constants are suprema over $`X\times P`$ and therefore need not apply on part of $`\bar B`$. *Interiority costs nothing extra here, but positive clearance from $`\partial P`$ does.* Since $`r>0`$, (B0) makes $`\bar B`$ a full-dimensional closed ball inside $`P`$, so every point interior to $`\bar B`$ is automatically interior to $`P`$ and the stationarity conclusion needs no hypothesis beyond (B0); an earlier version of this corollary added $`\bar B\subseteq\operatorname{int}P`$, which is redundant. The genuine cost is that (B0) with $`r>0`$ forces $`p^{(K)}\in\operatorname{int}P`$ at distance at least $`r`$ from $`\partial P`$, so *this corollary does not cover a minimiser on $`\partial P`$*, and the claim that Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a> frees the theory from interiority assumptions must not be read as covering it. Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and the displacement bound of Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> do hold verbatim for boundary minimisers, because they use only <a href="#eq:vi" data-reference-type="eqref" data-reference="eq:vi">(eq. vi)</a>. *It does not bound the global minimiser of $`\hat{\mathcal J}_K`$*: the coarse landscape acquires minima elsewhere as the windows grow (Figures <a href="#fig:gp" data-reference-type="ref" data-reference="fig:gp">5</a>, <a href="#fig:land1d" data-reference-type="ref" data-reference="fig:land1d">10</a>), and no perturbation argument can exclude a lower value far away. It says the coarse problem *restricted to the ball* has its minimiser strictly inside, close to $`p^{(K)}`$ — which is the object guess propagation chases, since the next stage starts *at* $`p^{(K)}`$. A continuous descent path started at $`p^{(K)}`$ cannot leave $`\bar B`$, because $`\hat{\mathcal J}_K>\hat{\mathcal J}_K(p^{(K)})`$ on the whole boundary shell; Nelder–Mead’s iterates are not a continuous path and can in principle jump the shell, so for the runs of §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> this is a statement about the restricted problem, not a guarantee about the optimiser used — and, by Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>, applies to those runs only under (R). Condition (B2) is a genuine smallness requirement: $`\bar\Delta`$ grows with $`|\mathcal I_R|`$, $`n_{\max}`$, $`\Delta T_1`$ and $`\Delta T_2`$, so it is the formal version of “remove few nodes at a time”. By <a href="#eq:DeltaK_quad" data-reference-type="eqref" data-reference="eq:DeltaK_quad">(eq. DeltaK_quad)</a>, $`\bar\Delta=\Delta_K`$ evaluated at $`\delta=\lVert p^{(K)}-p^\star\rVert+r`$.

<div id="cor:repartition" class="corollary">

**Corollary 6** (re-partition with simultaneous node additions and removals). **Scope (S3).* Assume (A0)–(A2) and work on $`E_X`$. Let $`A`$ and $`B`$ be two node sets, both containing $`t_0`$ and $`t_{N-1}`$, and let $`C=A\cup B`$ be their common refinement. Write $`J_A,J_B,J_C`$ for the corresponding costs <a href="#eq:JK" data-reference-type="eqref" data-reference="eq:JK">(eq. JK)</a>, let $`\Delta T_1^{C}`$ be the longest window of $`C`$, $`n^{C}_{\max}`$ the largest number of data in a window of $`C`$, and for $`S\in\{A,B\}`$ let $`\Delta T^{S}`$ be the longest window of $`S`$. Put
``` math
\begin{equation}
\Delta^{C\to S}(p)=n^{C}_{\max}\,\big|C\setminus S\big|\;
e^{\mu^{+}\Delta T_1^{C}}\;\bar\rho_{\Delta T^{S}}(p)\;
\Big[\bar\rho_{\Delta T_1^{C}+\Delta T^{S}}(p)+\bar\rho_{\Delta T_1^{C}}(p)\Big],
\qquad
\Delta_{A,B}=\Delta^{C\to A}+\Delta^{C\to B}.
\label{eq:repart}
\end{equation}
```
Then $`\big|J_A(p)-J_B(p)\big|\le\Delta_{A,B}(p)`$ for every $`p\in P`$. At $`p=p^\star`$,
``` math
\begin{equation}
\Delta^{C\to S}(p^\star)=n^{C}_{\max}\big|C\setminus S\big|e^{\mu^{+}\Delta T_1^{C}}
\big(1+e^{\mu^{+}\Delta T^{S}}\big)
\Big[\big(1+e^{\mu^{+}(\Delta T_1^{C}+\Delta T^{S})}\big)+\big(1+e^{\mu^{+}\Delta T_1^{C}}\big)\Big]\lVert \eta\rVert_{\max}^{2}.
\label{eq:repart_true}
\end{equation}
```
If moreover $`J_A`$ is $`m`$-strongly convex on a convex $`U\subseteq P`$ containing $`p_A\in\arg\min_{P}J_A`$ and $`p_B\in\arg\min_{P}J_B`$, then
``` math
\begin{equation}
\lVert p_B-p_A\rVert\ \le\ \sqrt{\frac2m\Big(\Delta_{A,B}(p_A)+\Delta_{A,B}(p_B)\Big)}
\ \le\ \sqrt{\frac4m\sup_{q\in U}\Delta_{A,B}(q)} ,
\label{eq:repart_disp}
\end{equation}
```
and if instead the hypotheses of Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> hold with $`J_A`$, $`J_B`$ and $`\Delta_{A,B}`$ in place of $`J_K`$, $`\hat{\mathcal J}_K`$ and $`\Delta_K`$, the minimiser of $`J_B`$ over $`\bar B(p_A,r)`$ is interior to that ball and satisfies $`\lVert \cdot-p_A\rVert\le2\sqrt{\bar\Delta/m}<r`$.*

</div>

<div class="proof">

*Proof.* $`A\subseteq C`$ and $`B\subseteq C`$, so $`A`$ is obtained from the fine partition $`C`$ by removing the nodes $`C\setminus A`$, and likewise for $`B`$; neither removed set contains the endpoints. Corollary <a href="#cor:removal_global" data-reference-type="ref" data-reference="cor:removal_global">4</a> applied twice with fine partition $`C`$ gives $`|J_A-J_C|\le\Delta^{C\to A}`$ and $`|J_C-J_B|\le\Delta^{C\to B}`$, where $`\Delta T_2`$ for the removal $`C\to S`$ is at most $`\Delta T^{S}`$, because the nearest retained node to the left of any removed node lies in $`S`$ and successive $`S`$-nodes are at most $`\Delta T^{S}`$ apart. The triangle inequality gives the first claim; <a href="#eq:repart_true" data-reference-type="eqref" data-reference="eq:repart_true">(eq. repart_true)</a> is Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(iv). For <a href="#eq:repart_disp" data-reference-type="eqref" data-reference="eq:repart_disp">(eq. repart_disp)</a>, run the proof of Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> with $`(J_K,\hat{\mathcal J}_K,\Delta_K)`$ replaced by $`(J_A,J_B,\Delta_{A,B})`$: the only properties used are the pointwise two-sided bound, $`J_B(p_B)\le J_B(p_A)`$, and Lemma <a href="#lem:vi" data-reference-type="ref" data-reference="lem:vi">2</a>; monotonicity of each $`\bar\rho`$ in $`\delta`$ (Lemma <a href="#lem:rhobar" data-reference-type="ref" data-reference="lem:rhobar">4</a>(iii)) gives the supremum form. The last claim is Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> verbatim with the same substitution. ◻

</div>

**Read this:** <a href="#eq:repart" data-reference-type="eqref" data-reference="eq:repart">(eq. repart)</a> inherits the corrected step of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>: the two residual envelopes are kept *separately* in the bracket, and the leading factor is $`n^{C}_{\max}`$, not $`2n^{C}_{\max}`$. A previous revision carried the discarded relaxation $`2n^{C}_{\max}|C\setminus S|e^{\mu^{+}\Delta T_1^{C}}\bar\rho_{\Delta T^{S}}\bar\rho_{\Delta T_1^{C}+\Delta T^{S}}`$, which is still a valid upper bound — the bracket is at most twice its longer envelope — but strictly weaker, and inconsistent with the tightened Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> it is supposed to inherit. Every downstream constant (the displacement <a href="#eq:repart_disp" data-reference-type="eqref" data-reference="eq:repart_disp">(eq. repart_disp)</a>, and the (B2) threshold when Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> is applied with $`\Delta_{A,B}`$) is computed from <a href="#eq:repart" data-reference-type="eqref" data-reference="eq:repart">(eq. repart)</a> as displayed.

<div id="rem:repart_numbers" class="remark">

*Remark 4* (the re-partition bound on this algorithm). For the FULL schedule’s stage $`\kappa=2\to\kappa=3`$ (a stage that is *not* a node removal, see §<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a>), with $`N=101`$ and the common terminal node $`t_{100}`$ belonging to every partition:
``` math
A=\{0,2,4,\dots,98\}\cup\{100\}\ (51\ \text{nodes}),\qquad
B=\{0,3,6,\dots,99\}\cup\{100\}\ (35\ \text{nodes}),
```
``` math
A\cap B=\{0,6,\dots,96\}\cup\{100\}\ (18\ \text{nodes}),\qquad
C=A\cup B\ (68\ \text{nodes}),
```
so $`|C\setminus A|=17`$, $`|C\setminus B|=33`$. The gaps of $`C`$ are $`1`$ or $`2`$ data intervals (the pattern $`0,2,3,4,6`$ repeats with period $`6`$; the tail is $`96,98,99,100`$), giving $`\Delta T_1^{C}=2\Delta t`$ and $`n^{C}_{\max}=2`$; the gaps of $`A`$ are all $`2\Delta t`$ (including $`98\to100`$) and the largest gap of $`B`$ is $`3\Delta t`$, so $`\Delta T^{A}=2\Delta t`$ and $`\Delta T^{B}=3\Delta t`$. The bound is therefore driven by $`|C\setminus A|+|C\setminus B|=50`$ removed-node terms — more than for a single clean removal. Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> restores a theorem for the algorithm as run; it does not make its stages cheap. **Read this:** every count above *includes the terminal node* $`t_{100}`$, which is a node of every partition. A previous version of this remark reported $`50`$, $`34`$, $`17`$ and $`67`$, which are the *launch*-node counts; mixing the two conventions in one example is what the indexing paragraph of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a> exists to prevent. The removed-node counts $`17`$ and $`33`$ are the same under either convention, since $`|C\setminus S|=|C|-|S|`$ and both counts shift by the same endpoint.

</div>

# Refinement: trajectory-dependent exponents and a weighted norm

The bounds above are a priori: they hold for every right-hand side with the same constants and are attained by the linear system $`\dot x=\mu x`$, so they are necessarily exponential. Along an attracting orbit the actual growth is far smaller, and the same proof yields a sharper, trajectory-dependent statement — but, as the remark after it records, one that may *not* simply be substituted back into the propositions.

<div id="cor:window" class="corollary">

**Corollary 7** (per-window and per-subinterval exponents). **Scope (S1).* Assume (A0) and let $`\mu(t)=\lambda_{\max}\big(\tfrac12(A(t)+A(t)^\top)\big)`$ for the mean-value matrix $`A(t)`$ of <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> associated with *the specific pair of trajectories and the specific parameter under consideration*, on an interval where that pair is confined in the sense of Definition <a href="#def:Tx" data-reference-type="ref" data-reference="def:Tx">1</a>. Then*

1.  *(state sensitivity, local form) for $`0\le a\le b`$,
    ``` math
    \begin{equation}
    \lVert \delta(b)\rVert\le\exp\Big(\int_a^b\mu(r)\,\mathrm{d}r\Big)\lVert \delta(a)\rVert,
    \qquad \delta=\varphi_f(\cdot;\,p,\,x_1)-\varphi_f(\cdot;\,p,\,x_2);
    \label{eq:window}
    \end{equation}
    ```*

2.  *(parameter sensitivity, local form) with $`\delta=\varphi_f(\cdot;\,p_1,\,x_0)-\varphi_f(\cdot;\,p_2,\,x_0)`$ and $`\delta(0)=0`$,
    ``` math
    \begin{equation}
    \lVert \delta(t)\rVert\ \le\ \tilde L\lVert p_1-p_2\rVert\int_0^t\exp\Big(\int_s^t\mu(r)\,\mathrm{d}r\Big)\,\mathrm{d}s .
    \label{eq:window_param}
    \end{equation}
    ```*

*Both are *realisation-wise and pair-specific*: $`\mu(\cdot)`$ is built from the two trajectories and the parameter named in the statement, and from no others.*

</div>

<div class="proof">

*Proof.* (i) is <a href="#eq:proof1" data-reference-type="eqref" data-reference="eq:proof1">(eq. proof1)</a> integrated between $`a`$ and $`b`$ instead of $`0`$ and $`t`$. (ii) is the Dini-derivative inequality $`D^{+}\lVert \delta\rVert\le\mu(t)\lVert \delta\rVert+\tilde L\lVert p_1-p_2\rVert`$ of Lemma <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> integrated by variation of constants. ◻

</div>

<div id="rem:envelopes" class="remark">

*Remark 5* (what would be needed to substitute these into the propositions). Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">7</a> does *not* license replacing $`e^{\mu^{+}s}`$ by $`e^{\Lambda_k}`$, or by $`M_k`$ of <a href="#eq:Mk" data-reference-type="eqref" data-reference="eq:Mk">(eq. Mk)</a>, anywhere in §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a>. What would license a substitution is a pair of *deterministic envelopes* taken over the class of pairs the proofs actually use. Call a pair $`(\xi_1,\xi_2)`$ of solutions of $`\dot\xi=f(\xi;p_j)`$ on $`[a,b]\subseteq[t_0,t_{N-1}]`$, with $`p_1,p_2\in P`$, *confined* if $`[\xi_1(s),\xi_2(s)]\subset X`$ for every $`s\in[a,b]`$, and write $`\mu_\xi(s)=\lambda_{\max}\big(\tfrac12(A_\xi+A_\xi^{\top})\big)`$ for its mean-value matrix $`A_\xi(s)=\int_0^1J_f\big(\xi_2(s)+\theta(\xi_1(s)-\xi_2(s));p_1\big)\,\mathrm{d}\theta`$. For $`h>0`$ put
``` math
\begin{align}
\mathcal M(h)&=\sup\Big\{\exp\Big(\int_a^b\mu_\xi(r)\,\mathrm{d}r\Big):\ \text{confined pairs with }p_1=p_2\in P,\
0\le b-a\le h\Big\},\label{eq:envelopeM}\\
\mathcal Q(h)&=\sup\Big\{\tilde L\int_a^b\exp\Big(\int_s^b\mu_\xi(r)\,\mathrm{d}r\Big)\,\mathrm{d}s:\
\text{confined pairs with }p_1,p_2\in P,\ 0\le b-a\le h\Big\}.\label{eq:envelopeQ}
\end{align}
```
With these, Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">8</a>, Propositions <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a> and <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> and Corollaries <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a>–<a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> hold with $`e^{\mu^{+}s}\rightsquigarrow\mathcal M(s)`$ and $`Q(s)\rightsquigarrow\mathcal Q(s)`$, and nothing is lost, since $`\mu_\xi(r)\le\mu`$ pointwise on $`X`$ gives $`\mathcal M(h)\le e^{\mu^{+}h}`$ and $`\mathcal Q(h)\le Q(h)`$. **Read this:** *why the class must be “confined”, not “launched within $`r'`$ of the orbit”.* On $`E_X`$ every shooting node satisfies $`\lVert y_\tau-x^\star(\tau)\rVert\le r'`$, so Lemma <a href="#lem:noescape" data-reference-type="ref" data-reference="lem:noescape">7</a> confines the trajectory launched from *each* such node — separately, under its own parameter — to $`\mathcal T\subseteq X`$, and convexity of $`X`$ places every joining chord in $`X`$. Every pair used in §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> is a pair of such trajectories, so every such pair is confined; that, and not any statement about launches from reachable states, is what licenses the substitution. Note that the pairs are *not* all launched at a common time or from states within $`r'`$ of the orbit at the comparison time: the coarse predictor of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> is compared with the fine one at $`\tau_k`$, where it has already run for $`w_k`$ and is known only to be within $`r_X`$ of the orbit, and the pairs of Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> are launched from the nodes of two different partitions. A previous version of this remark defined the suprema over pairs “launched anywhere within $`r'`$ of the orbit, at any $`p\in P`$”, which excludes those pairs and left the advertised substitution unlicensed in precisely the place it was advertised; and “at any $`p\in P`$” is ambiguous for $`\mathcal Q`$, whose pairs carry *two* parameters. Both are fixed above. *Four reasons the per-window quantities cannot play this role*, each sufficient on its own. *(1) Pair dependence.* $`\mu(\cdot)`$ is attached to one pair and one parameter, while the proofs use at least five: noisy versus clean launch, $`p`$ versus $`p^\star`$, coarse versus fine prediction, different $`q\in U`$, and windows of two partitions and their common refinement. *(2) Interval spanning.* A coarse interval crosses several fine windows and $`\exp(\int_a^b\mu)`$ is then a *product* of per-window factors, which $`\max_kM_k`$ does not bound. *(3) The forcing integral.* $`\int_0^t\exp(\int_s^t\mu)\,\mathrm{d}s\le M_kt`$ requires $`[s,t]`$ to lie inside window $`k`$ for every $`s\le t`$, which fails as soon as $`t`$ is in a later window than $`0`$. *(4) Randomness.* An a posteriori $`M_k`$ computed along a realised noisy pair is a random variable and cannot be factored out of $`\mathbb E\lVert \eta\rVert^2`$. And what §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> reports is neither $`\mathcal M`$ nor $`M_k`$: Table <a href="#tab:windows" data-reference-type="ref" data-reference="tab:windows">2</a> gives *linearised endpoint* exponents $`e^{\Lambda_k}`$ computed from $`J_f(x^\star(t);p^\star)`$ along the *true* orbit at the *true* parameter with windows aligned to $`t_0`$ — the infinitesimal-perturbation limit of one pair. These are not suprema over a tube of trajectories, not valid at another $`p`$, and not sub-interval bounds ($`M_k\ge\max\{1,e^{\Lambda_k}\}`$, strictly wherever $`\mu(\cdot)`$ changes sign inside the window, which on FHN happens twice per period, Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a>c). $`\mathcal M(h)`$ is not computed anywhere in this report. The node-placement rule below is therefore a *heuristic* read off a measurement, and a previous version of Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">7</a> carried it as a licensed substitution (an item (iii) asserting that every proposition holds with $`e^{\mu\Delta T}`$ replaced by $`\max_ke^{\Lambda_k}`$), which is withdrawn.

</div>

For FHN the heuristic reads: keep nodes just before the fast jumps, where $`\int\mu`$ accumulates, and remove them freely on the slow branches, where the linearised window exponents are negative. What no Grönwall-type argument can deliver is the observed *plateau* of the amplification (Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a>a): that is a property of the attractor and would require a contraction metric (impossible globally on a limit cycle, whose phase direction is neutral), a Lyapunov function, or a Floquet analysis of the variational equation.

<div id="rem:weighted" class="remark">

*Remark 6* (weighted norms: what actually transfers). Let $`\lVert x\rVert_D=\lVert Dx\rVert`$ with $`D`$ invertible, $`\mu_D`$ the logarithmic norm of $`DJ_fD^{-1}`$ over $`X\times P`$, and $`\mathrm{cond}(D)=\lVert D\rVert\lVert D^{-1}\rVert`$. Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> holds verbatim in $`\lVert \cdot\rVert_D`$ with $`\mu_D`$ in place of $`\mu`$, and converting to the Euclidean norm costs one factor of the condition number: $`\lVert \delta(t)\rVert\le\mathrm{cond}(D)\,e^{\mu_D t}\lVert \delta(0)\rVert`$. *Nothing else transfers for free*: Lemma <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> requires the weighted parameter constant $`\tilde L_D=\sup_{X\times P}\lVert D\,\partial f/\partial p\rVert_{2\leftarrow2}`$; Lemma <a href="#lem:residual" data-reference-type="ref" data-reference="lem:residual">8</a> requires the weighted noise $`\lVert D\eta_i\rVert`$ with $`\mathbb E\lVert D\eta_i\rVert^2=\operatorname{tr}(D\Sigma D^\top)`$; a cost built from $`D`$-weighted residuals has a *different minimiser* from <a href="#eq:JK" data-reference-type="eqref" data-reference="eq:JK">(eq. JK)</a>; and a $`D`$-norm residual bound converted back into a statement about the Euclidean *squared* cost picks up the conversion factor *squared*. We therefore make no weighted claim about $`J_K`$, $`\hat{\mathcal J}_K`$, $`p^{(K)}`$ or $`\hat p^{(K)}`$. The weighted entries of Table <a href="#tab:windows" data-reference-type="ref" data-reference="tab:windows">2</a> are reported as what they are: linearised endpoint amplification factors in $`\lVert \cdot\rVert_D`$ along the true orbit, whose Euclidean counterpart is larger by at most $`\mathrm{cond}(D)=3.54`$. On FHN, $`D=\mathrm{diag}(1,s)`$ with $`s=\sqrt{|J_{12}|/|J_{21}|}=3.54`$ makes the off-diagonal entries of $`DJ_fD^{-1}`$ equal and opposite, so its symmetric part is diagonal and $`\mu_D(x)=\max(1-v^2,-0.064)`$ — negative whenever $`|v|>1`$, i.e. on both slow branches of the relaxation oscillation, with global supremum $`1`$ *at $`p^\star`$*.

</div>

# Numerical results

All results in this section were computed in report R002 (frozen code snapshot, 522 optimisation runs, 18 gates) and R003 (bound evaluation, 8 gates); the figures and tables are copied here byte for byte and every number quoted in the text is regenerated from the copied result files (§<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>). The protocol is that of §<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a>. **Read this:** the constants reported here are *measurements along the true orbit at $`p^\star`$ over R002’s probe box*. They are not the suprema over $`X\times P`$ that (A0) defines, because $`X\times P`$ has never been instantiated; and the optimisers are unconstrained, so the minimisers reported here are the objects of the theory only under hypothesis (R) of Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a>. Both gaps are recorded as open premise checks P1 and P2 in §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a>.

## The bounds against measurement

**Figure 7** (`figures/R003_fig01_bounds.pdf`)

!7(figures/R003_fig01_bounds.pdf)

***Lemmas <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> on FHN.** (a) Amplification $`\lVert \varphi(t;p^\star,x_1)-\varphi(t;p^\star,x_2)\rVert/\lVert x_1-x_2\rVert`$, largest value over 24 probes ($`x_1`$ a datum at four phases of the orbit, $`x_2=x_1+10^{-3}u`$ for six random unit directions; blue), against the draft’s bound $`e^{Lt}`$ (vermilion) and the revised $`e^{\mu t}`$ (green dashed). (b) The same for the parameter sensitivity $`\lVert \varphi(t;p_1,x_0)-\varphi(t;p_2,x_0)\rVert/\lVert p_1-p_2\rVert`$ ($`p_2=p^\star+10^{-3}u`$, $`u\in\mathbb R^{20}`$) against $`\frac{\tilde L}{L}(e^{Lt}-1)`$ and $`Q(t)=\frac{\tilde L}{\mu}(e^{\mu t}-1)`$. Both bounds hold at every probe; the revised one is $`10^8`$ closer at $`t=10`$ but still grows, while the measurement peaks at $`8.5`$ near the fast jump and then contracts because the orbit is attracting. (c) Local rates along one FHN period ($`39.5`$ time units): the local Lipschitz constant $`\lVert \partial f/\partial x\rVert_2`$ (dotted) never drops below 1; the Euclidean logarithmic norm $`\mu(x^\star(t))`$ (green) is near zero on the slow branches and reaches $`1.17`$ only during the two fast jumps; in the weighted norm the local $`\mu_D`$ (purple) is negative for $`75\%`$ of the period. All panels are evaluated at $`p^\star`$ along the true orbit, i.e. they probe the constants, they do not certify the suprema of (A0). Inherited from R003 Figure 1 (‘figures/R003_fig01_bounds.pdf‘).*


**Table 1.** Draft versus revised bounds on FHN, evaluated at $`t=\Delta T`$, against the largest value observed over the 24 probes of Figure <a href="#fig:lemmas" data-reference-type="ref" data-reference="fig:lemmas">7</a> for $`t\le\Delta T`$. The two bound columns are the Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and Lemma <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> factors with the orbit-measured constants $`L=3.05`$, $`\mu=1.17`$, $`\tilde L=10.56`$; the “observed peak” column is a finite sample of probes, not a supremum. Inherited from R003 Table 1.


| $t=\Delta T$ | Lemma 1 original $e^{L\Delta T}$ | Lemma 1 proposed $e^{\mu\Delta T}$ | Lemma 1 observed peak | Lemma 2 original $\frac{\tilde L}{L}(e^{L\Delta T}-1)$ | Lemma 2 proposed $\frac{\tilde L}{\mu}(e^{\mu\Delta T}-1)$ | Lemma 2 observed peak |
|---|---|---|---|---|---|---|
| 1 | 21.1 | 3.23 | 2.93 | 69.8 | 20.1 | 3.7 |
| 2 | 447 | 10.4 | 6.92 | $1.5\times10^{3}$ | 84.9 | 6.7 |
| 5 | $4.2\times10^{6}$ | 349 | 8.48 | $1.5\times10^{7}$ | $3.1\times10^{3}$ | 12.0 |
| 10 | $1.8\times10^{13}$ | $1.2\times10^{5}$ | 8.48 | $6.2\times10^{13}$ | $1.1\times10^{6}$ | 15.1 |

**Table 2.** Per-window *linearised endpoint* exponents $`e^{\Lambda_k}`$ computed from $`J_f(x^\star(t);p^\star)`$ along the true FHN orbit ($`\Delta t=1`$, windows aligned to $`t_0`$), Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">7</a>: worst and median window, in the Euclidean norm and in the weighted norm $`\lVert Dx\rVert`$; “contracting” is the share of windows with $`\Lambda^D_k<0`$. **Read this:** these are *not* rigorous bounds for the propositions. They are endpoint exponents of one linearised pair, so the transition bound of <a href="#eq:Mk" data-reference-type="eqref" data-reference="eq:Mk">(eq. Mk)</a> satisfies $`M_k\ge\max\{1,e^{\Lambda_k}\}`$, strictly wherever $`\mu(\cdot)`$ changes sign inside the window, and Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> needs $`M_k`$ — in fact the envelope $`\mathcal M`$ of <a href="#eq:envelopeM" data-reference-type="eqref" data-reference="eq:envelopeM">(eq. envelopeM)</a> — not $`e^{\Lambda_k}`$; see Remark <a href="#rem:envelopes" data-reference-type="ref" data-reference="rem:envelopes">5</a> for the four independent reasons the substitution is not licensed. Weighted values are statements in $`\lVert \cdot\rVert_D`$; the Euclidean counterpart is larger by at most $`\mathrm{cond}(D)=3.54`$ (Remark <a href="#rem:weighted" data-reference-type="ref" data-reference="rem:weighted">6</a>). Inherited from R003 Table 2.


| $\Delta T$ | windows | a priori $e^{\mu\Delta T}$ | Euclidean $e^{\Lambda_k}$ worst | Euclidean median | weighted $e^{\Lambda^D_k}$ worst | weighted median | contracting windows |
|---|---|---|---|---|---|---|---|
| 1 | 100 | 3.23 | 3.10 | 1.16 | 2.60 | 0.94 | 74 % |
| 2 | 50 | 10.4 | 8.56 | 1.41 | 5.89 | 0.88 | 70 % |
| 5 | 20 | 349 | 34.78 | 3.75 | 8.04 | 0.73 | 55 % |
| 10 | 10 | $1.2\times10^{5}$ | 138.79 | 29.25 | 5.89 | 3.20 | 30 % |

**Figure 8** (`figures/R002_fig10_prop1_node_removal.pdf`)

!8(figures/R002_fig10_prop1_node_removal.pdf)

***Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>: node removal changes the cost by a bounded amount.** Both panels start from the finest partition ($`\kappa=1`$, every datum a node, $`K=100`$, $`\gamma=0`$) and *remove* nodes, so both are genuine sub-partitions and both are within the scope of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> — unlike the stages of the $`\kappa`$-sweep (§<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a>). (a) Keep every $`m`$-th node, so $`|\mathcal I_R|=100-\lceil100/m\rceil`$ nodes are removed; the fine window length is $`\Delta T_1=\Delta t`$ *throughout*, and the quantity that grows with $`m`$ is the removed-node offset $`\Delta T_2=(m-1)\Delta t`$. (b) Remove one contiguous block of $`b`$ nodes after node 30, so $`|\mathcal I_R|=b`$, $`\Delta T_1=\Delta t`$ and $`\Delta T_2=b\Delta t`$ grows. Shown for $`p=p^\star`$ (green), a vector $`0.06`$ from $`p^\star`$ on six coefficients (blue) and $`p_{\rm wrong}`$ of Figure <a href="#fig:single" data-reference-type="ref" data-reference="fig:single">2</a> (vermilion). $`|\hat{\mathcal J}-J|`$ grows with $`|\mathcal I_R|`$ and with $`\Delta T_2`$, and much faster away from $`p^\star`$, as the proposition says; the rate $`e^{L\Delta T_2}`$ of the draft (grey dashed in (b)) is far steeper than the observed growth, which saturates once the removed block spans a full fast excursion. **Read this:** the producing script `02_concept.jl` and the R002 caption label the stride experiment’s growing quantity $`\Delta T_1=m\Delta t`$ and its fixed quantity $`\Delta T_2=\Delta t`$; under the definitions of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a> those two are *swapped*, and the CSV column `DeltaT1` holds the *coarse* window length ($`m\Delta t`$ in panel (a), $`(b+1)\Delta t`$ in panel (b)), not $`\Delta T_1`$. The caption above uses the manuscript’s definitions; the CSV column names are not corrected in place, since the result file is frozen. Inherited from R002 Figure 10.*


## The cost landscape as the window grows

**Figure 9** (`figures/R002_fig11_landscape_2d_v_v3.pdf`)

!9(figures/R002_fig11_landscape_2d_v_v3.pdf)

***Two-dimensional slices of $`J_\kappa`$ in the $`(v,v^3)`$ coefficient plane of the $`\dot v`$ equation.** All other coefficients at their true values, $`x_0=y_0`$, $`\gamma=0.05`$; star = truth $`(1,-1/3)`$; grey = blow-up plateau; colour is $`\log J`$. As $`\kappa`$ grows from 1 to 100 the basin around the truth narrows by an order of magnitude and the plateau expands: any positive cubic coefficient makes $`\dot v\sim cv^3`$ blow up in finite time, and at $`\kappa=100`$ even $`v`$-coefficients above $`1.6`$ do. This is the geometric reason the control arm fails at large $`\kappa`$. The grey region is where the *implemented* loss returns its flat penalty; no inference about $`P`$ or $`\partial P`$ is drawn from it (Remark <a href="#rem:impl" data-reference-type="ref" data-reference="rem:impl">3</a>(iv)). Inherited from R002 Figure 11.*


**Figure 10** (`figures/R002_fig13_landscape_1d_slices.pdf`)

!10(figures/R002_fig13_landscape_1d_slices.pdf)

***One-dimensional slices along random directions and the number of local minima.** Top: $`J_\kappa(p^\star+s\,u_d)`$ for three random unit directions $`u_d\in\mathbb R^{20}`$ (light to dark $`=\kappa=1,2,5,10,25,50,100`$; gaps = plateau); top right: local minima per slice, mean over 12 directions (band = min–max), rising from $`1.25`$ at $`\kappa=1`$ to $`7.9`$ at $`\kappa=100`$. Bottom: the full $`s\in[-2,2]`$ range for direction 1. Larger windows sharpen the minimum but add spurious minima and cut the finite region down to $`|s|\lesssim0.5`$. Inherited from R002 Figure 13.*


**Figure 11** (`figures/R002_fig14_hessian_conditioning.pdf`)

!11(figures/R002_fig14_hessian_conditioning.pdf)

***Curvature of the data term at $`p^\star`$ versus window size.** (a) Largest and smallest eigenvalues of the Hessian of the *data term* ($`\gamma=0`$) at $`p^\star`$ in the $`n_p=20`$ parameters, and the data-term gradient norm: $`\lambda_{\max}`$ grows from $`36`$ ($`\kappa=1`$) to $`\ensuremath{1.2\times 10^{6}}`$ ($`\kappa=100`$) while $`\lVert \nabla J_\kappa(p^\star)\rVert`$ grows from $`0.04`$ to $`96`$; with noisy data the truth is not a stationary point and drifts away from the minimiser as $`\kappa`$ grows. (b) Number of negative eigenvalues: the noisy *data term* is indefinite at $`p^\star`$ for every $`\kappa\ge2`$ (up to $`6`$ of $`20`$ directions), so the strong-convexity premise of Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> and Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> cannot hold near $`p^\star`$; note that the optimised objective additionally carries the $`\gamma=0.05`$ penalty, whose Hessian is not included here (Remark <a href="#rem:impl" data-reference-type="ref" data-reference="rem:impl">3</a>(iii)). (c) The dependence on the free variable $`x_0`$: by the dummy-variable argument of §<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a> this is exactly the parabola $`\lVert x_0-y_0\rVert^2/N`$, identical at every $`\kappa`$; it is a property of the objective’s parametrisation, not of the landscape. Inherited from R002 Figure 14.*


## Guess propagation versus no propagation: the headline experiment

Protocol: $`8`$ optimiser seeds $`\times`$ 3 arms $`\times`$ the 16-stage FULL schedule, Nelder–Mead with 2500 iterations per stage, $`\gamma=0.05`$, $`S=10`$, flat penalty. Table <a href="#tab:T1" data-reference-type="ref" data-reference="tab:T1">3</a> gives the seed medians and the paired statistics per stage; Figures <a href="#fig:headline" data-reference-type="ref" data-reference="fig:headline">12</a>–<a href="#fig:coef" data-reference-type="ref" data-reference="fig:coef">15</a> show them.

**Table 3.** Headline sweep, seed medians per stage: cost $`J`$, parameter error $`\lVert p-p^\star\rVert`$ and recovery score for GP, best-GP and the control; the mean paired difference GP $`-`$ control in $`\lVert p-p^\star\rVert`$ with its 95 % bootstrap CI over the 8 seeds (resampling unit: the optimiser seed); the number of seeds in which GP is strictly better; the number of GP seeds on the plateau. Inherited from R002 Table T1 (`tables/T1_headline.tex`).


| κ | J GP | J best-GP | J control | ‖p−p*‖ GP | ‖p−p*‖ best-GP | ‖p−p*‖ control | score GP | score control | GP−control | CI lo | CI hi | #GP better | #GP on plateau |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 0.02569 | 0.02569 | 0.02569 | 0.8641 | 0.8641 | 0.8641 | 0.4 | 0.4 | 0 | 0 | 0 | 0 | 3 |
| 2 | 0.02081 | 0.02081 | 0.03726 | 0.3803 | 0.3803 | 1.314 | 0.75 | 0.275 | -0.7724 | -1.112 | -0.4196 | 7 | 1 |
| 3 | 0.01864 | 0.019 | 0.05955 | 0.3748 | 0.3893 | 1.466 | 0.75 | 0.225 | -0.8679 | -1.309 | -0.4865 | 7 | 1 |
| 4 | 0.01549 | 0.01577 | 0.09718 | 0.3325 | 0.3615 | 1.51 | 0.775 | 0.25 | -0.9232 | -1.248 | -0.5652 | 7 | 1 |
| 5 | 0.01906 | 0.01889 | 0.1259 | 0.2218 | 0.221 | 1.543 | 0.775 | 0.2 | -1.133 | -1.654 | -0.6627 | 7 | 1 |
| 6 | 0.02114 | 0.02054 | 0.2428 | 0.3441 | 0.4522 | 1.716 | 0.55 | 0.2 | -1.159 | -1.526 | -0.7401 | 7 | 1 |
| 8 | 0.0161 | 0.0154 | 0.3185 | 0.3593 | 0.3964 | 1.59 | 0.675 | 0.275 | -1.113 | -1.36 | -0.7559 | 7 | 1 |
| 10 | 0.01406 | 0.01438 | 0.3086 | 0.2995 | 0.3347 | 1.737 | 0.8 | 0.2 | -1.234 | -1.634 | -0.7787 | 7 | 1 |
| 12 | 0.01556 | 0.0155 | 0.6561 | 0.278 | 0.3312 | 1.622 | 0.8 | 0.25 | -1.163 | -1.415 | -0.7988 | 7 | 1 |
| 15 | 0.01414 | 0.01409 | 0.5398 | 0.3348 | 0.3402 | 1.574 | 0.725 | 0.275 | -1.109 | -1.327 | -0.7739 | 7 | 1 |
| 20 | 0.01436 | 0.01459 | 0.5358 | 0.2425 | 0.2709 | 1.612 | 0.8 | 0.25 | -1.236 | -1.547 | -0.8214 | 7 | 1 |
| 25 | 0.01345 | 0.01362 | 0.861 | 0.3113 | 0.3455 | 1.534 | 0.85 | 0.25 | -1.111 | -1.401 | -0.7455 | 7 | 1 |
| 33 | 0.01375 | 0.01373 | 0.8233 | 0.3372 | 0.3758 | 1.498 | 0.825 | 0.225 | -0.9927 | -1.358 | -0.5627 | 7 | 1 |
| 50 | 0.01327 | 0.01334 | 1.334 | 0.3353 | 0.3393 | 1.807 | 0.8 | 0.2 | -1.63 | -2.426 | -0.9189 | 7 | 1 |
| 75 | 0.0128 | 0.01289 | 1.392 | 0.3223 | 0.3179 | 1.793 | 0.8 | 0.225 | -1.167 | -1.522 | -0.7642 | 7 | 1 |
| 100 | 0.01273 | 0.01288 | 1.506 | 0.3216 | 0.3105 | 1.649 | 0.8 | 0.25 | -1.138 | -1.426 | -0.7719 | 7 | 1 |

**Figure 12** (`figures/R002_fig15_sweep_headline.pdf`)

!12(figures/R002_fig15_sweep_headline.pdf)

***Guess propagation versus no propagation, $`8`$ seeds.** (a) Cost, (b) parameter error and (c) recovery score at the minimiser of each stage; thin lines = seeds, thick = median; blue circles GP, green triangles best-GP, vermilion squares control. All arms coincide at $`\kappa=1`$ (same start). From $`\kappa=2`$ on, GP drops to $`\lVert p-p^\star\rVert\approx0.22`$–$`0.38`$ and ends single shooting at $`0.32`$ (score $`0.80`$), whereas the control climbs back to the seed distance ($`1.65`$ at $`\kappa=100`$; $`25\%`$ of its seeds end on the plateau, the others in spurious minima). One GP seed starts on the plateau at $`\kappa=1`$ and never moves (flat line at $`1.84`$). The parameter error is not monotone: it is smallest at $`\kappa=5`$ ($`0.22`$) and drifts up to $`0.32`$ while the cost keeps falling, the noise-induced bias of Figure <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a>. Inherited from R002 Figure 15.*


**Figure 13** (`figures/R002_fig16_sweep_paired.pdf`)

!13(figures/R002_fig16_sweep_paired.pdf)

***Paired comparison.** (a) Mean paired difference in $`\lVert p-p^\star\rVert`$ (GP minus control, vermilion; GP minus best-GP, green) with 95 % bootstrap CIs over the 8 seeds: the GP–control difference is $`-1.14`$ $`[-1.43,-0.77]`$ at $`\kappa=100`$ and its CI excludes zero at every $`\kappa\ge2`$; GP and best-GP are statistically indistinguishable. (b) Cellwise counts: GP is strictly better than the control in 7 of 8 seeds at every $`\kappa\ge2`$ and tied in the eighth (the plateau seed). Inherited from R002 Figure 16.*


**Figure 14** (`figures/R002_fig18_sweep_filmstrip.pdf`)

!14(figures/R002_fig18_sweep_filmstrip.pdf)

***Filmstrip of the sweep (seed 2).** Single-shooting simulation of $`v(t)`$ from the minimiser returned at $`\kappa=1,5,25,100`$ (columns) for GP (top, blue) and the control (bottom, vermilion); grey dashed = truth, dots = data; the parameter error is printed in each panel. The GP fit tracks the data at every stage; the control loses the trajectory as soon as the windows are long. Inherited from R002 Figure 18.*


**Figure 15** (`figures/R002_fig20_final_coefficients.pdf`)

!15(figures/R002_fig20_final_coefficients.pdf)

***Coefficients at the end of the sweep ($`\kappa=100`$).** Truth (grey), GP seed-median (blue) and control seed-median (vermilion) with the 8 seeds as dots, in library order. GP recovers the support (7 non-zeros, no false positive in the median) with a shrinkage of the $`w`$ coefficient of $`\dot v`$ due to the smooth-$`\ell_1`$ term at $`\gamma=0.05`$ — the bias bounded, under (C1)–(C2), by <a href="#eq:penbias" data-reference-type="eqref" data-reference="eq:penbias">(eq. penbias)</a>; the control’s median is near zero everywhere because its seeds scatter. Inherited from R002 Figure 20.*


## Variations

Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a> summarises the variation experiments of R002 (SHORT schedule unless stated; the schedule rows change the number of stages by design, and the optimiser rows do different work per iteration, so those comparisons are not at equal budget). Three readings matter for the method. Removing few nodes per step matters: DENSE ends at $`0.30`$, COARSE at $`0.52`$ and JUMP at $`0.76`$ (Figure <a href="#fig:schedule" data-reference-type="ref" data-reference="fig:schedule">16</a>), which is the design rule of Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> in practice. The graded blow-up penalty removes the “stuck on the plateau” failure (GP $`0.27`$, no seed on the plateau). With the exact gradient, BFGS with guess propagation is the best and cheapest arm ($`0.21`$, score $`0.95`$; Figure <a href="#fig:opt" data-reference-type="ref" data-reference="fig:opt">17</a>), while BFGS without propagation fails because the gradient vanishes on the plateau. GP is robust up to 10 % noise ($`0.31`$) and fails at 20 % ($`1.88`$); the control fails even at zero noise.

**Table 4.** Selected variation experiments (seed medians at $`\kappa=100`$). Derived from R002 Table T2 (`external_data/tables/T2_variations.csv`); the full table has 55 rows. Schedule and optimiser rows are *not* at equal budget, as the text says. Inherited from R002.


| variation (arm) | seeds | $\|p-p^\star\|$ at $\kappa=100$ | score | seeds on plateau |
|---|---|---|---|---|
| schedule DENSE (44 stages) | 8 | 0.30 | 0.80 | 12 % |
| schedule COARSE (4 stages) | 8 | 0.52 | 0.55 | 12 % |
| schedule JUMP (2 stages) | 8 | 0.76 | 0.42 | 12 % |
| graded blow-up penalty, GP | 8 | 0.27 | 0.82 | 0 % |
| graded blow-up penalty, control | 8 | 1.63 | 0.30 | 0 % |
| noise 0 %, GP | 6 | 0.23 | 0.80 | 17 % |
| noise 10 %, GP | 6 | 0.31 | 0.62 | 17 % |
| noise 20 %, GP | 6 | 1.88 | 0.23 | 17 % |
| noise 0 %, control | 6 | 1.63 | 0.25 | 33 % |
| $\gamma=0$, GP | 6 | 0.81 | 0.38 | 17 % |
| $\gamma=0.05$, GP | 6 | 0.22 | 0.80 | 17 % |
| $\gamma=1$, GP | 6 | 1.01 | 0.60 | 17 % |
| BFGS, GP | 8 | 0.21 | 0.95 | 12 % |
| BFGS, control | 8 | 1.65 | 0.23 | 62 % |
| Nelder–Mead 1000 iterations, GP | 4 | 0.61 | 0.57 | 25 % |
| Nelder–Mead 5000 iterations, GP | 4 | 0.20 | 0.90 | 25 % |
| integrator sub-steps $S=1$, GP | 4 | 0.50 | 0.75 | 0 % |
| integrator sub-steps $S=5$, GP | 4 | 0.29 | 0.82 | 25 % |

**Figure 16** (`figures/R002_fig21_schedule.pdf`)

!16(figures/R002_fig21_schedule.pdf)

***Window-size schedule.** Parameter error along the sweep for the DENSE (44 stages), FULL (16), COARSE (4) and JUMP (2) schedules, 8 seeds; removing few nodes per step matters. Note that the fully nested schedules (COARSE, JUMP) are the ones Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> covers stage by stage, and they are the ones that perform *worst*; the schedules that perform best are covered only through the common refinement of Corollary <a href="#cor:repartition" data-reference-type="ref" data-reference="cor:repartition">6</a> (§<a href="#sec:protocol" data-reference-type="ref" data-reference="sec:protocol">2.3</a>). Inherited from R002 Figure 21.*


**Figure 17** (`figures/R002_fig25_optimizers.pdf`)

!17(figures/R002_fig25_optimizers.pdf)

***Optimisers.** Nelder–Mead versus BFGS and L-BFGS (automatic-differentiation gradient through the windowed loss), with and without guess propagation; wall time per stage is machine-dependent and only comparative. BFGS with propagation is the best and cheapest arm; without propagation the gradient methods die on the plateau. Inherited from R002 Figure 25.*


## Other systems

The implementation is dimension- and degree-agnostic; Table <a href="#tab:T5" data-reference-type="ref" data-reference="tab:T5">5</a> and Figures <a href="#fig:lv" data-reference-type="ref" data-reference="fig:lv">18</a>–<a href="#fig:lorenz" data-reference-type="ref" data-reference="fig:lorenz">19</a> apply the same sweep to Lotka–Volterra ($`\dot x=x-0.5xy`$, $`\dot y=-0.8y+0.3xy`$; quadratic library, 12 coefficients, $`N=101`$, $`\Delta t=0.25`$, 5 % noise, Nelder–Mead, 6 seeds) and Lorenz ($`\sigma=10`$, $`\rho=28`$, $`\beta=8/3`$; quadratic library, 30 coefficients, $`N=101`$, $`\Delta t=0.02`$, 2 % noise, 4 seeds; BFGS and Nelder–Mead). GP recovers Lotka–Volterra ($`0.29`$, score $`0.83`$; the control ends at $`1.41`$ with $`83\%`$ of its seeds on the plateau) and, with BFGS, Lorenz ($`3.9`$ on coefficients of size up to 28, score $`0.83`$); GP with Nelder–Mead does not converge in 33 dimensions ($`18.1`$, score $`0.23`$), so a derivative-free sweep does not scale.

**Table 5.** Other systems, seed medians at $`\kappa=100`$. Inherited from R002 Table T5.


| system | optimizer | arm | n_seeds | ‖p−p*‖ (κ=100) | score | plateau frac |
|---|---|---|---|---|---|---|
| lorenz | bfgs | propagate | 4 | 3.852 | 0.8333 | 0 |
| lorenz | bfgs | reset | 4 | 31.95 | 0.03333 | 1 |
| lorenz | nm | propagate | 4 | 18.11 | 0.2333 | 0 |
| lorenz | nm | reset | 4 | 31.95 | 0.03333 | 1 |
| lv | nm | propagate | 6 | 0.29 | 0.8333 | 0 |
| lv | nm | reset | 6 | 1.408 | 0.2917 | 0.8333 |

**Figure 18** (`figures/R002_fig35_lotka_volterra.pdf`)

!18(figures/R002_fig35_lotka_volterra.pdf)

***Lotka–Volterra.** (a) Parameter error along the sweep (thin: 6 seeds, thick: median), (b) score, (c) the $`x(t)`$ fit at $`\kappa=100`$ for seed 1. Inherited from R002 Figure 35.*


**Figure 19** (`figures/R002_fig36_lorenz.pdf`)

!19(figures/R002_fig36_lorenz.pdf)

***Lorenz (chaotic, 30 coefficients).** (a) Parameter error along the sweep for BFGS (solid) and Nelder–Mead (dashed), GP (blue) and control (vermilion), 4 seeds; (b) score; (c) the $`x(t)`$ fit at $`\kappa=100`$, seed 1; (d) seed-median GP coefficients versus truth. Inherited from R002 Figure 36.*


## Empirical diagnostics for the premises of the theory

**Figure 20** (`figures/R002_fig37_post_hessian_path.pdf`)

!20(figures/R002_fig37_post_hessian_path.pdf)

***Empirical diagnostics at the minimisers.** (a) Smallest eigenvalue of the *data-term* Hessian ($`\gamma=0`$) at the GP minimiser of the *penalised* objective ($`\gamma=0.05`$) returned by Nelder–Mead at every stage, 7 seeds (plateau seed excluded; blue = positive definite, vermilion = at least one negative eigenvalue): the returned point is positive definite in only $`36\%`$ of the (seed, $`\kappa`$) cells, with on average less than one of 20 eigenvalues slightly negative elsewhere. This is an *empirical diagnostic*: it does not check strong convexity of the optimised objective on a neighbourhood, since it evaluates a different function (no penalty), at a single point rather than on a neighbourhood, at an iterate whose stationarity is not verified. The premise (A3) of Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> and the strong-convexity hypothesis of Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> are therefore *not verified* at these iterates (the BFGS iterates would be the place to test them). (b) The next stage’s cost along the straight chord from $`p^{(\kappa_i)}`$ to $`p^{(\kappa_{i+1})}`$ (seed 2, 21 sampled points), relative to its value at the carried guess: it decreases monotonically along every sampled chord, which is a *necessary condition for*, not a proof of, the carried guess lying in the basin of the next minimiser. (c) Cross-evaluation: the minimiser of $`\kappa_i`$ evaluated at every other $`\kappa_j`$; the near-diagonal structure shows that a minimiser is good only for nearby window sizes, which is why the schedule must be gradual. Inherited from R002 Figure 37.*


# Discussion

The analysis and the experiments agree on the mechanism, with the analysis conditional throughout. Short windows bound the amplification of the initial-condition error by $`e^{\mu\Delta T}`$ (Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a>, Proposition <a href="#prop:cost" data-reference-type="ref" data-reference="prop:cost">1</a>), which keeps the landscape smooth (Figures <a href="#fig:gp" data-reference-type="ref" data-reference="fig:gp">5</a>, <a href="#fig:land1d" data-reference-type="ref" data-reference="fig:land1d">10</a>) at the price of weak discrimination between nearby parameters (Figure <a href="#fig:multi" data-reference-type="ref" data-reference="fig:multi">4</a>c); removing few nodes at a time moves the cost, and hence the minimiser, by a bounded amount (Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a>, Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a>, Figures <a href="#fig:prop1" data-reference-type="ref" data-reference="fig:prop1">8</a>, <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>b), so — under the hypotheses of Corollary <a href="#cor:basin" data-reference-type="ref" data-reference="cor:basin">5</a> — the minimiser of the restricted coarse problem stays near the carried guess while the windows grow into single shooting (Figures <a href="#fig:headline" data-reference-type="ref" data-reference="fig:headline">12</a>–<a href="#fig:paired" data-reference-type="ref" data-reference="fig:paired">13</a>). The revised constants are of the right kind (they can saturate, and for the true FHN field at $`p^\star`$ the upper one is global in $`x`$), and along the orbit the linearised per-window endpoint exponents of Corollary <a href="#cor:window" data-reference-type="ref" data-reference="cor:window">7</a> are within a factor of the measurement; the remaining gap to the observed plateau is intrinsic to worst-case analysis.

Four limitations are visible in the data and in the hypotheses. The strong-convexity premise is not verified at the iterates a derivative-free optimiser returns (Figure <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a>a), so Theorem <a href="#thm:main" data-reference-type="ref" data-reference="thm:main">1</a> explains the observed behaviour rather than certifying it; with noisy data the truth is not a stationary point of any $`J_\kappa`$ and the parameter error is non-monotone along the sweep (Figures <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a>, <a href="#fig:headline" data-reference-type="ref" data-reference="fig:headline">12</a>); the flat blow-up penalty can trap a start on the plateau at the very first stage, a defect of the penalty rather than of the propagation that the graded penalty removes (Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a>); and, most consequentially, the two premise checks of §<a href="#sec:prov" data-reference-type="ref" data-reference="sec:prov">8</a> are unevaluated, so every statement of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> is conditional on an admissible region and an event this report has not instantiated, and on a reconnection hypothesis relating the constrained minimisers of the theory to the unconstrained iterates reported here. Gradient optimisers with guess propagation are the recommended configuration (Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a>, Figure <a href="#fig:lorenz" data-reference-type="ref" data-reference="fig:lorenz">19</a>).

# Provenance and gates

This report contains no new numerics. Its 20 figures are byte-identical copies of R002 Figures 1–6, 10, 11, 13–16, 18, 20, 21, 25, 35–37 and R003 Figure 1 (prefixed by their source report in `figures/`); the three hand-drawn figures of the draft (`shooting_partition.png`, `multiple_shooting.png`, `comparison_shooting_nodes.png`) and R002’s schematic Figure 7 (drawn with the draft’s $`e^{Lt}`$ envelope) are retired. Tables <a href="#tab:lemmas" data-reference-type="ref" data-reference="tab:lemmas">1</a>, <a href="#tab:windows" data-reference-type="ref" data-reference="tab:windows">2</a>, <a href="#tab:T1" data-reference-type="ref" data-reference="tab:T1">3</a> and <a href="#tab:T5" data-reference-type="ref" data-reference="tab:T5">5</a> are verbatim copies; Table <a href="#tab:T2" data-reference-type="ref" data-reference="tab:T2">4</a> and every number in the prose are regenerated by `analysis/make_tables.py` from the R002/R003 result files copied into `external_data/` (SHA-256 in `INPUTS.md`). `analysis/verify_R004.py` writes `analysis/results/gates_summary.json`:

- G1: every file in `external_data/` matches its hash in `INPUTS.md`, and every hash equals that of the file in the source report’s folder (byte identity with R002/R003).

- G2: every figure in `figures/` is byte-identical to its source figure in R002/R003, and there are exactly 20 of them.

- G3: every figure referenced by `report.tex` exists, the references are 20 distinct files, and no retired figure is referenced.

- G4: the verbatim tables in `tables/` are byte-identical to their sources.

- G5: `tables/numbers.tex`, `numbers.json` and `T2_compact.tex` regenerate byte-identically from `external_data/`.

- G6: the prose numbers agree with R002’s own `table_numbers.json` and R003’s `summary.json` to the printed precision.

- G7: the revised *lemma* constants never exceed the draft’s with the FHN numbers: $`\mu\le L`$, and $`e^{\mu s}\le e^{Ls}`$ and $`\frac{\tilde L}{\mu}(e^{\mu s}-1)\le\frac{\tilde L}{L}(e^{Ls}-1)`$ at $`s\in\{1,2,5,10\}`$. **Read this:** this gate compares Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a> and Lemma <a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> factors only. It does *not* compare the Proposition <a href="#prop:removal" data-reference-type="ref" data-reference="prop:removal">3</a> product over $`(\Delta T_1,\Delta T_2)`$ pairs; an earlier version of this bullet said it did, which was a misdescription of the gate that exists.

- G8: no probe violates the revised Lemma <a href="#lem:state" data-reference-type="ref" data-reference="lem:state">5</a>/<a href="#lem:param" data-reference-type="ref" data-reference="lem:param">6</a> bounds at any of the $`2\times2424`$ probe points of R002 (recomputed from the copied CSVs): the state ratios lie in $`[e^{\mu_-t},e^{\mu t}]`$ and the parameter ratios below $`Q(t)`$. **Read this:** this is a finite check, not a verification of the suprema in (A0); the lower-bound half uses the orbit-restricted $`\mu_-=-2.96`$ and the probes all start on the orbit.

- G9: `report.md` contains every section heading, table and figure caption of `report.tex`.

- G10: `report.pdf` exists, is newer than `report.tex`, and its page count is recorded.

Outcome: see `tables/gates_status.tex` (inlined at build time: 10 of 10 gates pass). Regeneration: `REPRODUCE.md`.

#### Open theoretical-premise checks.

The following two items are *not* gates and are not counted among G1–G10: those ten are mechanical inheritance checks on files, hashes, figures and regenerated numbers, all of which pass, and they are the ones recorded in `gates_summary.json`. The items below are premises of the *theory* of §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> that this report has not evaluated. They are listed separately so that an unevaluated premise is not mistaken for a broken inheritance check, and so that no future revision “repairs” one by weakening it.

- **P1 (premise check — *not evaluated*).** The closure condition (A1), <a href="#eq:closure" data-reference-type="eqref" data-reference="eq:closure">(eq. closure)</a>–<a href="#eq:closure_two" data-reference-type="eqref" data-reference="eq:closure_two">(eq. closure_two)</a>, has not been evaluated for any $`\kappa`$. Evaluating it requires instantiating a tube radius $`r_X`$, recomputing $`\mu`$ and $`\tilde L`$ as suprema over the resulting $`X\times P`$, fixing $`\operatorname{diam}(P)`$, and exhibiting $`r'>0`$ satisfying both (A1-i) and (A1-ii). None of these has been instantiated numerically. The values $`\mu=1.17`$, $`\tilde L=10.56`$ quoted in §<a href="#sec:defs" data-reference-type="ref" data-reference="sec:defs">2</a> were measured over the probe box of R002/R003 at $`p^\star`$, which is *not* the $`X\times P`$ of (A0); they are therefore *indicative*, and the order-of-magnitude table computed from them is not a check of (A1). What inserting them into the binding condition (A1-i), $`Q(\Delta T_{\max})\operatorname{diam}(P)<r_X`$, shows is that the probe-region constants produce enormous factors for long windows — which *motivates*, but does not *establish*, the infeasibility of a useful admissible region at those window lengths. *Status: unverified, not failed* — a condition evaluated with constants from the wrong region has not been evaluated. Consequently every statement of §<a href="#sec:theory" data-reference-type="ref" data-reference="sec:theory">3</a>–§<a href="#sec:removal" data-reference-type="ref" data-reference="sec:removal">4</a> is conditional on an event, $`E_X`$, whose probability this report does not bound numerically, at *every* stage of the schedule.

- **P2 (premise check — *not evaluated*).** The reconnection hypothesis (R) of Remark <a href="#rem:reconnect" data-reference-type="ref" data-reference="rem:reconnect">1</a> — the existence of a compact convex $`P`$ satisfying (A0)–(A1), a convex $`U\subseteq P`$ containing the reported optimum in its interior, stationarity of $`J_K`$ there, and $`m`$-strong convexity of $`J_K`$ on $`U`$ (together with $`p^\star\in U`$ for the variant (R$`^{p^\star}`$) that Proposition <a href="#prop:perr" data-reference-type="ref" data-reference="prop:perr">2</a> needs) — has not been evaluated. $`P`$, $`X`$, $`r_X`$ and $`\operatorname{diam}(P)`$ are not instantiated anywhere in this report, the optimisers of §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a> are unconstrained, and the Hessian panels of Figures <a href="#fig:hessian" data-reference-type="ref" data-reference="fig:hessian">11</a> and <a href="#fig:post" data-reference-type="ref" data-reference="fig:post">20</a> are measurements at particular points, not a strong-convexity certificate on a named convex set. Note also that (R) speaks of the unpenalised, exact-flow $`J_K`$, whereas the runs minimise the penalised, discretised <a href="#eq:Jkappa" data-reference-type="eqref" data-reference="eq:Jkappa">(eq. Jkappa)</a>: stationarity of the penalised numerical objective does not imply the stationarity (R) asserts. Until (R), the event $`E_X`$ and those two qualifications are established, the displacement, basin-retention and penalty-bias statements apply to the *constrained* minimisers of <a href="#eq:minimisers" data-reference-type="eqref" data-reference="eq:minimisers">(eq. minimisers)</a> and not to the numbers reported in §<a href="#sec:num" data-reference-type="ref" data-reference="sec:num">6</a>.
